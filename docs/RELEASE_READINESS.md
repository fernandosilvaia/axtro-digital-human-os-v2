# Prontidão de release — 2026-08-11 (pós D-V2-110: backlog P2/P3 fechado — 0 achados abertos das ondas 1-2)

## Notas (0–100, com evidência — não é opinião)

| Dimensão | Nota | Evidência / o que segura a nota |
|---|---|---|
| Funcionalidade | 91 | Fluxos principais e2e verdes (CI, 15 specs, build de produção); reunião externa agendada nunca validada com humano real; **corrigido**: lockout de billing — status `unpaid`/`incomplete_expired`/`paused` travava a conta sem conseguir assinar nem gerenciar (D-V2-107, achado P1 confirmado por 3 verificadores) |
| Facilidade de uso | 86 | Demo 1-clique, auto-provisão, copy corrigida, checkout com estado pendente; checklist do dashboard trocou o item "sempre verdadeiro" por sinal real de engajamento (D-V2-107); botão "Revogar" convite ganhou loading/erro visível, consistente com o resto de /configuracoes (D-V2-110) |
| Onboarding | 80 | Signup→agente ativo em minutos; checklist agora mede "testou o agente no chat" (usage.services_7d) em vez de "conta existe" — ainda sem passo-a-passo guiado além do checklist |
| Design | 88 | Sistema visual consistente, validado mobile+desktop; `/precos` linkada na nav/footer/sitemap; `overflow-wrap` corrigido nas bolhas de chat/transcrição — string longa sem espaço não estoura mais o card (D-V2-110) |
| Mobile | 87 | Landing e workspace verificados 375px; sidebar fechada fora da árvore de tabulação/leitor de tela (D-V2-105) |
| Acessibilidade | 80 | Labels/aria nos forms, aria-live no chat, sidebar mobile não-tabulável quando fechada, contraste de `--text-faint` em AA (D-V2-105) |
| Frontend | 88 | Error boundary do workspace + **novo**: error boundary das rotas públicas de auth (/login, /signup, /recuperar-senha, /nova-senha — D-V2-107, chamam Supabase Auth direto e antes caíam na tela crua do Next em inglês) |
| Backend | 91 | Ports/adapters, falha-fechada, idempotência nos caminhos críticos; billing.ts e a UI de /configuracoes agora usam a MESMA classificação de status de assinatura (D-V2-107); `UUID_V7_PATTERN` (validação do id que fundamenta o isolamento de tenant) deduplicado de 14 cópias locais pra fonte única em `@axtro/domain`, em 10 pacotes (D-V2-110) |
| Banco | 91 | RLS forçada 100% nas tabelas novas, guard monotônico de status; `tenant_isolation` das tabelas do kernel documentado corretamente como código morto (D-V2-105) |
| Segurança | 91 | Sem P0; auditoria adversarial 2026-08-11 confirmou 2 P1 (lockout de billing, /privacidade desatualizada pós D-V2-106) e 1 P2 real (SECRET_PATTERN não cobria formato de chave Stripe), todos corrigidos na mesma rodada; riscos residuais em SECURITY_REVIEW |
| Performance | 80 | Build ok, rotas dinâmicas enxutas; sem medição formal de p95 web |
| Confiabilidade | 87 | Degradação declarada em todo caminho; watchdogs; telemetria de fallback; **corrigido**: healthcheck do Railway apontava pra `/login` (página 100% estática, nunca detectaria um deploy com env var de Supabase quebrada) — agora aponta pra `/api/health` (D-V2-107) |
| Custos | 88 | Todo caminho pago com teto + ledger; catálogo de teste (1 Meter + 3 Products + 6 Prices) provisionado de verdade na Stripe e validado contra a API real; alertas proativos por e-mail em 80%/100% dos 4 tetos diários (D-V2-107); **achado crítico**: o ledger de custo do chat de teste vinha sendo silenciosamente subcontado em ~90% das chamadas reais desde 2026-08-05 (bug de double-rounding em `portal_log_ai_usage`, D-V2-109) — corrigido, migration 0033 aguardando aplicação |
| IA | 88 | Eval golden pelo adapter real; injeção de percepção fechada; RAG do brain fechado (D-V2-108) — `portal_search_knowledge_service` espelha exatamente a busca já usada no chat de teste, agora também no caminho de vídeo (Tavus) |
| Integrações | 88 | Tavus/OpenRouter/Recall com timeout/erros tipados/testes; Stripe com idempotência de checkout — chave de teste validada contra a API real (GET /v1/balance, 2026-08-10), catálogo real criado; checkout ponta a ponta ainda não rodado (falta env var no Railway); **corrigido**: `provider-recall` e `provider-stripe` tinham o mesmo bug de timeout que só protegia a fase de HEADERS, não a leitura do corpo — um corpo travado após um 200/201 nunca era interrompido (D-V2-109, mesmo padrão já corrigido em `provider-openrouter`/`provider-tavus` desde 2026-08-02, mas que ficou de fora daquela rodada para estes dois) |
| Testes | 91 | 639 Node + 26 Py + 15 e2e em CI contra build de produção; **novo**: 20 testes para `chunkContent`/`embedChunks`/`embedQuery` (caminho pago de ingestão de conhecimento, zero cobertura até D-V2-109) + 4 testes de regressão de timeout (recall/stripe) + 5 testes para `ensureTenantProvisioned` (D-V2-110) |
| Observabilidade | 87 | Logs estruturados com redaction (agora cobrindo também formato de chave Stripe); telemetria de degradação; alerta proativo de custo implementado (D-V2-107) |
| Infraestrutura | 87 | Railway + domínio próprio (`closer.axtroai.com`) + healthcheck real (`/api/health`, corrigido de `/login` — D-V2-107); rollback = redeploy de commit anterior |
| Prontidão comercial | 89 | Cadastro→uso→limites→legal→preço→checkout→cobrança ok em código, migrations 0025-0032 aplicadas, catálogo Stripe teste real provisionado, lockout de billing corrigido; falta as env vars no Railway + 1 assinatura de teste ponta a ponta + aplicar a 0033 (correção do ledger de custo) antes de considerar o funil comercialmente confiável |
| **Geral** | **88** | |

## O que bloqueia cada nível seguinte

- **Primeiros clientes pagantes**: ~~(1) criar conta Stripe e rodar `scripts/stripe_setup.mjs` em modo teste~~ — feito 2026-08-10 (Meter + 3 Products + 6 Prices reais, `livemode: false` confirmado). (2) configurar `STRIPE_SECRET_KEY`/`STRIPE_WEBHOOK_SECRET`/6 price ids no Railway (bloqueado — ação em infraestrutura compartilhada, aguardando o Fernando colar no dashboard); (3) fazer uma assinatura de teste ponta a ponta (checkout → webhook → painel mostrando o plano) antes de cobrar de cliente de verdade; (4) 1 reunião externa agendada validada com humano. ~~(5) aplicar as migrations 0031 (alertas de custo) e 0032 (RAG do brain)~~ — aplicadas 2026-08-11, autorizadas explicitamente pelo Fernando, verificadas via `execute_sql`. (6) aplicar a migration **0033** (corrige o ledger de custo do chat de teste, achado P0 de D-V2-109) — escrita, aguardando autorização. As migrations 0025-0032 já estão aplicadas no Supabase hospedado.
- **Escala inicial**: migrar Stripe de modo teste pra produção (gate humano — chave `sk_live_`); DPIA percepção; bake-off formal de providers (D-V2-048). ~~Alertas proativos de custo~~ — implementado e aplicado (D-V2-107). ~~RAG no cérebro custom~~ — implementado e aplicado (D-V2-108). ~~Persistência de transcript/histórico de conversa~~ — implementada e em produção (D-V2-106).
- ~~Backlog de qualidade da auditoria D-V2-109~~ — fechado (D-V2-110): `ensureTenantProvisioned` testado, `UUID_V7_PATTERN` deduplicado, botão "Revogar" convite com loading/erro, bolhas de chat com `overflow-wrap`. Nenhum achado aberto das ondas 1-2 do `/loop` autônomo restante.
