# Decision Log V2

| ID | Data | Decisão | Motivo | Reversibilidade |
|---|---|---|---|---|
| D-V2-001 | 2026-07-14 | Digital Human OS como plataforma; Sales Closer como Role Pack | evitar acoplamento do kernel a vendas | alta |
| D-V2-002 | 2026-07-14 | Codex-first handoff com AGENTS.md e task graph | Claude sem créditos; início imediato no Codex | alta |
| D-V2-003 | 2026-07-14 | Monólito modular no Control Plane | velocidade sem perder fronteiras | média |
| D-V2-004 | 2026-07-14 | Realtime worker separado por runtime | isolamento de latência e falhas | média |
| D-V2-005 | 2026-07-14 | Provider bake-off antes de escolher avatar definitivo | evitar lock-in e escolha por marketing | alta |
| D-V2-006 | 2026-07-14 | Pipeline modular como baseline e S2S como implementação paralela | controle, comparação e fallback | média |
| D-V2-007 | 2026-07-14 | PerceptionSignal e DerivedHypothesis com TTL | evitar leitura da mente e estado eterno | baixa |
| D-V2-008 | 2026-07-14 | InteractionQualityState multidimensional | explicabilidade superior a trust score único | média |
| D-V2-009 | 2026-07-14 | Eventos separados de workflows duráveis | retry, timers e compensação não pertencem ao event bus | média |
| D-V2-010 | 2026-07-14 | UUIDv7 gerado pela aplicação | ordenação temporal sem depender da versão do Postgres | média |
| D-V2-011 | 2026-07-14 | Vector sem dimensão no walking skeleton, sem ANN | manter provider bake-off e evitar índice incorreto precoce | média |
| D-V2-012 | 2026-07-14 | M0-M2 podem usar fakes sem credenciais | permitir desenvolvimento imediato e determinístico | alta |
| D-V2-013 | 2026-07-14 | Compilador TypeScript e tipos de Node são dependências somente de desenvolvimento, fixadas no lockfile | manter lint, typecheck e build reproduzíveis sem introduzir SDK de provider ou dependência de produção | alta |
| D-V2-014 | 2026-07-14 | Validadores Python e pytest ficam no grupo de desenvolvimento fixado por uv.lock | a CI e a execução local passam a usar os mesmos validadores e fixtures negativos sem dependências de produção | alta |
| D-V2-015 | 2026-07-14 | Workspace Python requer 3.10 ou superior | jsonschema 4.26.0 é o validador normativo e exige Python 3.10+, enquanto o gate documental mantém fallback para o Python 3.9 local | alta |
| D-V2-016 | 2026-07-14 | Tipos de contrato são gerados por script Python determinístico e carregam origem, versão e hash do schema | impede cópias manuais concorrentes e torna drift bloqueante na CI sem adicionar gerador externo | alta |
| D-V2-017 | 2026-07-14 | IDs e contextos de tenant e trace são value objects estritos e imutáveis no domínio | torna UUIDv7, actor type, correlação e classificação explícitos nas bordas, sem depender de framework, banco ou SDK | alta |
| D-V2-018 | 2026-07-14 | Reducers iniciam consentimento e disclosure em `pending`, exigem eventos dedicados para ativação e revalidam snapshots reidratados | impede que criação ou snapshot forjado burle disclosure, consentimento, tenancy ou One Mouth antes de produzir estado autoritativo | alta |
| D-V2-019 | 2026-07-14 | Configuração de runtime aceita somente modo `fake`, valida antes do startup e propaga `SecretHandle` opaco apenas ao adapter; o fake broker é vinculado ao contexto autorizado e exige escopo por purpose | mantém M0-M1 sem credenciais ou egressos, evita segredo em logs e prepara M0-09 para criar contexto somente a partir de claims verificadas | alta |
| D-V2-020 | 2026-07-14 | Migrations usam runner TypeScript via `psql` local, checksum receipt e cluster efêmero PostgreSQL 17 com pgvector para integração; nenhum ORM ou client foi escolhido | valida o contrato SQL real sem banco remoto ou credencial, preserva SQL normativo e adia `pg` ou Drizzle até existir um repositório que exija client | alta |
| D-V2-021 | 2026-07-14 | Runner local exige URL loopback sem senha com opt-in explícito, inicia filhos com allowlist mínima e GSS desabilitado, serializa apply/read/drift por identidade normalizada e valida mapeamentos RLS, triggers, UUIDv7 e funções de segurança de forma estrutural | evita túnel local acidental, credencial implícita e aprovação de drift que preserve somente contagens | alta |
| D-V2-022 | 2026-07-14 | Relações de presenter, turn e handoff passam a referenciar participante da mesma sessão; exclusão física de sessão com custo ou avaliação passa a ser restrita | reforça One Mouth e tenancy relacional sem reescrever migrations já aplicadas, sem quebrar a imutabilidade de eventos financeiros e sem perder atribuição de tenant | média |
| D-V2-023 | 2026-07-14 | Auth fake usa registry determinística server-side somente em development e test, com allowlist M0 de scopes e finalidades; bearer de service identity e `X-Tenant-Id` resolvem grant explícito antes de contexto transacional | permite M0 sem OIDC ou credenciais reais, bloqueia confused deputy e grants amplos, mantém users sem seleção implícita por header e deixa staging, canary e produção fechados para a implementação fake | alta |
| D-V2-024 | 2026-07-14 | M0 usa núcleo de telemetria compatível com OpenTelemetry, sinks locais injetáveis, carrier W3C estrito somente em fronteiras internas confiáveis e registros de atributos fechados; a API pública sempre inicia trace e correlação novas | permite propagação entre API, worker e provider fake sem SDK, backend, baggage ou dados de tenant no carrier, impede que headers ou payloads públicos controlem a linhagem ou vazem para logs e reduz referências de provider a valores locais criados pelo runtime | alta |
| D-V2-025 | 2026-07-14 | M0 normaliza ports de provider e registry de capacidade por contratos sem SDK, com capability canônica plural, timeout derivado, cancelamento propagado, health e fallback somente explícito | preserva troca de provider, mantém candidatos fake sem escolha de produção, bloqueia capability inativa, circuito não fechado ou health desconhecido, mantém storage em capacidade opaca tenant-scoped e reserva ToolPort para o Action Runtime de M0-14 | alta |
| D-V2-026 | 2026-07-14 | Fakes de provider usam três contratos gerados, cenário estrito e serializável, seed limitada, clock manual pertencente ao pacote e journal sem PII; parciais são somente marcadores até M2. O provider contract deriva orçamento imutável de deadline na fronteira do adapter, e o fake o aplica no scheduler manual sem relógio ambiente. | permite reproduzir latência, falha, cancelamento e timeout sem credencial, rede, SDK, trace público ou estado compartilhado, fecha a lista de operações de journal, evita relógio controlado pelo caller, usa apenas dependência interna de tipos, preserva storage scoped e mantém ToolPort fechado até o Action Runtime | alta |
| D-V2-027 | 2026-07-14 | M0 usa repositório de outbox transacional determinístico e local, contexto autenticado opaco, envelope existente e `event_id` materializado com unicidade por tenant. A migration exige que a identidade de evento e tenant do envelope corresponda à linha. O relay é fake at-least-once com efeito idempotente por tenant e evento. | prova atomicidade, ordenação por aggregate, isolamento do envelope e retry sem credencial, broker ou egress, sem antecipar lease, dead-letter, consumer ou workflow de produção que pertencem a M1-07 | alta |
| D-V2-028 | 2026-07-14 | M0-14 usa um catálogo fake determinístico, privado ao Action Runtime e somente leitura; o ToolPort de provider continua fail-closed. O perfil fechado de approval apenas torna a policy mais restritiva e o catálogo permanece alinhado ao contrato ativo | prova o funil ActionIntent, PolicyDecision, idempotência e receipt sem criar factory de autoridade, impede que texto, decisão, session, actor ou nova chave reexecute efeito desconhecido e preserva a allowlist de tenant, risco e atores | média |
| D-V2-029 | 2026-07-14 | M0-15 usa limites de ingress code-owned, deadline cancelável, rate limiter local bounded por tenant e ator autenticados e egress default deny por capability opaca de adapter; o scan de dependências lê locks e advisory snapshot local | bloqueia abuso de request sem confiar em headers públicos, impede saída ou SSRF por adapter não autorizado, preserva fakes sem rede e mantém a CI reproduzível sem auditoria remota nesta etapa | alta |
| D-V2-030 | 2026-07-14 | M0-16 registra custo como eventos append-only em USD, calcula com inteiros escalados e arredondamento half-up, mantém estimado, medido e provider_reported separados e usa rate card e request ref opacos locais. A request ref é vinculada a rate card, tenant e sessão, é de uso único por fonte e a migration preserva legado com checks forward-only e trigger de reconciliação | preserva atribuição auditável e determinística sem SDK, credencial, invoice ou chamada síncrona ao provider, impede replay de custo e reconciliação sem evidência estimada compatível; uma integração de invoice exigirá contrato e migration próprios | alta |
| D-V2-031 | 2026-07-14 | M0-17 usa um seed SQL fixo, idempotente e local para `tenant-zero-alpha` e `tenant-zero-beta`, com role pack Sales Closer e somente conexões a providers fake. O comando exige URL loopback autorizada, drift verde e ambiente `psql` sanitizado antes de executar o arquivo fixo, e a transação falha se qualquer linha canônica divergir | permite demo e testes repetíveis sem PII, segredo, URL externa ou credencial, mantém a semeadura fora da role runtime, impede estado de demo adulterado e prova isolamento dos tenants com RLS | média |
| D-V2-032 | 2026-07-14 | M1-01 introduz `@axtro/session-application` para comandos lifecycle, lote determinístico de timeline e outbox, disclosure persistido com receipt da fake local, catálogo server-side de presenter e canal, CAS e ledger idempotente tenant-scoped, bounded e sem TTL | mantém HTTP, domínio, persistência e providers separados; impede ativação sem prova de disclosure entregue, consentimento ou participante válido; descarta motivo após hash canônico para idempotência; e bloqueia deadline antes do commit sem escolher servidor, ORM, broker, provider ou banco remoto | alta |
| D-V2-033 | 2026-07-14 | M1-02 usa `@axtro/session-runtime` como actor único, por chave tenant e sessão, que reduz somente envelopes canônicos já comprometidos em mailbox bounded sem ordem global. O ledger hot canônico é bounded; após evicção, uma reentrega consulta o replay canônico bounded e retorna o receipt histórico sem nova redução. Cada leitura da fonte recebe signal e deadline de 1.000 ms por padrão, no máximo 10.000 ms. Cada actor permite um único lookup histórico em voo e coalesce identidade e fingerprint idênticos. O ledger separado de cancelamento só descarta registros settled quando uma nova entrada precisa de capacidade | evita usar o coordenador de transação fake como lock realtime, mantém timeline como verdade, preserva redelivery canônico idempotente sem permitir que cancelamentos concluídos esgotem a lane de segurança, impede flood de replay antes de I/O, descarta I/O tardio e remove actor parcialmente hidratado após timeout ou falha de scheduler, rejeita ausência de evidência com erro de janela sem mutação, valida snapshot e replay, preserva One Mouth pelo reducer e adia lookup persistido, bridge Python e mídia para M1-06 e M2 | alta |
| D-V2-034 | 2026-07-14 | M1-03 introduz um Turn Driver TypeScript como escritor canônico de turnos, com diretório server-side de participante único, schemas explícitos de submissão e commit, transcript `restricted`, Fast Lane fake cancelável por geração e request e resposta Presenter cercada por geração e floor | fecha a race entre output tardio e troca de Presenter no reducer e no outbox, evita confiar no corpo da request, torna o marcador de interrupção possível somente para a geração pendente efetivamente cancelada, mantém o Actor como projeção, limita o texto e a idempotência por tenant, sessão e participante, e preserva M1 sem usuário público, ferramenta, mídia, provider, rede, credencial ou Axtro Agent síncrono | alta |
| D-V2-035 | 2026-07-14 | M1-04 usa Context Composer local e síncrono, sem cache, com snapshot opaco do estado projetado, contexto estruturado, budget UTF-8, provenance, TTL calendariamente estrito, catálogo opaco de conhecimento aprovado e snapshots opacos de hipótese e sugestão versionados pelo estado. Conhecimento, sugestão e hipótese não aceitam `restricted`; knowledge exige receipt em provenance; o Turn Driver revalida estrutura e freshness no seu clock antes da Fast Lane | impede que transcript, resumo, RAG, prioridade, estado bruto ou flag de aprovação não confiáveis virem instrução, fato, policy, tool grant ou autoridade; preserva isolamento tenant, descarte de trabalho tardio e Fast Lane sem Axtro Agent, provider, banco ou rede | alta |
| D-V2-036 | 2026-07-14 | A consulta explícita de catálogo usa comando estruturado e coordenador server-side fora da Fast Lane; tenant, ator, intent, idempotência e receipt são derivados e a fala permanece somente candidata | prova o fluxo M1 por receipt sem abrir ToolPort, chamar provider, aceitar texto de modelo ou inserir ferramenta no caminho crítico realtime; unknown exige reconciliação autenticada e tenant-scoped | alta |
| D-V2-037 | 2026-07-15 | M1-06 separa o repository tenant-scoped de timeline em `@axtro/events` do verificador e Actor read-only em `@axtro/session-runtime`; eventos preservam identidade e fingerprint, snapshot do aggregate completo é gerado apenas por replay canônico, e migration forward-only materializa `event_id` e reconcilia o envelope na `session_timeline` | prova replay zero igual a snapshot mais tail real sem dar write port ao Actor, prepara o consumer idempotente de M1-07, mantém RLS e append-only no banco e evita chamar o fake local de adapter PostgreSQL | média |
| D-V2-038 | 2026-07-15 | M1-07 usa relay `runOnce` bounded, claim com lease exclusivo e token histórico single-use, retries limitados, DLQ sem PII e registry code-owned limitado a `session-timeline`; o consumer reconcilia todo o receipt antes do ACK, `event:relay` e `event:observe` exigem workflow service mais `essential_processing`, e spans preservam trace/correlation sem afetar o estado quando o sink falha | prova crash antes do efeito e depois do efeito sem duplicação, preserva ordem por aggregate e isolamento tenant, evita consumer squatting, retry storm e escritor de sessão controlando o relay, sem selecionar broker, client PostgreSQL, provider ou workflow engine | alta |
| D-V2-039 | 2026-07-15 | M1-08 usa handoff composto e estreito no consumer `session-timeline`, `workflow:dispatch` separado de execução, perfil fechado `post_call_processing@1.0.0`, store local com clock autoritativo, falhas classificadas, worker de um checkpoint por `runOnce` e marker de follow-up `not_sent` sem efeito externo; a migration 0011 fixa vínculos compostos tenant-scoped sem selecionar adapter ou engine | prova resume por replacement worker, idempotência, retry, cancellation, fencing e isolamento sem fanout genérico, provider, rede, credencial, ferramenta ou follow-up real; claim ledger persistente continua obrigatório antes de adapter PostgreSQL, e qualquer segundo perfil, engine durável ou ação externa exige novo contrato e revisão | alta |
| D-V2-040 | 2026-07-15 | M1-09 usa SSR TypeScript framework-neutral em `apps/web` com renderer HTML puro em `@axtro/ui`; a rota sem tenant selector consulta lifecycle antes de timeline M1-06, liga action intent, policy e receipt em projeção read-only bounded, distingue hipótese de evidência confirmada e soma custos por fonte com inteiros escalados | entrega o console mínimo sem selecionar Next.js, React, browser auth, servidor, deploy ou dependência de produção; impede enumeração cross-tenant, execução pela UI, exposição de payload bruto e total financeiro ambíguo, mantendo a futura shell web substituível | alta |
| D-V2-041 | 2026-07-15 | M1-10 dá ao coordenador de catálogo uma capability separada, tenant-scoped, bounded e somente leitura, que projeta metadata allowlisted da cadeia exata já produzida | prova o produtor real até o console sem fabricar receipts, expor argumentos, resultado, erro, provider ou execução, abrir ToolPort, criar contrato wire ou aceitar evidência do caller | alta |
| D-V2-042 | 2026-07-15 | O release gate M1 exige `essential_processing` junto de todo scope de sessão em lifecycle, timeline, Session Actor, outbox e custo; grants com somente `provider_auth` ou `tool_auth` falham no guard do bounded context | fecha purpose escalation entre capacidades válidas do mesmo tenant, preserva defesa em profundidade e least privilege sem alterar contratos, RLS, providers ou a Constituição | alta |
| D-V2-043 | 2026-07-15 | M2 é um spike de evidência (Human Presence Spike): os novos pacotes M2 usam validação de entrada explícita, tipos fechados, imutabilidade e determinismo por seed/clock injetado, mas não replicam toda a allowlist de chaves exatas e branding `WeakMap`/`WeakSet` de M0-M1 nos limites internos entre pacotes M2, reservados a fronteiras que não cruzam wire, banco ou tenant | M0-M1 protegem fronteiras de rede, persistência e multi-tenant; os pacotes M2 (`RoomTransport`, Turn Coordinator, directors, fabric, perception) são compostos in-process atrás dos mesmos `ProviderPort`s já validados, e a Constituição já governa comportamento (One Mouth, disclosure, degradação) independentemente do nível de boilerplate de parsing; qualquer capability M2 promovida além do spike em M3 deve revisar se precisa do padrão de validação completo de M0-M1 | média |
| D-V2-044 | 2026-07-15 | M2-01 implementa `RoomTransport` em `@axtro/meeting-gateway` como camada de participante e track sobre exatamente uma conexão `ChannelPort` fake; `apps/meeting-room` é o único ponto de composição entre `@axtro/provider-fakes` e `@axtro/meeting-gateway` | prova que sessão e turn-coordinator dependem apenas de `RoomTransport`, nunca de um SDK concreto; ADR-003 permanece "accepted with benchmark gate" e nenhum candidato (LiveKit ou outro) foi escolhido ou importado | alta |
| D-V2-045 | 2026-07-15 | M2-02 implementa `@axtro/turn-coordinator` como reducer puro mais um coordenador com estado que combina sinais em política de endpoint e barge-in por perfil; um `max_utterance_timeout` forçado comita mesmo com fala contínua (não é cancelável por `speech_resumed`), e um único sinal de silêncio que já satisfaz ambos os limiares comita na mesma chamada | fecha duas ambiguidades do diagrama ASCII de `docs/architecture/TURN_COORDINATOR.md` de forma testável e determinística: sem essas duas regras, um utterance longo nunca fecharia turno e um silêncio longo isolado exigiria um sinal artificial extra para comitar | alta |
| D-V2-046 | 2026-07-15 | M2-03/M2-04 usam um `TextGenerationPort` local em `@axtro/model-gateway` para o passo de LLM do caminho modular, com fake determinístico próprio, em vez de adicionar `llm` à lista fechada `PROVIDER_PORT_KINDS` de `@axtro/provider-contracts` | evita alargar o registry fechado de M0-11/M0-12 (que exigiria tocar `FAKE_PROVIDER_OPERATIONS`, `provider-fakes` e o schema de capability) no meio do spike M2; a rota S2S continua usando `RealtimeModelPort` sem alteração, e formalizar "llm" como décimo port kind fica para M2-13/M3 se o caminho modular for promovido | média |
| D-V2-047 | 2026-07-15 | M2-11 usa um vocabulário de span próprio em `@axtro/realtime-telemetry` (`REALTIME_SPAN_KINDS`) em vez de estender `TELEMETRY_SPAN_NAMES` fechado de `@axtro/observability` | mesmo racional do D-V2-046: evita alargar um enum fechado de M0 dentro do spike M2; toda amostra ainda correlaciona por `generationId`, consistente com o fencing de turn-coordinator/avatar/scene; unificar os dois vocabulários de telemetria é candidato a decisão de M2-13/M3 | média |
| D-V2-048 | 2026-07-15 | M2-13 registra `continue` (duas com `tune` menor) para todas as áreas de arquitetura M2-01 a M2-11 com base em `artifacts/m2/evidence.json`, e `blocked` para as 10 linhas de `CURRENT_PROVIDER_MATRIX.md` mais Hedra (excluído), sem nenhum blocker de qualidade ou jurídico além da ausência de bake-off credenciado | a sessão inteira foi fake-first sem credencial real; misturar decisão de arquitetura (comprovável com fake) e decisão de provider (exige credencial e gate humano) no mesmo veredito esconderia que nenhum provider foi de fato testado; M3 pode assumir os contratos M2 como estáveis mas deve orçar o bake-off como item próprio | alta |
| D-V2-049 | 2026-07-15 | M3-01 mantém M3-01 a M3-09 fake-first/dry-run exatamente como o texto de aceite de cada tarefa já exige; o bake-off credenciado de provider (`PROVIDER_BENCHMARK_PROTOCOL.md`) e os 20 chamados internos reais de M3-10 ficam como itens de escopo próprios, explicitamente fora do que este agente executa sozinho | `AGENTS.md` isenta apenas M0-M2 de bloqueio por falta de credencial; `HANDOFF_TO_CODEX.md` classifica M3 como "não declarar pronto para cliente sem... provider bake-off... aprovação de lançamento" e reserva "usar credenciais reais" e aprovações de beta para decisão humana; construir M3-01..M3-09 sem credencial real não viola essa regra porque nenhuma dessas tarefas exige provider real no próprio texto de aceite | alta |
| D-V2-050 | 2026-07-15 | M3-01 adiciona o evento `sales.uninstalled` (payload vazio) ao vocabulário fechado de `@axtro/domain` (`INTERACTION_EVENT_TYPES`), com reducer `uninstallSales` que exige extensão instalada e limpa `extensions` de volta a `{}`; reinstalar após remover é permitido | "pack can be enabled or removed per tenant" (M3-01) não tinha evento de remoção — só `sales.installed`/`sales.updated` existiam; ao contrário de M2 (spike, vocabulário próprio para não tocar enum fechado de M0), M3 é produto real e o Role Pack é exatamente o extension point que `packages/domain` já antecipava (`extensions.sales?`), então estender esse mesmo pacote é a continuação correta, não uma cópia de escopo | alta |
| D-V2-051 | 2026-07-15 | M3-01 usa um `TenantRolePackRegistry` process-local (Map em memória) para habilitar/desabilitar Role Pack por tenant, separado do estado de extensão por sessão (`sales.installed`/`sales.uninstalled`); nenhuma tabela de banco foi criada | não existe tabela de configuração de tenant para Role Packs ainda; "install disabled -> enable in test tenant -> promote" (`ROLE_AND_SKILL_PACKS.md`) descreve um conceito por tenant, não por sessão, e uma sessão já ativa fica pinada ao `role_pack_version` do início (M1) então não deveria perder a extensão retroativamente; mesma limitação aceita de stores process-local documentada em M1 | média |
| D-V2-052 | 2026-07-15 | ADR-031 registra `@axtro/knowledge-engine` como port síncrono sobre store em memória espelhando `database/migrations/0004_knowledge_governance.sql`; nenhum client `pg` nem provider de embedding real foi escolhido; revogação é reavaliada a cada consulta (sem cache a invalidar) | fecha a reserva explícita do ADR-025 ("A durable cache or RAG retrieval requires its own ADR, revocation semantics, and multi-tenant test matrix"); a tabela já existia desde M0 mas nenhum código de aplicação a consultava; manter o port desacoplado do adapter real permite trocar por Postgres+pgvector depois sem mudar nenhum chamador | alta |
| D-V2-053 | 2026-07-15 | M3-06 (`@axtro/handoff`) gerencia o ciclo de vida da proposta de handoff (pending/accepted/declined/timed_out/rolled_back, uma pendente por sessão) fora do aggregate de domínio, e delega a troca real de `active_presenter_id` a um `PresenterFloorChanger` injetável em vez de emitir `presenter.changed` diretamente ou usar o status `handoff_pending` já reservado no enum de `packages/domain` | evita uma segunda mudança nesta sessão no reducer central (já alterado em M3-01 para `sales.uninstalled`); o mecanismo CAS de `presenter.changed` já existe e é testado desde M1-02, então reaproveitá-lo por injeção prova "muda uma vez por transferência aceita" sem duplicar essa garantia; ligar `handoff_pending` ao status da sessão de fato fica para quando `packages/session-application` integrar este coordenador | média |
| D-V2-054 | 2026-07-15 | M3-10 entrega somente a ferramenta (`generatePilotGateReport`) e uma execução de demonstração com 20 registros sintéticos explicitamente rotulados `FAKE_SYNTHETIC_DATA_NOT_A_REAL_INTERNAL_PILOT` em `artifacts/m3/evidence.json`; nenhuma chamada interna real, custo real de provider ou decisão de piloto/beta foi produzida ou fabricada | os critérios de aceite de M3-10 exigem 20 chamadas internas REAIS revisadas, custo REAL medido por provider, e aprovação separada de beta com cliente — nenhum desses três itens pode ser produzido por um agente autônomo sem credencial real e sem gate humano; fabricar números "realistas" para parecer que o piloto aconteceu violaria a integridade da evidência e o próprio propósito do gate | alta |
| D-V2-055 | 2026-07-16 | Projeto Supabase real (`digital-human-os`, org Axtro AI, us-east-1, US$10/mês) criado com aprovação explícita do usuário; as 11 migrations portáveis de `database/migrations/` foram aplicadas nele sem alteração. RLS de leitura pública (`ENABLE`/`FORCE ROW LEVEL SECURITY` + policy `SELECT USING (true)`) foi aplicado diretamente no projeto Supabase, fora do `database/migrations/`, para `schema_registry`, `provider_catalog` e `region_policy_catalog` — as 3 únicas tabelas sem `tenant_id` que o advisor de segurança da Supabase acusou como expostas via PostgREST à role `anon` | essas 3 tabelas nunca tiveram RLS porque `0005_rls_and_immutability.sql` só cobre tabelas com `tenant_id` (`EXPECTED_TENANT_TABLES`); isso nunca importou localmente porque o harness local usa `psql` direto, sem PostgREST. Adicionar essa policy como migration portátil quebraria o fingerprint exato de `CATALOG_ASSERTION_SQL` (`EXPECTED_RLS_TABLES`/`expected_policies` esperam exatamente as tabelas com `tenant_id`, cmd `ALL`); o shape correto (policy `SELECT`-only, sem `tenant_id`) é uma característica do ambiente hospedado (exposição PostgREST), não do contrato de schema portátil — por isso ficou fora de `database/migrations/` e documentada aqui em vez de estender `CATALOG_ASSERTION_SQL` sem poder validar contra um Postgres local real nesta sessão | média |
| D-V2-056 | 2026-07-16 | ADR-032: sessão de usuário humano vira `TenantContext` via claim assinada no JWT (`app_metadata.tenant_id`/`actor_id`/`tenant_role`, injetada por um Custom Access Token Hook lendo `public.user_tenant_memberships`), nunca por header — função nova `resolveAuthorizedUserRequestContext` (async), `resolveAuthorizedRequestContext` (M0-09, sync, service-only) intocada. Cadastro no portal cria o próprio tenant (self-serve, decisão confirmada com o usuário) via RPC `SECURITY DEFINER` `provision_self_serve_tenant`, sem precisar da chave `service_role` | M0-09 já deixava escrito que seleção de tenant por usuário "precisa de um contrato público baseado em claim" e não pode vir de header; a chave `service_role` fica reservada pro fim desta fase (instrução explícita do usuário), então a RPC gera o efeito de provisionamento sem precisar dela. `auth.users.id` é UUIDv4, não passa em `parseActorId` (exige UUIDv7) — por isso existe um `actor_id` UUIDv7 próprio gerado em TypeScript e carregado na claim | alta |
| D-V2-057 | 2026-07-16 | Habilitar o Custom Access Token Hook (`Authentication > Hooks` no dashboard Supabase, ou o endpoint de config de auth da Management API) ficou fora desta sessão — nenhuma ferramenta MCP disponível expõe essa configuração; a função já está publicada no projeto (`public.custom_access_token_hook`), só falta ativá-la | escopo de ferramentas: `apply_migration`/`execute_sql` publicam a função no banco, mas o registro do hook em si é configuração da camada Auth do Supabase (GoTrue), não uma tabela consultável por SQL nem uma operação coberta pelas ferramentas MCP conectadas nesta sessão | baixa |
| D-V2-058 | 2026-07-16 | As telas operacionais do portal (`/dashboard`, `/agentes`, `/conhecimento`, `/configuracoes`) leem e escrevem dados de tenant via RPCs `SECURITY DEFINER` (`portal_tenant_overview`, `portal_list_agents`, `portal_list_knowledge_sources`, `portal_update_tenant_profile`) que resolvem o tenant do chamador por `auth.uid()` → `user_tenant_memberships`, em vez de acesso direto às tabelas via PostgREST com RLS por claim de JWT | as policies RLS existentes (`tenant_isolation`) dependem de `app.current_tenant_id()` (setting transacional), que nunca está presente numa requisição PostgREST — acesso direto retornaria zero linhas; RLS por claim exigiria o Custom Access Token Hook habilitado, que segue bloqueado por gate manual (D-V2-057). As RPCs funcionam hoje, não precisam de `service_role`, validam papel (`tenant_admin` para escrita) e mantêm um único ponto auditável por operação; migrar para RLS-por-claim continua possível depois sem tocar as telas | média |
| D-V2-059 | 2026-07-16 | O provisionamento self-serve foi movido do layout do grupo `(app)` para dentro de `fetchTenantOverview` (deduplicado por request com React `cache()`): o primeiro chamador provisiona, os demais aguardam a mesma promise | bug real encontrado testando no navegador: o App Router renderiza layout e página em paralelo, então provisionar no layout não garante ordem — o primeiro dashboard após signup mostrava "Conta ainda não provisionada" (corrida confirmada ao vivo). Com o provisionamento dentro do fetch cacheado, qualquer ordem de execução converge para o mesmo resultado provisionado | alta |
| D-V2-060 | 2026-07-16 | Convites de equipe funcionam por e-mail pré-aprovado, sem envio de e-mail e sem token: o admin registra o convite (`tenant_invites`, RPC `portal_invite_member`), e quando alguém cria conta com aquele e-mail o `provision_self_serve_tenant` o coloca no tenant do convite com o papel definido (aceito atomicamente com `FOR UPDATE`, convite mais antigo vence), em vez de criar tenant próprio. Mantida a restrição de um tenant por usuário (PK de `user_tenant_memberships` em `user_id`) | enviar e-mail de convite exigiria SMTP próprio (o builtin do Supabase estourou rate limit durante o teste desta própria sessão — risco #4 já documentado) e um fluxo de token com expiração; o modelo por e-mail pré-aprovado entrega multiusuário por tenant agora, com o mesmo signup existente, sem canal novo de ataque (o convite não dá acesso a nada até a pessoa provar posse do e-mail via confirmação normal do Supabase Auth). Convite para e-mail que já tem conta/workspace é rejeitado explicitamente — mover usuário existente entre tenants fica para a modelagem multi-tenant futura (revisit do ADR-032) | média |
| D-V2-061 | 2026-07-16 | Recuperação de senha usa o fluxo nativo do Supabase (`resetPasswordForEmail` → link → `exchangeCodeForSession` no callback já existente → `/nova-senha` com `updateUser`); a página de solicitação responde de forma idêntica para e-mail existente ou não, e o erro 429 (rate limit do SMTP builtin) também é respondido como "enviado" | não vazar existência de conta é requisito básico de enumeração; tratar 429 como sucesso silencioso evita que o rate limit do SMTP builtin (baixo, compartilhado) sirva de oráculo. O trecho e-mail→link não é testável nesta sessão (SMTP builtin rate-limited); o trecho sessão→troca de senha foi testado e2e no navegador com re-login usando a senha nova | alta |
| D-V2-062 | 2026-07-16 | O portal cria agentes em `status='draft'` (RPC `portal_create_agent`, role_type restrito a `'sales'`, `disclosure_profile_id='disclosure-standard-v1'`, limite de 20 por tenant) e fontes de conhecimento em `status='pending'` (RPC `portal_create_knowledge_source`, tipos `document/faq/url`, classificação `internal/confidential/restricted`, limite de 50) — ambos admin-only; ativação/ingestão continuam fora do portal | o risco aceito #5 ("telas read-only porque criação exige provedores") era verdade só para ATIVAÇÃO: o schema de M0 sempre teve `draft`/`pending` exatamente para preparar a conta antes de provedores conectados. Criar rascunho agora deixa o operador adiantar trabalho real sem fingir capacidade que não existe — a UI diz explicitamente que ativação/ingestão dependem de provedores. `role_type='sales'` casa com o manifest do Role Pack sales-closer (M3-01); o perfil de disclosure é um identificador de perfil padrão, a ser validado no fluxo de ativação | média |
| D-V2-063 | 2026-07-16 | Os três gates de auth foram aplicados via Supabase Management API (token do CLI local + chave do Resend lida do Doppler `axtro-human-digital-os`, nunca impressa): SMTP custom (smtp.resend.com:465, user `resend`, remetente no-reply@axtroai.com, domínio axtroai.com já verificado no Resend, rate limit 30/h) e Custom Access Token Hook habilitado (`pg-functions://postgres/public/custom_access_token_hook`); Site URL/redirect já tinham sido configurados pelo Fernando em outra sessão. Tudo testado ao vivo: login sem membership continua funcionando (hook tolera), login com membership carrega `app_metadata.tenant_id/actor_id/tenant_role` no JWT, e um signup real enviou o e-mail de confirmação pelo Resend (bounce esperado — caixa de teste inexistente); dados de teste removidos | D-V2-057 tinha registrado o hook como bloqueado por falta de ferramenta — estava errado no meio: as ferramentas MCP não expõem a config de auth, mas o token do Supabase CLI (keychain local) autoriza a Management API `PATCH /v1/projects/{ref}/config/auth`. Armadilhas reais encontradas: a API exige o bloco SMTP básico ANTES de sender_name/rate_limit (403 se juntos na primeira chamada), o user-agent do urllib do Python é bloqueado pelo Cloudflare (usar curl), e numa das gravações o host persistiu truncado (`smtp.resend.co`) — sempre reler a config depois de gravar | alta |
| D-V2-064 | 2026-07-16 | Primeira integração de provider real (autorizada pelo Fernando): `@axtro/provider-openrouter` implementa um `TextGenerationPort` próprio do control-plane para o chat de teste de agente do portal (`/agentes/[id]/testar`), com disclosure de IA no system prompt, proibição de citar preços sem fontes conectadas, teto diário de 500k tokens/tenant e uso registrado em `cost_events` (primeiro uso real do ledger de M0; `unit_cost=0, source='estimated'` até existir rate card). Os contratos fake de M0-M2 (`providerMode: "fake"`) não foram tocados; o bake-off (D-V2-048) e voz/avatar reais seguem pendentes | ver ADR-033; wiring o OpenRouter nos ports realtime exigiria abrir o tipo fechado de D-V2-019 com blast radius em todo M2 — o chat de texto é uma superfície nova e não substitui o bake-off. Armadilha real: `search_path=''` em RPC SECURITY DEFINER quebra o trigger de reconciliação de cost_events (0009 não qualifica a tabela) — corrigido com `search_path='public'` | média |
| D-V2-065 | 2026-07-16 | Agente institucional "Aurora — Axtro AI" criado como persona Tavus própria (`pdd6c8593976`): percepção `raven-1`, STT com interrupção alta + smart turn, TTS Cartesia em português; adapter estendido com modo persona; tabela `agent_video_config` mapeia agente→persona por tenant (supabase-only 0009). Aurora semeada no tenant demo | ver ADR-034. Voz brasileira garantida (Azure pt-BR) dá 500 nesta conta Tavus — só Cartesia funciona; não dá pra julgar áudio/lip sync deste ambiente (validação humana do Fernando), e o upgrade garantido de sotaque seria ElevenLabs BR (precisa de chave). Persona carrega prompt/voz/percepção fora do nosso código, então trocar voz/rosto é update de persona, sem deploy | média |
| D-V2-066 | 2026-07-16 | Voz da Aurora trocada para ElevenLabs BR (voz "Marilita — Natural & Conversacional", `k5aKjuBz9NhUMZc7SDpl`, verificada pt:brazilian) — nova persona Tavus `p059e7b04961` com TTS `elevenlabs` + `external_voice_id` + `api_key` (chave própria do ElevenLabs no Doppler), mantendo raven-1 e interrupção alta; `agent_video_config` da Aurora repontada, persona Cartesia antiga removida | o Cartesia falando português ainda soava com sotaque não-brasileiro (queixa do usuário); ElevenLabs tem vozes nativas pt-BR verificadas e pluga direto no TTS do Tavus (Azure dá 500 nesta conta). Armadilha real: o campo é `external_voice_id`, não `voice_id` (este dá 500), e `model` não é campo válido no layer TTS. Troca de voz é update de persona, sem deploy — quando houver áudio da Raissa, clono a voz dela (plano Creator permite) e troco o external_voice_id | média |
| D-V2-067 | 2026-07-16 | Aurora v3 (persona `pa40663b00df`): rosto trocado de Gloria para Anna (`r6ae5b6efc9d`, mesma da Rafaela, feedback estético do usuário), voz Raissa SDR com settings de expressividade no ElevenLabs (stability 0.32, style 0.45 — corrige perguntas soando como afirmações), TTS com `tts_model_name: eleven_multilingual_v2` (prosódia pt melhor; campo aceito pelo Tavus, provado por probe), e system prompt com regra explícita de pergunta curta/isolada no fim do turno. Persona v2 removida | feedback do usuário após ouvir: voz boa mas entonação interrogativa plana e rosto não agradou; entonação vive no TTS (settings/modelo), não no prompt sozinho. LIMITE ATINGIDO: créditos conversacionais free do Tavus esgotados durante os testes — novas conversas de vídeo falham até upgrade do plano Tavus (gate humano de custo) | média |
| D-V2-068 | 2026-07-16 | Novo agente "Amanda — Ecoloop Solar (EN)" no tenant demo: persona Tavus `p098a75bfbeb` construída a partir do documento de contexto da Ecoloop fornecido pelo usuário (credibilidade BBB A+/6 anos/in-house crews, virada do crédito fiscal 25D→48E via leasing, qualificação, playbook de objeções, fechamento com data, disclosure de IA obrigatória na abertura conforme o próprio doc, campos 【preencher】 nunca inventados). Voz ElevenLabs "Sarah" premade + eleven_multilingual_v2 (inglês padrão, troca para pt-BR quando o cliente falar), rosto Anna, raven-1 + interrupção alta; `agent_video_config.language='english'` | o doc da Ecoloop é o primeiro caso real de "cliente com contexto próprio" — a persona carrega o playbook; conhecimento operacional exato (cobertura FL, licenças, financiadoras, garantias) fica explicitamente como "confirmo com o time" até existirem fontes conectadas. Vídeo não testado ao vivo: créditos conversacionais do Tavus esgotados (D-V2-067), upgrade de plano é gate humano de pagamento — cartão NUNCA é manuseado pelo agente, mesmo com autorização | média |
| D-V2-069 | 2026-07-17 | VISUAL-01 adota uma linguagem visual única para landing e workspace: obsidiana com indigo/violeta e sinal coral, tipografia editorial, motion progressivo com respeito a `prefers-reduced-motion`, e hero com asset autoral de digital human claramente sintético | o portal atual tinha uma landing estática e um workspace funcional, mas sem hierarquia de conversão nem continuidade visual entre aquisição e operação; a mudança é reversível e não altera contratos, auth, tenant isolation ou providers | média |
| D-V2-070 | 2026-07-18 | Conhecimento REAL com a chave OpenRouter existente: embeddings via `POST /api/v1/embeddings` (OpenAI-compat, `openai/text-embedding-3-small`, 1536 dims) num novo `createOpenRouterEmbeddingPort` no mesmo pacote/egress; supabase-only 0010 com `portal_ingest_knowledge` (admin, ids UUIDv7 do chamador, re-ingestão substitui a versão anterior por delete cascade), `portal_search_knowledge` (cosine `<=>`, top-N sobre fontes ativas) e `portal_knowledge_digest` (concat ordenado p/ contexto de vídeo). Portal: textarea de conteúdo na criação de fonte → chunking ~1200 chars → embeddings em lotes de 32 → RPC → fonte ativa; RAG no chat de teste (fontes numa 2ª mensagem system) e digest injetado como `conversational_context` nas chamadas Tavus (persona e réplica). Custos no ledger: `portal.knowledge_embedding` e `portal.knowledge_retrieval` | teste ao vivo provou que a chave OpenRouter serve para embeddings — zero credencial nova. Armadilhas reais encontradas no e2e: (a) variável plpgsql `c` colide com alias `c` de `jsonb_array_elements` ("column reference is ambiguous"); (b) o cap de 4000 chars/mensagem do adapter derrubava o system prompt com chunks — e um cap agressivo no bloco cortou exatamente o chunk do crédito fiscal, fazendo o modelo alucinar "ITC até 2032" de memória; a correção foi mover as fontes para mensagem system própria (até ~3800 chars) + regra explícita de nunca responder imposto/crédito de memória. E2e verde: doc Ecoloop ingerido pela UI (17 chunks/17 embeddings), Amanda respondeu 25D encerrado + pivot leasing/48E até 2027 fiel à fonte | alta |
| D-V2-071 | 2026-07-18 | Governança de conhecimento fecha a promessa da UI ("revogação imediata"): supabase-only 0011 com `portal_set_knowledge_source_status` (admin; `disabled` revoga na hora porque busca e digest só leem `active`; `active` exige conteúdo ingerido) e ações por fonte na tela de Conhecimento (Revogar/Reativar/Atualizar conteúdo — re-ingestão via `portal_ingest_knowledge`, versão `v<epoch>`, substitui a anterior). Vídeo entra no ledger: `portal_log_video_usage` insere um cost_event por conversa Tavus criada (provider `tavus`, unit `conversation`, unit_cost 0/source measured até rate card, mesma política do 0008) | e2e no navegador: revogar a fonte Ecoloop fez a Amanda parar de citar 48E/2027 na mensagem seguinte (degradou para "confirmo na consulta" + avanço) e reativar restaurou; "Atualizar conteúdo" ingeriu a Tabela de preços (pendente → ativa, 1 chunk) e a Rafaela passou a citar R$ 18.900/10%/60x com disclaimer de estimativa. Limite honesto: o log de custo de vídeo foi validado por typecheck/revisão — a primeira linha real no ledger aparece na próxima conversa Tavus iniciada (não iniciei chamada para não gastar créditos) | média |
| D-V2-072 | 2026-07-18 | Painel "Uso de IA" no dashboard: supabase-only 0012 com `portal_usage_summary` (tokens de hoje, conversas de vídeo de hoje e agregado de 7 dias por serviço, direto de cost_events; leitura para qualquer membro) + seção no dashboard com % do teto diário de 500k e breakdown por serviço com rótulos amigáveis. Sem valores monetários: unit_cost ainda é 0 até rate card, e mostrar "R$ 0" seria mentira de precisão. Classe `.grid-3` adicionada ao globals.css (só existiam grid-2/grid-4) com os mesmos breakpoints | o ledger já registrava tudo mas nada era visível ao operador — governança sem visibilidade é meia promessa. Falha do resumo não derruba o dashboard (painel é opcional). Verificado no navegador desktop e mobile com dados reais do tenant demo (9.904 tokens, 3 serviços) | baixa |
| D-V2-073 | 2026-07-19 | Cérebro Método Silva: os 38 manuais da Coleção Método Silva v3.0 foram baixados do Drive do Fernando para `knowledge-vault/metodo-silva/` (gitignored — repo público, IP proprietária; manifesto SHA256SUMS cumpre a exigência de PENDENCIAS_EXTERNAS), destilados por 3 agentes em extratos estruturados (`knowledge-vault/brain/extrato-0{1,2,3}*.md`) e transformados em módulo `apps/portal/src/lib/brain/metodo-silva.ts` seguindo os 9 blocos do "System Prompt Silva" que o próprio Manual de Agentes Autônomos prescreve. Chat de teste usa 2 mensagens system (identidade+regras ≤4000, método ≤4000 — cap do adapter); personas de vídeo usam prompt de ~7-8k chars (teto de conforto de latência do provider ≈20k). 10 manuais de venda ingeridos como fontes RAG do tenant demo (438 chunks, 115k tokens de embedding no ledger); Caso Modelo ContaLeve deliberadamente EXCLUÍDO do RAG (preços fictícios da empresa-exemplo poderiam ser citados como fatos da conta) | o método define venda (6 fases, SILVA, EARC) E define como construir o Closer IA — o cérebro segue a receita do próprio método ("o prompt herda o manual"). E2e API: objeção "tá caro" → RAG retornou Kit 05 + tabela de preços da conta e a resposta foi EARC literal ("Entendo — investimento relevante mesmo. Caro comparado a quê...") | média |
| D-V2-074 | 2026-07-19 | Personas Tavus renovadas com o cérebro + percepção comportamental + modo apresentação: Aurora (`pa2dcc2d9c3e`) e Amanda (`pe468ba01ef5`) PATCHadas preservando voz/STT (playbook Ecoloop da Amanda preservado como apêndice; backups em `knowledge-vault/brain/persona-backup-*.json`); Rafaela ganhou persona própria `p8966676f4d2` (rosto Anna, voz ElevenLabs da Aurora via chave no Doppler, raven-1, interrupção alta, `tavus-glm-4.7`) registrada em supabase-only 0013. Percepção: 5 `ambient_awareness_queries` comportamentais por idioma (engajamento/confusão/ceticismo/vontade de falar/2ª pessoa no quadro) com instrução explícita no prompt de tratar sinal como HIPÓTESE e nunca afirmar leitura de emoção (Constituição). Apresentação: tools globais `next_slide`/`previous_slide`/`go_to_slide` (`/v2/tools`, on_call silent, on_resolve add_to_context, delivery app_message) anexadas às 3 personas via `/v2/pals/{id}/tools`; portal ganhou `startPresentationConversation` (deck estrutural Método Silva SEM números nos slides — fatos só da boca da agente via digest) + sala custom `presentation-room.tsx` com `@daily-co/daily-js@0.91.0` (pinada; necessária porque o iframe não dá acesso ao data channel dos tool calls) que escuta `conversation.tool_call` e devolve `conversation.tool_result`. E2e real: conversa `c1187a82019d14ac` criada e Rafaela vista ao vivo na sala Daily (créditos Tavus voltaram a funcionar — limite de D-V2-067 não se reproduziu); sala encerrada via API para preservar créditos. Armadilha real: GET de persona MASCARA `layers.tts.api_key` — reusar o payload do GET num POST dá 400 "Voice not accessible"; a chave real precisa vir do Doppler | média |
| D-V2-075 | 2026-07-19 | Percepção emocional profunda vira capacidade CENTRAL por decisão explícita do Fernando (dono do produto): Art. 4 da Constituição reescrito (emenda via ADR-035), README princípio 5 e proibição do AGENTS.md atualizados. As agentes agora leem emoção expressa, micro-expressões, linguagem corporal, atenção e sinais de compra (8 ambient_awareness_queries por idioma no raven-1) e o cérebro instrui MAESTRIA: a leitura decide o que perguntar, o que responder, quando aprofundar e quando fechar, podendo ser nomeada com tato ("sinto que esse ponto te preocupou"). Aplicado ao vivo nas 3 personas (Aurora, Amanda, Rafaela). Permanecem proibidos (protegem a operação sem conflitar com o desejo do produto): identificação biométrica oculta, inferência de atributo protegido, alegação de detecção de mentira e diagnóstico médico/psicológico; a leitura é declarada via disclosure (Art. 6) e governada pelas finalidades do Art. 5 | o Método Silva sempre mandou ler o cliente ("lead nervoso → baixa o ritmo; defensivo → dá espaço") — a versão anterior do Art. 4 travava a plataforma abaixo do que um closer humano de elite faz. Mudança constitucional feita como o repo exige: ADR aceito + testes no mesmo PR (testes de "nunca ler emoção" substituídos por testes de presença da maestria + permanência das 4 linhas vermelhas). DPIA/validação por jurisdição em PENDENCIAS_EXTERNAS ganha relevância antes de mercados regulados | média — reversível re-emendando o Art. 4 e re-patchando personas |
| D-V2-076 | 2026-07-22 | Modelo LLM das 3 personas Tavus (Aurora, Amanda, Rafaela) migrado de `tavus-glm-4.7` para `tavus-gemma-4` — o novo default recomendado (baixa latência + bom uso de tools, o que serve tanto ao ritmo de conversa quanto às tools de slides do modo apresentação) | e-mail do time de suporte Tavus (Andy) avisou que `tavus-glm-4.7` entrou na lista de modelos depreciados, com deadline 2026-08-14; confirmado direto na doc oficial (`/sections/conversational-video-interface/pal/llm`) antes de agir — o e-mail só listava Amanda e Rafaela, mas a Aurora também estava no modelo depreciado (checado nas 3, corrigidas as 3). PATCH via `layers/llm/model`, verificado por GET nas 3 personas depois | alta — troca de modelo é reversível sem deploy, mesma mecânica de update de persona já usada nesta sessão |
| D-V2-077 | 2026-07-24 | A superfície pública do Axtro fica concentrada na landing `/`: apenas ela é indexável e recebe sitemap, enquanto autenticação, workspace privado e APIs recebem noindex ou disallow. SEO, AEO, Open Graph, PWA e `llms.txt` usam somente identidade e capacidades confirmadas do Digital Human OS; nenhum texto, marca ou claim do brief Raízes Finance foi transplantado | reduz risco de indexar dados privados ou prometer capacidades não comprovadas, mantém FAQ visível coerente com `FAQPage`, e deixa a publicação futura configurável por `NEXT_PUBLIC_SITE_URL` sem alterar a arquitetura do portal | média, reversível por rota e metadata |
| D-V2-078 | 2026-07-24 | T8 — rate card de custos desbloqueado com preços PÚBLICOS de tabela (não taxa negociada): OpenRouter Claude Haiku 4.5 (US$1/US$5 por 1M tokens entrada/saída), OpenRouter text-embedding-3-small (US$0,02/1M entrada), Tavus (piso de US$0,175/conversa = mínimo de cobrança de 30s a US$0,35/min, ponto médio do overage Starter/Growth). Supabase-only 0017: `portal_log_ai_usage` agora grava `amount_usd`/`unit_cost_usd` REAIS calculados do input/output exato de cada chamada (nunca 0); `portal_log_video_usage` mantém quantity=1/custo=0 no evento (duração real não é capturada) mas corrige `source` de `'measured'` (afirmava um custo que não media) para `'estimated'`; `portal_usage_summary` soma custo de IA exato + piso de vídeo, SEPARADOS, para a UI nunca somar um número exato com um piso como se fosse tudo preciso. Painel "Uso de IA" ganha tile de custo estimado hoje/7d e valor por linha de serviço | testado ao vivo: 1000in/500out logados → US$0,0035 exato (bate a conta manual); fontes antigas mantêm amount_usd=0 (histórico nunca é reescrito). `source` continua em `'estimated'` (nunca `'measured'`/`'provider_reported'`) — honestidade estrutural, Art. 16: preço de tabela datado e citável, não a fatura real. Revisar quando o Fernando tiver a taxa negociada | média — reversível voltando unit_cost a 0 ou trocando as constantes de preço |
| D-V2-079 | 2026-07-24 | Bug real encontrado no smoke test pós-deploy do SEO-AEO-01: `/llms.txt` e `/llms-full.txt` respondiam HTTP 307 para `/login` em vez de 200 — o middleware de auth (`proxy.ts`) não os excluía do matcher, junto com `robots.txt`/`sitemap.xml`/`manifest.json`/`opengraph-image`. Corrigido no mesmo padrão dos demais; deploy re-executado e confirmado (200 nos dois). O teste unitário existente (`portal-seo-surface.test.mjs`) só checava a presença da string no source do regex — nunca teria pego um 307 real; teste HTTP novo em `apps/portal/e2e/portal.spec.ts` cobre as 6 rotas públicas do middleware contra o servidor de verdade | um crawler de IA lendo `llms.txt` receberia uma página de login em vez do conteúdo — anula o propósito de AEO que o SEO-AEO-01 tinha acabado de implementar. Achado por smoke test manual (`curl`) contra produção logo após o deploy, não por CI — reforça por que o teste HTTP e2e era a lacuna real | baixa — fix de 1 linha, sem mudança de contrato |
| D-V2-080 | 2026-07-27 | M4-01: início da construção do cérebro customizado (M4, autorizado pelo Fernando após o spike D-V2-076 confirmar viabilidade). `runBrainChatCompletion` extraído de `agent-preview.ts` para `apps/portal/src/lib/brain/chat-completion-core.ts`, com geração e log de uso injetados por porta (sem Supabase/HTTP/provider diretos) — reutilizável pelo sandbox de chat e, a partir de M4-04, pelo endpoint que o Tavus vai chamar como LLM da persona. Nova capacidade: bloco de PERCEPÇÃO como mensagem system separada, rotulada e limitada, explicitamente marcada como evidência de terceiro que nunca decide preço/política (Art. 15) — pronta para as tags do raven-1 assim que M4-02 as extrair da requisição do Tavus. M4 registrado em `backlog/MVP_TASK_GRAPH.yaml` (M4-01 a M4-04); M4-03/M4-04 tocam produção real (persona ao vivo, segredo por agente) e ficam para gate humano antes de qualquer rewiring da persona em produção | teste do orçamento de mensagens sob histórico longo encontrou um bug real: o guard de tamanho herdado do sandbox (`MAX_HISTORY_TURNS*2`) rejeitava com erro qualquer conversa longa — correto para o sandbox (nós controlamos o histórico), errado para o caminho Tavus (histórico chega pronto, fora do nosso controle). Trocado por teto de sanidade generoso (500 entradas) com corte por orçamento dinâmico (nunca > 24 mensagens, teto do adapter OpenRouter) em vez de rejeição | alta — extração pura, `agent-preview.ts` sem mudança de comportamento, 437 testes Node + 26 Python, typecheck, lint e 9 validadores verdes |
| D-V2-090 | 2026-07-29 | Capacidade de reunião externa completa: `apps/portal/src/lib/time/florida.ts` converte horário local da Flórida pro UTC exigido pelo Recall.ai (sem lib nova — só `Intl.DateTimeFormat`, testado inclusive na transição de horário de verão de 2026); `packages/provider-recall` ganhou `outputMediaWebpageUrl` inline em `createBot` (confirmado na doc oficial: entrada imediata já liga a câmera na mesma chamada, sem passo extra); `apps/portal/src/lib/meetings/join-meeting.ts` (núcleo puro que orquestra Tavus + Recall.ai: cria a sala do agente, cria o bot, grava o recibo — falha ao gravar nunca desfaz o bot que já entrou de verdade); migration `0021_meeting_bot_sessions.sql` (tabela tenant-scoped + 3 RPCs: registrar via usuário autenticado, atualizar status via `service_role` a partir do webhook, listar); `apps/portal/src/app/api/recall/webhook/route.ts` (autenticado por token na própria URL — mecanismo oficial do Recall.ai, mais confiável que adivinhar formato de HMAC não documentado; mapeia 8 eventos de status de bot pro enum fechado da sessão; eventos fora do mapa — participant_events, breakout room — são escopo declarado, não erro); região da conta do Fernando confirmada como `us-west-2` | pedido explícito do Fernando pra converter automaticamente o horário que ele digita (Flórida) em vez de exigir UTC dele; webhook por token (não HMAC) porque a doc não detalhava o formato exato da assinatura — melhor usar o mecanismo simples e 100% confirmado do que arriscar implementar verificação criptográfica errada | alta — tudo aditivo, testado com fakes; nenhuma chamada real ao Recall.ai/Tavus feita a partir deste fluxo ainda, migration pendente de aplicação |
| D-V2-089 | 2026-07-29 | Correção de escopo do Fernando: Recall.ai não é uma peça interna do fallback do control-tower — é capacidade CENTRAL do produto (qualquer cliente do Human Digital precisa poder colocar o agente numa reunião externa de Zoom/Meet/Teams). `packages/provider-recall` criado aqui (terceiro adapter de provider real do projeto, junto de OpenRouter e Tavus): `createBot`, `startCameraWebpage`/`stopCameraWebpage` (Output Media — a URL da sala de vídeo do agente vira a "câmera" do bot dentro da reunião alheia), `leaveCall`. Payload e endpoints confirmados na doc oficial via WebFetch (`docs.recall.ai/docs/stream-media`, `/reference/bot_leave_call_create`) — não inventado nem copiado sem checar da sessão paralela do control-tower, que tinha adivinhado o formato certo do Output Media mas não tinha os outros 3 endpoints confirmados. Bug real pego pelo próprio teste: usei `bot_abc123` (com underscore) como id de teste, mas Recall.ai usa UUID — corrigido o regex de validação E os fixtures de teste, os dois estavam errados | a doc confirma que Recall.ai é sharded por região (us-east-1, us-west-2, eu-central-1, ap-northeast-1, escolhida na criação da conta) — não adivinhei, região é parâmetro obrigatório explícito | alta — pacote novo, isolado, sem chamada real feita ainda (só testado com fakes) |
| D-V2-088 | 2026-07-29 | Bug real achado no smoke test pós-deploy (mesma classe do D-V2-079, llms.txt): o matcher do middleware de auth (`proxy.ts`) só excluía `api/health` — `/api/brain/[agentId]/chat/completions` e `/api/leads/video-session`, as duas rotas servidor-a-servidor com autenticação própria por bearer/segredo, caíam no 307 pro `/login` antes de tocar a lógica da rota. Corrigido excluindo o prefixo `api/` inteiro do middleware (não mais rota por rota) — toda `/api/*` deste projeto já implementa sua própria autenticação, nenhuma depende de sessão de cookie. Teste e2e novo (`portal.spec.ts`) prova com HTTP real que nenhuma das três rotas redireciona | achado ao testar de ponta a ponta depois de configurar `SUPABASE_SERVICE_ROLE_KEY`/`RAISSA_TOOLS_SECRET` no Railway — sem o teste manual, essas duas rotas ficariam inacessíveis em produção indefinidamente (os testes locais nunca passam pelo middleware) | alta — mudança de 1 linha no matcher, reversível, testada com HTTP real |
| D-V2-087 | 2026-07-29 | Nova rota `POST /api/leads/video-session`: contrato do lado Digital Human OS do funil de vídeo sob demanda (desenhado em paralelo no control-tower — régua de qualificação, ligação de voz, PR #118/#19). Servidor-a-servidor, autenticada por `RAISSA_TOOLS_SECRET` (segredo estático único, diferente do segredo por agente do M4 — aqui só existe um chamador conhecido e uma agente institucional). Resolve a persona Tavus da Raissa via `agent_video_config.presentation_kind = 'platform'` (o mesmo campo do D-V2-086), cria a conversa e devolve a URL. `handle-video-session` puro e testado (10 casos): autenticação com comparação em tempo constante, degrada pra erro tipado (503/502) em vez de 500 cru quando a persona ou o provider falham. Gap declarado: ainda não grava custo em `cost_events` — precisa de uma RPC `_service` de log de vídeo (mesmo padrão de `portal_log_ai_usage_service`, 0019), candidata a sessão futura | pedido do Fernando após confirmar o desenho do funil (voz → "vídeo agora ou agendar?" → esta rota no branch AGORA); o lado do control-tower foi construído por outra sessão em paralelo, este PR só cobre o que é deste repositório | alta — rota nova, aditiva, sem persona real apontando pra cá ainda; RAISSA_TOOLS_SECRET nunca configurada em produção |
| D-V2-086 | 2026-07-29 | Aurora renomeada para Raissa (mesma identidade da SDR de telefone que liga para leads do formulário do site): `agents.name` atualizado no banco, persona Tavus `pa2dcc2d9c3e` com `persona_name` limpo ("Raissa — Axtro AI", antes "Aurora — Axtro AI Anna + voz Raissa v4 turbo " — um rótulo interno de engenharia vazando pra tela) e `system_prompt` com a auto-identificação corrigida ("Você é Raissa"). Voz já era a da Raissa (ElevenLabs `jXrDvfgyizjxORhXsh4y`, `eleven_turbo_v2_5`) — o nome antigo da persona já documentava isso, nenhuma troca de voz foi necessária. Migration 0020 aplicada ANTES do rename: adiciona `presentation_kind` explícito a `agent_video_config` e corrige `video-conversation.ts`, que decidia entre deck institucional/vendas checando `agent.name.startsWith("Aurora")` — quebraria silenciosamente com o rename se não corrigido primeiro | pedido do Fernando; a checagem por nome era uma bomba-relógio que o próprio rename ia detonar — corrigida antes, não depois | média — persona e agente reversíveis (outro PATCH/UPDATE); a correção do `presentation_kind` é estritamente aditiva |
| D-V2-085 | 2026-07-27 | Arte dos slides da apresentação ao vivo (Aurora e demais closers) redesenhada: ícones SVG inline por tipo de slide (bússola, colunas, escudo, balança — metáforas, nunca gráfico com trend implícito), pilares viram grid de 3 cards numerados em vez de lista de texto, prova vira selos com check verde, fundo ganha glow radial + textura de pontos em vez de gradiente chapado, dots de progresso no rodapé. `deck.ts` (dados dos slides) intocado — mudança é só em `presentation-room.tsx` (renderização) | pedido do Fernando ("melhorar arte"); confirmado visualmente no navegador com o deck institucional da Aurora em modo demonstração antes de fechar — capa, agenda, pilares e prova revisados um a um | alta — só CSS/JSX, nenhuma mudança de dado, comportamento ou contrato |
| D-V2-084 | 2026-07-27 | Migrations `0018_agent_brain_config.sql` e `0019_agent_brain_service_role_rpcs.sql` aplicadas no Supabase real (`ovctadcrvnfpgxzplupp`) via MCP `apply_migration`, com autorização explícita do Fernando ("sim aplique as 2 migrations") depois do bloqueio do classificador de segurança da sessão anterior (D-V2-082). Confirmado por leitura (`execute_sql`, sem mutar nada): tabela `agent_brain_config` + 5 funções presentes, RLS forçada. Advisor de segurança revisado: `agent_brain_config` cai no mesmo INFO `rls_enabled_no_policy` intencional de `agent_video_config`/`tenant_invites`; as 3 RPCs expostas a `authenticated` caem no mesmo WARN já aceito e documentado para toda RPC `portal_*`; as 2 RPCs `_service` corretamente não aparecem nesse WARN (só `service_role` chama) — nenhum problema de segurança novo | o guardrail de DDL em produção fez exatamente o papel esperado: pausou para uma decisão explícita em vez de aplicar sozinho, e a autorização veio no turno seguinte da conversa, não inferida | alta — tabela nova, aditiva, sem policy própria (só RPC), reversível com um `DROP` se necessário; falta ainda `SUPABASE_SERVICE_ROLE_KEY` e o rewiring de uma persona real pro endpoint funcionar de ponta a ponta |
| D-V2-083 | 2026-07-27 | M4-04: rota `/api/brain/[agentId]/chat/completions` completa e testada — `handle-chat-request.ts` autentica por bearer (mesma mensagem genérica para segredo inválido e agente incompatível, não vaza qual metade errou) e degrada pra fala explícita (nunca 500 cru) toda falha DEPOIS de autenticar, seguindo Art. 14. Duas RPCs novas `_service` (`0019`, granted só a `service_role`) resolvem tenant/agente pelo hash e logam custo, porque `portal_search_knowledge`/`portal_log_ai_usage` exigem `auth.uid()` que não existe numa chamada servidor-a-servidor. Preço de tabela DUPLICADO da `portal_log_ai_usage` real (0017) em vez de refatorá-la — evita risco numa função já validada em produção com dólares reais. RAG real fica de fora deste corte (gap declarado, `retrieveKnowledge` devolve vazio) — não é o RAG que falta, é a ausência de sessão de usuário que impede reusar a RPC existente | escopo deliberadamente reduzido: ligar RAG exigiria uma terceira RPC `_service` e mais uma rodada de aplicação bloqueada; melhor entregar o cérebro funcional (identidade + método + percepção) agora e declarar o gap do que atrasar tudo por uma peça | alta — código novo, nada em produção, nenhuma persona real aponta pra cá |
| D-V2-082 | 2026-07-27 | M4-03: `0018_agent_brain_config.sql` escrito e revisado (tabela tenant-scoped + 3 RPCs `SECURITY DEFINER` no padrão de `agent_video_config`), `secret.ts` gera e hasheia o segredo sempre na aplicação (nunca em SQL — confirmado que o projeto nunca usa `pgcrypto`/`extensions.gen_random_bytes`, então não introduzi essa dependência). A tentativa de aplicar no Supabase real (`ovctadcrvnfpgxzplupp`) via MCP `apply_migration` foi BLOQUEADA pelo classificador de segurança da sessão — DDL em produção é ação difícil de reverter e pede confirmação explícita, mesmo sob instrução de "autonomia 100%". Não tentei contornar (ex.: `execute_sql` no lugar do `apply_migration` recusado); migration fica commitada mas não aplicada até o Fernando confirmar | o próprio guardrail da plataforma tratou "DDL em banco de produção" como categoria que exige aprovação humana, mesmo com autonomia ampla concedida pelo usuário na conversa — autonomia concedida em chat não substitui o controle de infraestrutura de produção | alta — nada foi aplicado; reverter é só não aplicar o arquivo |
| D-V2-081 | 2026-07-27 | M4-02: `tavus-request.ts` traduz o array `messages` do Tavus para o formato do núcleo do cérebro. Toda mensagem `system` do Tavus é descartada da conversa (identidade e método continuam sendo só os nossos — Art. 15), mas é varrida por tags `<user_appearance>`/`<user_emotions>`/`<user_screenshare>` antes de descartar, porque a documentação oficial não garante em qual papel de mensagem o raven-1 injeta a leitura ambiente — o parser varre todas para não perder a percepção se ela vier num lugar diferente do esperado. Falha fechada por padrão em toda entrada malformada (array vazio, role desconhecida, content não-string, sem turno final de usuário, tetos de tamanho) | decisão de design deliberada, não observação empírica — o spike (D-V2-076) não conseguiu capturar um payload real do Tavus com percepção (faltou microfone real no navegador automatizado), então o parser foi construído para ser robusto à incerteza de posicionamento em vez de assumir um único formato baseado só na doc | alta — módulo novo, puro, sem uso em produção ainda; M4-04 vai validar contra tráfego real do Tavus antes do gate humano de produção |
| D-V2-091 | 2026-07-31 | Capacidade de reunião externa ligada de verdade: chave do Recall.ai (que já estava no Doppler) validada contra a API real — região `us-west-2` confirmada empiricamente (as outras 3 respondem 401), `RECALL_API_KEY`/`RECALL_API_REGION`/`RECALL_WEBHOOK_TOKEN` + `RESEND_API_KEY`/`PORTAL_PUBLIC_URL` configuradas no Railway. UI nova em `/agentes/<id>/testar`: cola o link do Meet/Zoom/Teams, opcionalmente agenda no horário da Flórida, e o agente entra. **Rota `/rosto-agente`** criada como a página que o bot transmite: entra na sala do Tavus sozinha (Daily callObject, sem câmera/mic próprios) e mostra só o vídeo da agente em tela cheia | achado que teria quebrado a demo ao vivo: o Recall.ai renderiza LITERALMENTE a página passada como `output_media.camera`, e a URL crua do Tavus abre uma tela de "digite seu nome" — os participantes veriam o formulário, não a agente. Verificado no navegador com conversa Tavus real antes de qualquer teste em reunião: a Rafaela aparece sangrando na tela, sem UI. Guardrails da rota pública (o bot não tem sessão nossa): allowlist de host `tavus.daily.co` — sem isso seria um renderizador de página arbitrária dentro de reuniões alheias —, `noindex` e exclusão documentada no matcher do middleware. Webhook do Recall.ai é config de dashboard (Svix), não tem API: fica como passo manual, e é opcional para testar (sem ele o agente entra igual, só o status no nosso banco não atualiza) | média — reversível removendo a seção da UI; chaves saem das variáveis do Railway |
| D-V2-092 | 2026-07-31 | Ciclo de áudio da reunião externa fechado nos dois sentidos. O primeiro teste ao vivo (Meet real, bot admitido pelo Fernando) provou o vídeo — a Rafaela apareceu como participante com o rosto correto — mas ela não ouvia ninguém nem era ouvida. O palco (`/rosto-agente`) entrava na sala do Tavus com `startAudioOff: true` e sem microfone algum: a agente ficava surda, e sem entrada não havia o que responder. Correção: dentro do bot do Recall.ai, `getUserMedia({audio:true})` devolve o áudio da REUNIÃO (o bot concede a permissão automaticamente, sem clique) — esse track vira o `audioSource` do join no Daily. Somado a isso, `play()` explícito no elemento de áudio da agente, porque é o áudio DA PÁGINA que o bot captura e envia à reunião, e `autoPlay` isolado pode ser barrado por política de autoplay | o mecanismo estava documentado (docs.recall.ai/docs/stream-media, seção "Listening to meeting audio from the webpage"), mas a implementação inicial só cobriu o vídeo — o áudio foi presumido como automático. Só um teste com participante humano de verdade exporia isso: o pipeline inteiro (chave, região, bot, palco, recibo, webhook) estava verde e mesmo assim a conversa era impossível. Fora do bot, `getUserMedia` falha e a página degrada para só-vídeo em vez de quebrar | baixa — a mudança é local ao palco; reverter volta ao estado mudo |
| D-V2-093 | 2026-07-31 | Áudio picotado/robotizado na reunião externa corrigido por dois fatores somados: (1) o variant default `web` do bot do Recall.ai dá 250 millicores + 750MB à página de Output Media — CPU insuficiente pra chamada WebRTC completa (decode 720p + áudio bidirecional), produzindo underruns nos dois sentidos; entrada imediata (câmera ligada) agora usa `web_4_core` (2250 millicores/5250MB, US$0,60/h pay-as-you-go), com o campo `variant` novo no adapter provider-recall aplicado aos 3 platforms; bot sentinela agendado continua no default barato. (2) `getUserMedia` do palco agora pede o sinal CRU (echoCancellation/noiseSuppression/autoGainControl desligados) — o "microfone" é o áudio já mixado/processado da reunião, e o reprocessamento do Chrome distorce e bombeia volume | segundo teste ao vivo (Meet real, com o Fernando): vídeo perfeito, áudio presente mas inutilizável — sintoma clássico de CPU faminta, confirmado na doc oficial do Recall.ai ("Addressing audio and video issues: bot variants"), que existe exatamente porque esse é um problema comum de Output Media. Primeira iteração do teste também expôs que o bot expira na sala de espera (`timeout_exceeded_waiting_room`) se ninguém admite a tempo — operacional, não bug: disparar com o anfitrião já dentro. Pedido do Fernando: próximos testes com a Raissa (institucional) em vez da Rafaela | baixa — variant é por-chamada; reverter = tirar o campo |
| D-V2-094 | 2026-07-31 | Produto aberto para clientes de verdade — três fechos de uma vez: (1) **auto-provisão de vídeo**: ativar um agente sem config de vídeo agora cria a persona Tavus automaticamente (cérebro Método Silva, voz ElevenLabs Raissa, raven-1 com 8 queries, `tavus-gemma-4`, tools de slides anexadas) e grava via RPC nova (supabase-only 0022) — antes, só os agentes demo configurados à mão tinham vídeo, um cliente novo ficava sem; best-effort: falha nunca desfaz a ativação. Validado no caminho real: persona `p243348ebb20` criada pra Marina e config confirmada. (2) **teto de 20 conversas de vídeo/dia por tenant** nas 3 actions (sala, apresentação, reunião externa) — cada conversa custa dinheiro real e não havia NENHUM cap de vídeo; falha fechada como o cap de tokens. (3) **seção "Plano e contratação"** em /configuracoes: plano Avaliação com limites explícitos e CTA de contato — contratação passa por diagnóstico humano, sem tabela de preço pública inventada. E2e clique-a-clique ampliado para 11 specs (signup, ciclo de convite criar→revogar, revogar/reativar fonte) — 11/11 verdes | decisão autônoma autorizada ("pode decidir escolhendo as melhores decisões"). Billing/Stripe deliberadamente NÃO construído: sem decisão comercial de preços e sem conta Stripe, seria scaffolding morto — o funil honesto hoje é trial self-serve + contato comercial. Achado real do e2e: a fonte seed "FAQ" nunca teve ingestão e não reativa (correto por design) — teste trocado pra fonte com conteúdo e estado restaurado via SQL | média — provisão é por-agente e reversível; teto é constante única; seção de plano é UI |
| D-V2-095 | 2026-07-31 | Onda de produção 2 — completude operacional: (1) **exclusão real** de agente em rascunho (`portal_delete_draft_agent` — recusa agente com sessões) e de fonte revogada/pendente (`portal_delete_knowledge_source` — exige revogação antes), supabase-only 0023; usuários criavam mas não removiam nada, e os limites de 20/50 entupiam sem saída. Exclusão física (não soft): libera limite naturalmente e é a operação LGPD-amigável; dupla confirmação na UI. (2) **/termos e /privacidade** honestos (descrevem só o que o produto faz, provedores nomeados, direitos LGPD, contato), linkados no signup e no sitemap, marcados como v1 sujeitos a revisão jurídica formal (segue em PENDENCIAS_EXTERNAS). (3) **E2e 14 specs**: ciclos de vida completos clique-a-clique (agente criar→ativar→pausar→excluir; fonte criar→excluir) agora possíveis sem poluir o tenant demo — o teste limpa atrás de si | escolha deliberada de exclusão física versus 'archived'/'deleted' soft: os CHECKs suportam soft, mas as contagens de limite (create RPCs) contam todas as linhas — soft-delete exigiria reescrever 4 funções antigas; delete físico com guardas de segurança resolve o caso de uso real com 2 funções novas. Páginas legais são transparência operacional, não parecer jurídico — dito na própria página | baixa — RPCs aditivas; UI reversível |
| D-V2-096 | 2026-07-31 | CI passa a rodar o e2e Playwright (14 specs) a cada PR/push, job `e2e-portal` novo em `docs-qa.yml`: instala Chromium do Playwright (`--with-deps`), escreve `.env.local` efêmero no runner a partir de 4 GitHub secrets (`SUPABASE_URL`, `SUPABASE_PUBLISHABLE_KEY`, `DEMO_EMAIL`, `DEMO_PASSWORD`) e roda a suite contra fake providers + login demo real. `playwright.config.ts` ajustado para não fixar `channel: "chrome"` quando `process.env.CI` existe — usa o Chromium baixado pelo Playwright no Linux do runner em vez do Chrome do sistema (que só existe por causa do SIGABRT do chromium baixado nesta máquina Mac específica). Job condicionado a `secrets.DEMO_PASSWORD != ''`: sem os 4 secrets configurados no repositório, o job inteiro é pulado (nunca falha por ausência de credencial) — mesmo padrão do resto do projeto, documentado em NEEDS_CONNECTION.md | achado real: os 14 specs de e2e (incluindo os dois testes HTTP que pegaram bugs reais de middleware — D-V2-079, D-V2-088) só rodavam manualmente nesta sessão; nenhuma regressão futura seria pega automaticamente sem esse gate. PRs de fork não têm acesso a secrets do repositório, então o skip nesse caso é automático e não expõe a senha da conta demo a contribuidor externo | alta — só arquivo de CI/config de teste; sem secrets configurados o comportamento de hoje não muda em nada |
| D-V2-097 | 2026-08-01 | Domínio próprio `closer.axtroai.com` conectado no Railway (feito pelo Fernando fora desta sessão) — mas `PORTAL_PUBLIC_URL` (e-mails, URL base do palco `/rosto-agente` do bot Recall.ai) e `NEXT_PUBLIC_SITE_URL` (canonical, Open Graph, sitemap, robots — build-time) continuavam apontando pra raw URL `portal-production-b43e.up.railway.app`. Corrigido: as duas variáveis atualizadas no Railway pro domínio próprio e novo deploy disparado pra rebuildar o valor build-time. Verificado ao vivo pós-deploy: `sitemap.xml`, `robots.txt`, `og:url` e `<link rel="canonical">` todos em `closer.axtroai.com`. Webhook do Recall.ai NÃO precisou migrar — a raw URL do Railway continua servindo em paralelo ao domínio próprio (confirmado com `curl`, 200) | achado real durante a auditoria do domínio pedida pelo Fernando: "atualiza o needs connection" só faria sentido documentar como resolvido se as variáveis realmente refletissem o domínio novo — deixar como estava geraria e-mails, sitemap e metadados de SEO com a marca errada indefinidamente | baixa — só variáveis de ambiente + rebuild; raw URL do Railway segue funcionando como fallback |
| D-V2-098 | 2026-08-01 | **CI do e2e-portal estava quebrado desde D-V2-096** (24h sem ninguém perceber) por dois bugs reais, achados só ao cadastrar os secrets pedidos pelo Fernando e verificar o run de verdade em vez de confiar no docs_qa.py local: (1) `if: ${{ secrets.DEMO_PASSWORD != '' }}` no nível do JOB — `jobs.<job_id>.if` só aceita os contextos `github/needs/vars/inputs`, não `secrets` (confirmado na doc oficial de contexts do GitHub); isso não gera erro de lint, mas invalida o workflow YAML INTEIRO na validação do GitHub — os 4 jobs do arquivo (não só o e2e) somem silenciosamente e todo run vira "workflow file issue" com 0 jobs. Corrigido com o padrão canônico: job `check-e2e-secrets` roda um `step` (onde `secrets` É permitido) que expõe `has-secrets` como output; `e2e-portal` passa a usar `needs.check-e2e-secrets.outputs.has-secrets` no `if`. (2) o job `e2e-portal` nunca buildava os pacotes internos do monorepo (`@axtro/domain`, `@axtro/provider-openrouter`, `@axtro/provider-tavus`) antes de subir o servidor Next.js do Playwright — `Module not found` nas 3 rotas de API que os importam, 1 dos 14 specs falhava (timeout esperando o dashboard depois do login). `build-portal` já tinha o passo certo (`pnpm typecheck` builda via `tsc --build` com project references, efeito colateral documentado no próprio comentário daquele job) — só faltava copiar pro job novo. Validado com 2 rodadas completas numa branch de teste (`test/ci-fix-verify`, deletada depois) antes de tocar a `main`: primeira rodada provou os 5 jobs aparecendo (fix 1), segunda provou os 14 specs passando de verdade em CI (fix 2) | lição igual à do llms.txt (D-V2-079) e do /api/* (D-V2-088): comportamento de CI/schema do GitHub só se prova com um run real, nunca com lint local ou parse de YAML genérico (PyYAML inclusive interpreta `on:` como booleano e mesmo assim não pega esse tipo de erro — são validações de schema específicas do GitHub, não de sintaxe YAML). A partir de agora, toda mudança em `.github/workflows/*.yml` que adicionar `if`/`needs` novo deve ser testada numa branch descartável antes de ir pra `main` | alta — só arquivo de CI; comportamento de deploy/produção nunca foi afetado |
| D-V2-099 | 2026-08-01 | Webhook do Recall.ai ganha uma segunda camada de autenticação: verificação de assinatura HMAC-SHA256 (formato Standard Webhooks/Svix, confirmado em docs.recall.ai/docs/authenticating-requests-from-recallai) sobre os cabeçalhos `webhook-id`/`webhook-timestamp`/`webhook-signature`, além do token na URL já existente. O comentário original do código (D-V2-091) dizia explicitamente que HMAC foi descartado por "formato não confirmado na doc" — o Fernando gerou o segredo do workspace (`RECALL_WEBHOOK_SECRET`, prefixo `whsec_`) no dashboard deles e colocou no Doppler, o que confirma o formato e destrava a implementação. Verificação é OPCIONAL (só roda se a env var estiver setada) pra não quebrar ambientes sem o segredo; quando setada, é obrigatória (fail closed) e não substitui o token — as duas camadas são exigidas juntas. Inclui janela de tolerância de ±5min contra replay e suporte a múltiplas assinaturas espaço-separadas (rotação de segredo). Sincronizado no Railway prod. 9 testes novos com vetor de teste real (HMAC calculado com Node crypto, não segredo de produção) | o token na URL é funcional mas tem uma fraqueza conhecida — pode vazar em logs de acesso, header Referer, ou histórico de proxy, porque viaja na própria URL. Assinatura em cabeçalho HTTP não tem esse problema. Optamos por somar em vez de substituir: zero risco de regressão no que já funciona (confirmado ao vivo com a Raissa), e ganho de defesa em profundidade real | baixa — puramente aditivo; sem a env var, comportamento idêntico ao anterior |
| D-V2-100 | 2026-08-02 | Rodada de hardening autônomo (prompt mestre do Fernando, Ultracode): auditoria multi-agente de 6 dimensões (44 achados) + 25 correções reais em 4 commits. Destaques: o cérebro customizado NUNCA teria funcionado (prompt de vídeo >10k chars numa única mensagem system vs. cap de 4k do adapter — toda chamada degradava; corrigido com split por seção + eval golden que atravessa o validador REAL do adapter); 4 caminhos de gasto sem teto fechados (brain 500k tokens/dia + rate limit, video-session 20/dia + ledger, reunião externa contando no cap, automatic_leave no bot Recall); injeção via tag de percepção em turno de usuário bloqueada (percepção só de mensagens system); sequestro da persona institucional fechado (0024 aplicada: 'platform' fora do self-service + pin RAISSA_VIDEO_AGENT_ID); agendado não cria mais sala Tavus que expira (sala nasce no webhook com claim atômica); doutrina Maestria Humana autoral (sem texto de livro de terceiro — decisão de IP mantida) nas 3 personas vivas. Docs novos: AUDIT_BASELINE, SECURITY_REVIEW, COST_OPTIMIZATION, QA_REPORT, USER_JOURNEY, RELEASE_READINESS, AUTONOMOUS_EXECUTION_STATE, FINAL_IMPLEMENTATION_REPORT | os verificadores adversariais do workflow caíram no limite de sessão — cada achado corrigido foi re-verificado MANUALMENTE no código antes da correção (nenhum fix às cegas); 532 Node + 26 Py + typecheck + lint + 9 validadores + next build verdes em cada lote | alta — tudo aditivo/testado; migration 0024 aplicada é create-or-replace reversível |
| D-V2-101 | 2026-08-03 | Cobrança real dos planos (Piloto/Crescimento/Escala, US$497/1.497/3.997 por mês, minutos incluídos por mês virou CONVERSAS incluídas — o produto não mede duração real de chamada, cobrar por minuto prometeria precisão que não existe): package provider-stripe (Checkout Sessions, Billing Portal, Billing Meters API pra overage — não a usage_records legada, removida desde a versão 2025-03-31.basil), catálogo de planos em código, migrations 0025 (achado crítico: cost_events_unit_type_check NUNCA permitiu unit_type='conversation' desde 0011/2026-07-18 — todo log de custo de vídeo falhava silenciosamente e o teto diário de vídeo nunca esteve ativo em produção, confirmado via execute_sql, zero linhas 'conversation' existiam) e 0026 (tenant_subscriptions, RLS forçada sem policy, 2 RPCs), webhook /api/stripe/webhook (HMAC Stripe-Signature, guarda monotônica por event_created contra entrega fora de ordem), checkVideoCap agora ciente do plano (teto diário de segurança sempre ativo, teto mensal do plano nunca bloqueia — overage é cobrado, não rejeitado), página /precos pública e seção de plano/uso em Configurações. Revisão de segurança adversarial (4 dimensões, 3 verificadores por achado grave) confirmou 4 P1 REAIS, todos corrigidos na mesma rodada: plan_id lido só de metadata da assinatura (nunca reescrita pela Stripe numa troca de plano pelo Customer Portal — corrigido resolvendo o plano pelo price id do item licensed), startCheckout/openBillingPortal sem checagem de papel no servidor (corrigido, mesmo padrão das RPCs administrativas), erro de leitura engolido em reportConversationOverageIfNeeded (corrigido), guarda anti-produção do script de setup só bloqueava sk_live_ (agora bloqueia rk_live_ também) | preço ancorado em unit economics medido (docs/PRICING_UNIT_ECONOMICS.md, ~US$4,55/conversa de 12min de custo real): margem 90%+ no incluído, nunca abaixo de 55% no overage mesmo no pior caso — não dá pra copiar preço flat-ilimitado de AI SDR de texto porque o custo marginal de vídeo é ordens de grandeza maior | 563 Node + 26 Py + typecheck + lint + 9 validadores + next build verdes; migrations 0025/0026 escritas mas NÃO aplicadas (aguardando autorização explícita do Fernando); nenhuma chave Stripe real configurada ainda (tudo testado com fakes) |
| D-V2-102 | 2026-08-03 | Lote final da auditoria D-V2-100 — os 15 achados menores que sobraram dos 3 lotes: (1) **headers de segurança HTTP** no next.config (produção não servia NENHUM — HSTS sem includeSubDomains, nosniff, Referrer-Policy, X-Frame-Options DENY exceto /rosto-agente que o bot do Recall renderiza, Permissions-Policy delegando camera/microphone pro origin tavus.daily.co porque a sala do /testar é iframe cross-origin — restringir a self quebraria a sala); (2) validação de `next` no /auth/callback (hardening; o PKCE já bloqueava o ataque real, refutado pelo verificador); (3) redação de `error.message` na telemetria (e-mail + formatos de credencial); (4) erros de login/signup/nova-senha mapeados pra pt-BR com role="alert" (antes: "Invalid login credentials" cru); (5) /login lê ?demo=unavailable (CTA da landing não é mais beco sem saída); (6) not-found.tsx pt-BR; (7) try/catch com banner na página /testar; (8) sala de vídeo ganha Encerrar/Copiar link/Abrir em nova aba (iframe morto pós-10min + testável do celular); (9) CTAs de demo da landing com useFormStatus; (10) footer da landing linka termos/privacidade; (11) copy do /conhecimento vazio corrigida ("provedor de embeddings" era mentira desde D-V2-070); (12) ingestão passa a funcionar no modo demonstração (fakeProvidersEnabled nas 2 guardas + mensagem de falha de ingestão não induz mais duplicata); (13) **painel "Reuniões externas"** na página /testar — a RPC portal_list_meeting_bot_sessions (0021) e o webhook de status existiam em produção mas NENHUMA UI lia; quem agendava perdia toda visibilidade; (14) DST da Flórida: segunda iteração de offset (03:30 do spring-forward errava 1h), 4 testes novos; (15) leitura de config na auto-provisão fail-closed (erro de RPC não vira mais persona duplicada por cima de persona curada). CI: concurrency group + push só em main (runs duplicados mutavam o mesmo tenant demo em paralelo) + heredoc quotado (senha com $ corrompia) ; e2e: auto-reparo dos testes que mutam seeds (Bruno/Kit 05) + /rosto-agente e /recuperar-senha no teste de rotas públicas; /configuracoes desacoplada do billing (falha da RPC de cobrança degrada só o card, não a página inteira — condição pro deploy antes das migrations 0025/0026) | fechar TODOS os achados verificados da auditoria antes do deploy, sem deixar cauda; cada item cita o achado de origem no comentário do código | baixa — itens independentes, todos pequenos e revertíveis isoladamente |
| D-V2-103 | 2026-08-05 | W4 — página de venda publicada + fecho do backlog deferido da auditoria: (1) **/precos descobrível**: link "Planos" na nav e no footer da landing + entrada no sitemap (a página de venda existia mas não era linkada de lugar NENHUM — despublicada na prática); (2) **digest de conhecimento round-robin** (0027): orçamento dividido por fonte com piso de 600 chars, fontes recentes primeiro — antes, com 2+ fontes o vídeo recebia só o começo da mais antiga como "única fonte de fatos" (tenant demo: passa de 1 fonte truncada pra ~5); (3) **custo real do OpenRouter**: adapter pede `usage: {include:true}` e expõe `reportedCostUsd`; `portal_log_ai_usage` grava source `provider_reported` com o valor faturado exato — neutraliza o achado do rate card cravado no Haiku com OPENROUTER_MODEL configurável; (4) **cost_event do bot Recall** por sessão de reunião externa (convenção: evento com custo 0, precificação na leitura via rate_card_ref); (5) **fuso por tenant no agendamento**: wallClockToUtcIso(tz) genérico + default_timezone do tenant na UI/action/painel (dono em São Paulo agendava "15:00" e o bot entrava 1-2h errado) — Flórida segue default; (6) **réplica EN**: saudação + contexto de vendas em inglês quando o agente é 'english' (abria call em pt-BR); (7) **copiar conversa** no chat de teste; (8) **CI valida build de produção**: webServer do Playwright faz `next build && next start` no CI (classe de falha só-de-produção passava verde no dev server); (9) fail-closed na leitura de config do meeting-bot (call site que ficou fora do fix D-V2-102). Migração 0027 aplicada e verificada (lição nova: `create or replace` com parâmetro novo cria OVERLOAD e quebra revoke/RPC — sempre DROP da assinatura antiga; o primeiro apply falhou nisso e reverteu limpo) | fechar 100% do backlog implementável sem o Fernando antes de considerar a página "publicada pra venda"; itens que continuam com ele: chaves Stripe, revisão jurídica, DPIA | baixa-média — itens independentes; digest muda comportamento de contexto de vídeo (melhora declarada), o resto é aditivo |
| D-V2-104 | 2026-08-06 | **O primeiro run do e2e em modo produção (D-V2-103) pegou um bug de framework que afeta usuários reais**: server actions travavam intermitentemente no build de produção — o insert acontecia no banco, mas a resposta HTTP nunca voltava; o usuário ficava com "Criando..." eterno e a lista nunca atualizava (e clicar de novo criaria duplicata). Flake de ~50% reproduzido deterministicamente em local prod (`next build && next start`): cada run falhava num passo diferente (criar, pausar→ativar, confirmar exclusão) — assinatura de hang de transporte, não de lógica. Causa raiz: bug conhecido do binário do React empacotado no Next 16.2.x (vercel/next.js discussion 88767, "Server Actions + useTransition hanging indefinitely"); fix upstream só na série seguinte. Correção: upgrade Next 16.2.10 → 16.3.0 + React 19.2.7 → 19.2.8 — 3 rodadas consecutivas dos specs de ciclo verdes pós-upgrade (antes: ~50% de falha). Hardening que fica mesmo com o fix: router.refresh() explícito pós-sucesso nos 5 componentes de mutação (toggle/delete/create de agente, create/actions de fonte — atualização de UI determinística, não dependente do comportamento de revalidação do framework), desarme da dupla confirmação 5s → 8s (5s desarmava antes do segundo clique legítimo com re-render lento de prod), artifact de falha do CI corrigido pra test-results/ (playwright-report/ só existe com reporter html — a primeira falha real ficou sem evidência), PW_REUSE=1 no playwright.config pra depurar contra servidor de produção local, e limpeza dos 5+ agentes órfãos "E2E Ciclo" que os runs falhos deixaram no tenant demo (um preso em Ativa) | o gate novo de CI pagou por si no PRIMEIRO run: sem ele, esse hang seguiria mordendo usuários reais em produção indefinidamente — dev server nunca reproduz (React de desenvolvimento não tem o bug). Upgrade de framework validado pela pipeline inteira + suite e2e completa em modo produção antes do deploy | média — upgrade minor de framework; rollback = reverter package.json/lockfile |
| D-V2-105 | 2026-08-06 | Onda W5 — auditoria multi-agente em 6 dimensões novas (billing, acessibilidade, dashboard/equipe, banco/RLS, qualidade de código, ideias), 12 achados, verificação adversarial nos críticos/altos. Corrigidos: (1) **checkout Stripe sem idempotência** — duplo clique/duas abas podiam gerar duas assinaturas cobrando em paralelo com só uma visível na UI (achado `high`, confirmado pelo verificador); chave de idempotência determinística (janela de 1min) + botão "Assinar" desabilita no primeiro clique (useFormStatus); (2) `existingStripeCustomerId` agora reaproveitado ao reassinar depois de cancelar (evita Customer duplicado no Stripe); (3) webhook Stripe loga evento do tipo certo mas payload malformado (antes: silêncio idêntico ao de tipo fora de escopo); (4) **portal_remove_member** — não existia NENHUMA forma de revogar acesso de um membro já aceito, só convite pendente (gap de segurança operacional real: colaborador desligado só perdia acesso com suporte manual no banco); guarda contra remover o último admin e contra se auto-remover; (5) sidebar mobile fechada agora sai da árvore de tabulação/leitor de tela (`visibility:hidden` na media query — transform sozinho só move visualmente); (6) `--text-faint` corrigido de ~4.1:1 para ~5.6:1 de contraste (abaixo do mínimo AA 4.5:1 nos dois blocos `:root` do CSS, incluindo o que vence a cascata); (7) `database/rls-policy-matrix.md` corrigido — documentava um mecanismo (`SET LOCAL app.tenant_id`) que nunca foi implementado; a policy `tenant_isolation` das ~30 tabelas do kernel é código morto (nega tudo, nunca foi vulnerabilidade — o GUC sempre retorna NULL) mas o doc lia como proteção ativa; SQL não tocado (risco de fingerprint da migration portátil), só a documentação; (8) dedup do padrão "ler portal_agent_video_config" — o MESMO bug de fail-open tinha sido corrigido separadamente em 4 call sites ao longo de duas auditorias porque a lógica estava copiada; extraído `lib/video-config.ts` (`resolveAgentVideoConfig`/`fetchKnowledgeDigest`), usado pelos 4; (9) e-mail aos admins quando reunião externa termina (`ended`/`failed`) — o evento de negócio mais importante do produto não notificava ninguém, só guarda no `applied=true` da RPC de status pra nunca duplicar em retry; (10) erros de RPC do perfil da conta traduzidos pra pt-BR (mesmo padrão de `INVITE_ERROR_MESSAGES`). Migration 0028 aplicada e verificada no banco vivo. **Decisão deliberada de escopo**: a ideia de maior valor do lote ("nenhuma conversa — chat, vídeo ou reunião externa — fica registrada pro dono revisar depois", severidade `critical` como VALOR) foi documentada em RELEASE_READINESS.md como próximo passo em vez de implementada nesta onda — é uma feature nova (tabela + RLS + RPC + 3 pontos de integração + UI), não um bug, e o restante do lote já tinha 12 itens; construí-la com pressa no fim de uma sessão longa arriscaria menos rigor do que o resto da auditoria recebeu. Pipeline completa: 578 Node (+10 novos) + 26 Python + lint + typecheck + build + 9 validadores + e2e contra build de produção | mesma disciplina de D-V2-100/102/104: auditoria adversarial, corrigir o que é bug real, documentar honestamente o que foi deliberadamente adiado e por quê, nunca implementar feature grande sem o mesmo rigor do resto | baixa-média — itens de bug são pequenos e independentes; RPC nova é aditiva; doc de RLS é só texto |
| D-V2-106 | 2026-08-10 | Histórico de conversa (chat/vídeo/reunião externa) — o gap de maior valor deixado de propósito de fora da onda W5 (D-V2-105). Migration 0029: tabela `conversation_transcripts` (RLS forçada, zero policy) + 5 RPCs — `portal_upsert_conversation_transcript` (autenticada, chat sandbox), `portal_append_transcript_turns_service` (`service_role`, webhooks — resolve a linha só por `surface+external_ref`, nunca confia em tenant vindo de webhook, Art. 9), `portal_get_meeting_bot_agent_service`, `portal_list_conversation_transcripts`, `portal_get_conversation_transcript`. Captura nas 3 superfícies: chat sandbox (upsert incremental a cada resposta), vídeo Tavus (`callback_url` na criação da conversa → webhook `/api/tavus/webhook`, evento `application.transcription_ready`, sem HMAC — auth por token na URL, confirmado ausência de assinatura na doc oficial via WebFetch), reunião externa Recall.ai (`recording_config.transcript.recallai_streaming` no bot → webhook `transcript.done` → 2 hops: metadata → `download_url` assinado (domínio diferente, SEM header de Authorization da API) → conteúdo). Arquitetura placeholder-então-preenche: a linha nasce em contexto autenticado (tenant conhecido de verdade) no momento em que a conversa/bot é criado; o webhook só PREENCHE `turns` depois, nunca cria a linha. Tela nova `/conversas` (lista + detalhe). **Revisão adversarial (workflow multi-agente, 4 dimensões × 3 verificadores) confirmou 1×P0/2×P1, mesma causa raiz**: o índice único original era só `(tenant_id, surface, external_ref)` — a RPC do webhook resolve a linha SEM tenant_id no WHERE, então sem uma garantia global de banco, dois tenants podiam colidir num `external_ref` e um webhook sobrescreveria as duas linhas em silêncio, vazando histórico de lead pro tenant errado. Corrigido: índice único GLOBAL parcial pra vídeo/reunião (mesma disciplina já usada em `meeting_bot_sessions`, 0021, pra essa mesma classe de id gerado por provider) + guarda que recusa (não silencia) qualquer match ambíguo + `app.validate_transcript_turns` (teto de 1000 turnos, shape validado) chamado nas duas RPCs de escrita. Migration 0030 (achado colateral, não relacionado): `portal_upsert_tenant_subscription_service` (0026, já em produção) tinha `GET DIAGNOSTICS` numa variável `boolean` — confirmado ao vivo que quebra com `row_count >= 2` (`22P02`); corrigido pro tipo certo antes do primeiro webhook real da Stripe depender disso. Migrations 0029 e 0030 aplicadas em produção e verificadas via `execute_sql`, sob autorização explícita do Fernando | pedido explícito do Fernando ("continua implementando tudo que falta") + o próprio gap já estava documentado como o item de maior valor adiado em D-V2-105/RELEASE_READINESS.md; contratos de API dos dois providers confirmados via WebFetch contra a doc oficial antes de escrever qualquer linha (nunca adivinhados) — mesma disciplina do resto do projeto | baixa-média — tabela/RPCs novas são aditivas; 0030 é `create or replace` reversível de uma função já em produção; 594 Node + 26 Python + typecheck + lint + 9 validadores + `next build` verdes |
| D-V2-107 | 2026-08-11 | Auditoria multi-agente pós D-V2-106 (workflow: 5 finders — segurança, bugs, onboarding, alertas de custo, prontidão geral — + 3 verificadores adversariais por achado grave) e correção de tudo confirmado. **2 P1 confirmados por unanimidade**: (1) `startCheckout` usava definição de "assinante ativo" diferente da UI (`billing-section.tsx`) — contas com status Stripe `unpaid`/`incomplete_expired`/`paused` ficavam travadas, sem conseguir assinar de novo (bloqueado como "já assinante") nem gerenciar (a UI só mostra o botão "gerenciar" pra `active`/`trialing`/`past_due`); corrigido com classificação compartilhada (`BILLING_TERMINAL_STATUSES`/`hasNonTerminalSubscription` em `lib/billing/plans.ts`) usada nos dois lados, mais um branch `NeedsAttentionCard` novo na UI pros 3 status órfãos. (2) `/privacidade` nunca foi atualizada depois do D-V2-106 — não mencionava que o histórico de conversa (chat/vídeo/reunião) é armazenado na íntegra e visível a qualquer membro do tenant; corrigido com o texto real do que o produto faz hoje, mesmo princípio já declarado no arquivo. **3 achados menores corrigidos**: `SECRET_PATTERN` da telemetria não cobria os formatos `sk_test_`/`sk_live_`/`rk_test_`/`rk_live_` da Stripe (só o formato com hífen da OpenRouter); healthcheck do Railway apontava pra `/login` (página 100% estática — um deploy com env var do Supabase quebrada passaria "saudável" indefinidamente) — trocado por `/api/health`; nenhum teste e2e cobria o caminho de LEITURA do histórico de conversa. **Ao escrever esse último teste e RODÁ-LO de verdade (não só ler o código), achado um bug real que a auditoria estática não pegou**: em modo demonstração (`PORTAL_FAKE_PROVIDERS=1`, usado pelo CI e por qualquer visitante testando o produto), `sendAgentPreviewMessage` retornava a resposta simulada ANTES do ponto onde `recordChatTranscript` é chamado — nenhuma conversa de demonstração virava histórico. Confirmado via Railway que a env var nunca é setada em produção real (clientes pagantes não são afetados), mas o CI e a experiência de demo ficavam sem conseguir provar a própria feature; corrigido registrando o transcript também no branch de fake mode. **Também implementado (propostas da auditoria)**: onboarding — o checklist do dashboard tinha um item sempre-verdadeiro ("Conta provisionada", provisionada em silêncio antes da página renderizar) que dava a falsa sensação de progresso sem o usuário ter feito nada; trocado por "Testar no chat", medido via `usage.services_7d` (linha `portal.agent_preview`) que a página já busca — zero query nova. Alertas proativos de custo — `docs/COST_OPTIMIZATION.md` declarava esse gap explicitamente ("os tetos cortam, mas não avisam antes"); `lib/cost-alerts.ts` (`maybeAlertCostCap`) chamado fire-and-forget nos 4 pontos que já checam teto diário (vídeo do portal, vídeo do lead institucional/`leads/video-session`, tokens do brain, tokens do chat de teste), dispara e-mail aos admins em 80%/100% de uso; dedup por `(tenant, teto, threshold, dia UTC)` via nova migration `0031_cost_alert_dispatches.sql` (`portal_claim_cost_alert_service`, INSERT...ON CONFLICT DO NOTHING — resolve corrida entre requisições concorrentes sem lock de aplicação; `portal_billing_status` ganha `tenant_id` no retorno, campo aditivo, pra `checkVideoCap` não precisar de query extra). Erro de design evitado por revisão própria: `email.ts` importava `telemetry.ts` via alias `@/lib/telemetry`, o que teria quebrado o `tests/portal/video-cap.test.mjs` existente assim que `cost-alerts.ts` (importado por `video-cap.ts`) tentasse carregar `email.ts` sob Node puro (sem resolução de alias) — corrigido trocando esse import por caminho relativo `.ts` antes de integrar, sem afetar nenhum consumidor via `@/lib/email` (Next.js resolve os dois estilos igual). Migration 0031 escrita mas **NÃO aplicada** — aguardando autorização explícita do Fernando, mesmo padrão de sempre | pedido explícito do Fernando ("continua implementando... com autonomia 1000%... até o sistema estar preparado para o mercado de usuários finais"), executado via `/loop` em modo dinâmico — a mesma disciplina de todas as rodadas anteriores (achado real, verificado por leitura de código E por execução real quando possível, nunca corrigido às cegas); o bug do modo demonstração é a prova concreta de por que "rodar o teste" vale mais que só "ler o código" | baixa-média — todos os itens são aditivos ou correções cirúrgicas; migration 0031 é a única peça que toca produção e está bloqueada até autorização; 611 Node + 26 Python + lint + typecheck + 9 validadores + `next build` + 15 e2e (build de produção local) verdes |
| D-V2-108 | 2026-08-11 | Fecha o gap de RAG no cérebro custom declarado desde D-V2-083 (M4-04): o endpoint `/api/brain/[agentId]/chat/completions` — o LLM que o Tavus chama pra persona de VÍDEO, servidor-a-servidor, sem sessão de usuário — nunca conseguia buscar conhecimento autorizado, porque `portal_search_knowledge` (0010) exige `auth.uid()`. Resultado prático: o mesmo agente citava fontes corretamente no chat de teste e "esquecia" tudo numa call de vídeo real com um lead. Migration `0032_search_knowledge_service.sql`: `portal_search_knowledge_service`, variante service-role de `portal_search_knowledge` — corpo de busca IDÊNTICO (mesmo filtro de fonte ativa, mesmo modelo de embedding, mesmo teto de resultados), só a resolução de identidade muda (`p_tenant_id` explícito, validado contra `public.tenants`, em vez de `auth.uid()`), mesmo padrão já usado por `portal_log_ai_usage_service` (0019) e `portal_get_meeting_bot_agent_service` (0028). `retrieveKnowledge()` em `route.ts` — antes um stub que sempre devolvia `[]` — agora embeda a pergunta (`embedQuery`, mesmo helper do chat de teste) e busca via a RPC nova, com o MESMO piso de similaridade (0.25 cosine) já usado no caminho de chat; qualquer falha (embedding indisponível, RPC fora do ar) degrada pra `[]` sem lançar — RAG indisponível nunca derruba a call de vídeo, e sem fontes o agente não inventa (Art. 14). Gap deliberadamente aceito e declarado: o custo da chamada de embedding desta busca não entra no ledger (`cost_events`) — `portal_log_ai_usage_service` tem preço fixo de chat (Haiku), não de embedding, e mudar sua assinatura pra aceitar um serviço diferente exigiria DROP+CREATE numa função já em produção (risco real, lição de D-V2-103) por um ganho econômico irrelevante (uma query de busca é ~30-100 tokens a US$0,02/1M, fração de centavo, já dominado pelo piso Tavus de US$0,175/conversa) — mesmo espírito de "custo por conversa é piso, não exato" já aceito no resto do produto. Migration 0032 escrita mas **NÃO aplicada** — aguardando autorização explícita do Fernando | continuação do `/loop` autônomo (pedido do Fernando, "continua implementando... até o sistema estar preparado para o mercado de usuários finais") — RAG no cérebro custom era o próximo item de maior valor real ainda não bloqueado por decisão externa (DPIA, bake-off de provider e migração pra chave live da Stripe seguem bloqueados; reunião externa agendada precisa de humano real) | baixa — mudança aditiva, espelha uma função já em produção (mesmo corpo de query, só a resolução de tenant muda); zero mudança de comportamento no caminho de chat existente; 611 Node + 26 Python + lint + typecheck + 9 validadores + `next build` verdes; migration 0032 é a única peça que toca produção e está bloqueada até autorização |
| D-V2-109 | 2026-08-11 | Onda 2 do `/loop` autônomo — auditoria multi-dimensão (cobertura de teste, qualidade de código, UX, integridade de dados, resiliência de provider), 26 agentes, 7 achados graves confirmados por verificação adversarial (0 refutados). **P0 corrigido**: `portal_log_ai_usage`'s branch `provider_reported` (0027) calculava `amount_usd` e `unit_cost_usd` como dois arredondamentos INDEPENDENTES do mesmo valor — viola `cost_events_amount_reconciliation_check` em ~90% das chamadas reais (reproduzido contra o Postgres real: `round(x,8)=0.04739379` vs a fórmula do CHECK `round(qty*round(x/qty,10),8)=0.04739384`). Efeito real: toda vez que o chat de teste gera uma resposta real (OpenRouter `usage.cost`, único chamador que passa `p_reported_cost_usd`), ~90% de chance da linha de custo ser descartada em silêncio desde que a 0027 foi ao ar (2026-08-05/D-V2-103) — corroendo o ledger exato, o teto diário de segurança (`portal_ai_tokens_today`, subconta uso real) e o painel de custo do tenant_admin. Corrigido na migration `0033_fix_reported_cost_reconciliation.sql`: `v_amount` agora DERIVADO de `v_unit_cost` pela mesma fórmula do CHECK (mesmo padrão que os outros 2 branches da função já usavam, e que 0017 já documentava como obrigatório) — correto por construção, validado contra o Postgres real com o contra-exemplo exato do achado antes de escrever a migration. **3 achados P1 corrigidos**: (1) `provider-recall` e `provider-stripe` tinham o mesmo bug — `clearTimeout` rodava assim que os headers chegavam, ANTES da leitura do corpo, então um corpo travado depois de um 200/201 nunca era interrompido pelo timeout (contradizia o comentário do próprio arquivo, que afirmava paridade com `provider-openrouter`/`provider-tavus` — esses dois SIM tinham sido corrigidos na auditoria 2026-08-02, mas `provider-recall` e `provider-stripe` ficaram de fora daquela rodada); corrigido replicando o padrão exato dos dois adapters já corretos (timer só é limpo depois do corpo consumido), com 3 testes de regressão novos (corpo travado após headers, timeout ainda dispara). (2) `chunkContent`/`embedChunks`/`embedQuery` (`knowledge.ts`, caminho pago de ingestão de conhecimento) não tinham NENHUM teste — 20 testes novos cobrindo os limites exatos do merge/split de parágrafos (inclusive o limite `<=` do teto de 1200 chars), determinismo do modo fake, e o provider real via fetch injetado (lote de 32, soma de tokens, erro de dimensão). (3) `/conversas` e `/conversas/[id]` eram as únicas rotas do app sem NENHUM `loading.tsx` em toda a árvore de ancestrais (achado original também citava `/agentes/[id]/testar`, mas a verificação adversarial provou empiricamente, com um servidor de dev real, que essa rota já herda o skeleton de `agentes/loading.tsx` via cascata de Suspense boundary do App Router — refutado corretamente); corrigido com um único `conversas/loading.tsx` (a mesma cascata cobre `/conversas/[id]` automaticamente, sem precisar de um segundo arquivo). 4 achados P2/P3 identificados mas não corrigidos nesta onda (backlog: `ensureTenantProvisioned` sem teste, `UUID_V7_PATTERN` duplicado em 13+ arquivos, botão "Revogar" convite sem loading/engolindo erro, bolhas de chat sem `overflow-wrap`) | disciplina do `/loop`: nunca declarar nada pronto sem rodar o teste que prova — o bug P0 foi confirmado por 3 votos adversariais independentes com reprodução matemática, e eu mesmo revalidei contra o Postgres real (não só a simulação dos verificadores) antes de escrever a correção; os testes de regressão de timeout usam a mesma técnica dos verificadores (corpo que só resolve/rejeita quando o AbortSignal dispara) pra provar que o fix funciona, não só que não quebrou nada | baixa-média — migration 0033 escrita mas NÃO aplicada (aguardando autorização explícita do Fernando, mesmo padrão de sempre); as correções de TS/testes são aditivas e não mudam contrato público; 634 Node (+34 novos) + 26 Python + lint + typecheck + 9 validadores + `next build` verdes |
| D-V2-110 | 2026-08-11 | Fecha o backlog P2/P3 declarado em D-V2-109 (achados menores da onda 2 do `/loop` autônomo, não corrigidos naquela rodada). (1) **Botão "Revogar" convite** (`team-section.tsx`) sem estado de carregamento e engolindo erro de RPC em silêncio — inconsistente com `InviteForm`/`MemberRemoveButton` no mesmo arquivo. `revokeInvite` (`team.ts`) mudou de `<form action>` retornando `void` para receber `inviteId` direto e devolver `{error}`, mesmo padrão de `removeMember`; novo componente cliente `InviteRevokeButton` (sem confirmação dupla — revogar um convite pendente é reversível, stakes menores que remover um membro ativo). (2) **Bolhas de chat/transcrição** (`preview-chat.tsx`, `conversas/[id]/page.tsx`) sem `overflow-wrap` — uma string longa sem espaço (URL, token colado) estourava a largura do card; `overflowWrap: "anywhere"` adicionado nas duas. (3) **`ensureTenantProvisioned`** (único caminho de provisionamento self-serve, chamado a cada load do dashboard pra tenant novo) sem nenhum teste — 5 testes novos cobrindo os uuids v7 gerados, o slug derivado, o fallback "Nova conta" sem e-mail, e o erro explícito quando a RPC falha. (4) **`UUID_V7_PATTERN` deduplicado**: o mesmo regex literal estava copiado em 14 lugares (13 arquivos declarando localmente sob nomes diferentes — `TENANT_ID_PATTERN`, `UUIDV7_PATTERN` — + 1 uso inline com `/i` em `resources.ts`) em vez de importado de `@axtro/domain`, que já exporta `UUID_V7_PATTERN` (e `isUuidV7`) como fonte única. 10 pacotes (`tool-adapters/{calendar,crm-lite,proposal}`, `handoff`, `provider-fakes`, `evaluation`, `provider-contracts`, `role-packs/sales-closer`, `knowledge-engine`, `ui`) ganharam `@axtro/domain` como dependência real (package.json + referência de projeto TS no tsconfig.json — nenhum deles dependia antes; `@axtro/domain` só depende de `@axtro/contracts-ts`, sem risco de ciclo) — cada arquivo importa `UUID_V7_PATTERN` (com alias pro nome local já usado em todo o resto do arquivo, zero risco de esquecer um call site). `packages/workflows/src/index.ts` tinha uma cópia morta nunca referenciada (achado P3 à parte) — removida, não substituída por import (já tinha `@axtro/domain` como dependência, mas o símbolo nunca era usado). `resources.ts` trocou o regex inline por `isUuidV7()`, já importado de `@axtro/domain` no mesmo arquivo. 2 testes-âncora (`provider-contracts.test.mjs`, `deterministic-provider-fakes.test.mjs`) que afirmavam a lista EXATA de dependências desses 2 pacotes precisaram de atualização — a intenção real deles é "nenhum SDK de provider" (openai/tavus/recall/etc.), não "nenhuma dependência interna nova", então adicionar `@axtro/domain` não viola o que protegem | fechar o backlog identificado na própria auditoria antes de considerá-lo resolvido, mesma disciplina de D-V2-102/103/107 (nunca deixar achado sem ação declarada); a dedup foi avaliada como risco real (10 pacotes ganhando uma dependência nova) mas de baixo risco genuíno (`@axtro/domain` é puramente interno, zero SDK externo, zero risco de ciclo) — optei por fazer a correção de verdade em vez de um workaround mais barato (um teste de "drift" comparando os literais), porque o ganho é maior (nenhuma cópia sobra pra divergir) e o risco medido era baixo | baixa — UI: aditivo/comportamento equivalente; testes: aditivos; dedup: mecânica, sem mudança de comportamento (mesmo regex, mesma checagem), validada por rebuild completo (`pnpm install` + `tsc --build --force`) e pipeline inteira; 639 Node (+5 novos) + 26 Python + lint + typecheck + 9 validadores + `next build` verdes |
| D-V2-111 | 2026-08-11 | Onda 3 do `/loop` — auto-revisão adversarial (13 agentes) do código NOVO desta sessão (transcripts, RAG, cost-alerts, os fixes de timeout/billing/custo), ainda não visto por ninguém além de quem escreveu. **1 achado P1 confirmado por unanimidade (9/9 votos, achado independentemente por 3 dimensões diferentes)**: `processMeetingTranscript` (webhook do Recall.ai, D-V2-106) montava o array `turns` sem nenhum truncamento por turno nem teto de quantidade — diferente do caminho irmão do Tavus (`tavus-webhook.ts`, que já tinha `MAX_TURN_CHARS=4000`/`MAX_TURNS=500`). Uma reunião real (bot fica em call até 2400s/40min via `automatic_leave`) com um bloco de fala contínua >8000 chars OU mais de 1000 blocos de diarização fazia `app.validate_transcript_turns` (0029) rejeitar o array INTEIRO — a transcrição da reunião inteira era perdida em silêncio (erro só logado, nunca um salvamento parcial), e o webhook ainda respondia `{ok:true, handled:true}` pra Recall independente do resultado real, mascarando a falha de qualquer observabilidade futura. Corrigido: mesmo teto do Tavus (4000/500) aplicado no `route.ts` do Recall — trunca cada turno E corta o array ANTES do teto do banco, com folga; parcial é sempre melhor que perder tudo. `handled` na resposta agora reflete o resultado real de `processMeetingTranscript` (que passou a devolver `boolean`), não mais um `true` incondicional. **2 achados P3 adicionais fechados** (fora do escopo do achado grave, mas baratos e no mesmo raio): `provider-tavus` tinha ZERO teste de timeout (só `provider-recall`/`provider-stripe` ganharam testes de regressão em D-V2-109) — 2 testes novos (timeout básico + "corpo travado depois dos headers"), confirmando que o padrão correto (herdado da auditoria 2026-08-02) realmente funciona e ficará protegido contra regressão futura. `DAILY_VIDEO_CONVERSATION_CAP` estava duplicado como literal `20` em `leads/video-session/route.ts` em vez de importado de `video-cap.ts` (que já o exporta) — trocado por import, mesmo espírito da dedup de `UUID_V7_PATTERN` em D-V2-110. **Achados P3 confirmados como NÃO-defeito** (documentados pela própria auditoria, não exigem ação): o fix de timeout de D-V2-109 cobre de fato TODOS os métodos expostos de `provider-recall`/`provider-stripe` (nenhum vazamento de timer, processo sempre sai limpo); `provider-openrouter` já tinha a proteção correta desde 2026-08-02 e nunca regrediu | mesma disciplina do resto da sessão: código que EU MESMO escrevi não fica isento de revisão adversarial só por ter sido escrito com cuidado — os 3 achados anteriores (P0 em D-V2-109, P1×3, P2/P3×4 em D-V2-109/110) já provaram que revisão externa encontra coisa que a própria implementação não vê; rodar mais uma auditoria focada no que mudou nesta sessão (em vez de repetir dimensões já cobertas) foi o uso mais inteligente de mais uma rodada | baixa — mudança aditiva e localizada (só o webhook do Recall + 2 arquivos de dedup/teste); nenhuma migration nova, nenhuma mudança de contrato de API; 641 Node (+2 novos: timeout do tavus) + 26 Python + lint + typecheck + 9 validadores + `next build` verdes |
| D-V2-112 | 2026-08-12 | Onda 4 do `/loop` — auditoria multi-dimensão (edge cases de auth, cobertura de rate-limit, precisão de docs, conteúdo de e-mail, varredura de TODO/FIXME), 8 agentes, 13 achados totais, 1 grave confirmado por unanimidade (3/3 verificadores). **P1 confirmado**: cadastro (`signUp`, `auth.ts:53`) sem nenhum rate-limit/CAPTCHA de aplicação, combinado com dois fatos já existentes no produto — provisionamento self-serve 100% automático (tenant → agente → ativação dispara `provisionAgentVideoIfMissing` criando uma persona REAL na Tavus sem revisão humana) e `BILLING_TRIAL_LIMIT_ENABLED` desligado por padrão (só o teto DIÁRIO de 20 conversas/tenant segurava gasto, sem teto mensal algum) — permitia multiplicar custo real em Tavus/OpenRouter linearmente com N contas fake, sem exigir cartão em nenhum ponto da cadeia. Cadeia verificada linha a linha pelos 3 verificadores contra o código-fonte real (não só a descrição do achado): `auth.ts` → `resources.ts` (`createAgent`/`setAgentStatus`) → `agent-video.ts` (`provisionAgentVideoIfMissing`) → `video-conversation.ts`/`video-cap.ts`. **Corrigido com 2 mudanças independentes, cada uma fechando metade da cadeia**: (1) `apps/portal/src/lib/rate-limit.ts` (novo) extrai o padrão de sliding-window em memória já usado no endpoint de chat do brain (`api/brain/[agentId]/chat/completions/route.ts`) pra uma superfície pré-autenticação, onde os tetos por-tenant do banco (0015) não se aplicam (não existe tenant ainda). Aplicado em `signUp` (5/hora por IP) e, por extensão barata e recomendada pelo mesmo achado (dimensão `rate-limiting-coverage` também citou `signIn` como P2 de credential-stuffing), em `signIn` (10/min por IP). Defesa em profundidade, não a barreira única — um atacante trocando de IP ainda contorna; documentado como tal no código. (2) `isTrialLimitEnabled()` (`billing/plans.ts`) invertido de opt-in (`BILLING_TRIAL_LIMIT_ENABLED=true` pra ativar, desligado por padrão desde D-V2-101) pra opt-out (ativo por padrão, só desliga com a env var explicitamente `"false"`) — fecha a metade mais cara da cadeia: mesmo se um atacante contornar o rate-limit de signup, cada tenant fake agora tem teto mensal de 5 conversas (~US$17,50 no pior caso, não mais ilimitado). Reversível a qualquer momento setando a env var no Railway, sem deploy de código. Teste pré-existente (`video-cap.test.mjs`, status não-ativo cai pro branch sem-assinatura) isolado do novo default via `BILLING_TRIAL_LIMIT_ENABLED=false` explícito — o que ele prova (roteamento por status) é ortogonal ao valor do teto de trial. **12 achados menores (P2/P3) documentados como backlog, não corrigidos nesta onda** (nenhum bloqueia produção): enumeração de conta via `/signup` e via `email_not_confirmed` no `/login`; race sem lock no `provision_self_serve_tenant` (SELECT sem FOR UPDATE — erro não tratado na perda, não corrupção); `updatePassword` aceita qualquer sessão viva sem exigir senha atual nem AAL de recovery; `/api/leads/video-session` sem rate-limit próprio (mitigado a jusante pelo teto diário); `docs/NEEDS_CONNECTION.md` e `docs/PRICING_UNIT_ECONOMICS.md` com texto desatualizado (billing descrito como pendente, já está em produção desde D-V2-101/107); contagem de specs Playwright desatualizada; e-mail de alerta de custo sem link/CTA pro dashboard (o mais acionável dos 4 templates, o único sem botão); 3 dos 4 templates de e-mail não explicam por que o destinatário recebeu. `todo-fixme-sweep` (5ª dimensão) voltou vazio — nenhum TODO/FIXME órfão real no código novo | pedido explícito do Fernando ("segue em looping contínuo onda a onda... com total autonomia para decisões recomendadas"); a inversão do default de trial é uma decisão de segurança recomendada pela própria auditoria, não uma mudança de comportamento de billing arbitrária — só afeta contas SEM assinatura (nenhum cliente pagante é tocado, `ACTIVE_STATUSES` sempre bypassa o teto de trial) e é a correção mais direta pro P0 real do achado (gasto ilimitado sem cartão); rate-limit por IP segue o mesmo padrão já aceito e documentado no endpoint de chat do brain (processo único no Railway, melhor-esforço se houver réplicas) | baixa — nenhuma migration nova, nenhuma mudança de contrato de API pública, nenhum cliente pagante afetado (trial cap só toca contas sem assinatura ativa); mudança de default é reversível via env var sem deploy; 646 Node (+5 novos) + 26 Python + lint + typecheck (raiz + portal) + 9 validadores + `next build` verdes |
| D-V2-113 | 2026-08-12 | Fecha 4 dos 12 achados menores declarados como backlog em D-V2-112 (os mecânicos/baratos; os 8 restantes ficam explicitamente adiados, ver racional). (1) **Enumeração de conta via `/signup`**: `signUp` mostrava mensagem distinta ("já existe conta com esse e-mail") quando o e-mail já tinha conta, ao contrário de `requestPasswordReset` no mesmo arquivo, que já unifica a resposta pra nunca revelar existência. Corrigido no mesmo princípio: `user_already_exists` agora trata como sucesso (redireciona pra `/login?confirm=1`, sem reenviar nada) em vez de expor a distinção; branch morto removido de `authErrorMessage` (só era alcançável por esse caminho). (2) **Rate-limit em `/api/leads/video-session`**: única rota autenticada só por segredo estático (`RAISSA_TOOLS_SECRET`) sem nenhum controle de volume — se o segredo vazar, nada contém o abuso até a rotação; 30/min por IP usando o mesmo `rate-limit.ts` de D-V2-112. (3) **3 docs desatualizados**: `NEEDS_CONNECTION.md` descrevia billing como precisando de 2 migrations + `stripe_setup.mjs` ainda por rodar — ambos já concluídos (migrations 0025-0033 aplicadas, catálogo Stripe teste real desde 2026-08-10); reescrito pra refletir que só falta configurar env vars no Railway. `PRICING_UNIT_ECONOMICS.md` tinha "hoje o produto não tem billing" como próximo passo — corrigido pra declarar o billing implementado, com pointer pro gate real (env vars). Contagem de specs Playwright corrigida de 14 pra 15 (confirmado via grep no spec real) em `NEEDS_CONNECTION.md`. (4) **CTA no e-mail de alerta de custo**: `sendCostCapAlertEmail` era o único dos 4 templates sem link, apesar de ser o mais acionável (aviso de bloqueio); adicionado botão "Ver uso no painel" apontando pra `/dashboard` (mesmo padrão visual dos outros 3 templates). **8 achados explicitamente adiados, com razão**: enumeração via `email_not_confirmed` no `/login` (P3) — fechar por completo tira um sinal genuinamente útil pra usuário legítimo não-confirmado, é uma troca de UX que merece decisão própria, não uma correção mecânica; race sem lock em `provision_self_serve_tenant` (P2) — exige migration nova (`FOR UPDATE` ou lock consultivo), bloqueada pelo gate de autorização explícita de migration, mesmo padrão de sempre; `updatePassword` aceitando qualquer sessão viva (P2) — é o ÚNICO caminho de troca de senha do produto; endurecer sem cuidado (checar AAL/AMR da sessão de recovery) arrisca travar usuários legítimos no meio do fluxo de recuperação — split pra uma rodada dedicada só a isso; 3 templates de e-mail sem "por que você recebeu isto" (P3) — copy de baixo valor, mesmo raciocínio de "não inventar trabalho de baixo valor" já aplicado antes nesta sessão. Nenhum teste novo necessário: `authErrorMessage`/`signUp`/`signIn` não são testados diretamente nesta base (mesma limitação já documentada em D-V2-112 — `next/headers`/`next/navigation` não resolvem fora do runtime do Next sob Node puro); `route.ts` do leads/video-session também segue a convenção existente de não ter teste direto (só o núcleo `handleVideoSessionRequest`, inalterado, é testado); `sendCostCapAlertEmail.test` não afirma conteúdo HTML, só `sent`/`reason` | continuação do `/loop` (pedido do Fernando: "continua o loop infinito... com autonomia... fazendo o que for mais recomendavel e inteligente"); fechar backlog reconhecido antes de abrir uma onda nova é a mesma disciplina de D-V2-109→110; a decisão de QUAIS itens fechar agora vs adiar seguiu peso de risco: mecânico e sem ambiguidade = fechar; qualquer item que mexe em fluxo de autenticação de forma que poderia quebrar um caminho legítimo (recovery de senha) ou que precisa de aprovação externa (migration) = adiar com razão explícita, nunca corrigido às pressas | baixa — nenhuma migration, nenhuma mudança de contrato de API pública; a mudança de `/signup` é a única com efeito comportamental real (visitante que já tem conta não vê mais o erro distinto), consistente com o padrão já aceito em `requestPasswordReset`; 646 Node (sem novos — nenhuma lógica nova testável isoladamente que já não estivesse coberta) + 26 Python + lint + typecheck (raiz + portal) + 9 validadores + `next build` verdes |
| D-V2-114 | 2026-08-12 | Onda 5 do `/loop` — auditoria multi-dimensão em ângulos NUNCA cobertos nesta sessão (profundidade LGPD/privacidade, disaster recovery/backup, completude de monitoramento/alertas, isolamento multi-tenant, concorrência/idempotência), 23 agentes, 17 achados totais, **6 graves confirmados por unanimidade (3/3 verificadores cada)** — a onda de maior densidade de achados reais desde a auditoria inicial. **P1 #1 (LGPD)**: `conversation_transcripts` (0029) não tinha NENHUMA RPC de exclusão apesar de `/privacidade` prometer "exclusão dos seus dados — incluindo o histórico de conversas — a qualquer momento" — cumprir um pedido real exigiria um operador escrever um DELETE cru em produção, sem trilha de auditoria. **P1 #2 (LGPD)**: a mesma tabela também não tinha NENHUM mecanismo de retenção/expurgo — texto integral de conversa (até 1000 turnos × 8000 chars) fica pra sempre, contradizendo `ADR-016` ("shortest configured period"); o v1 legado tinha um job diário de purga que nunca foi carregado pro v2. Corrigidos juntos na migration `0034`: `portal_delete_conversation_transcript` (autenticada, tenant_admin, wired numa UI nova de exclusão em `/conversas`), `portal_delete_conversation_transcript_service` (o caminho real que o Fernando usaria pra cumprir um pedido chegado por e-mail, já que ele não tem sessão de tenant_admin de cada tenant) e `portal_purge_old_conversation_transcripts_service` (parametrizada por dias, piso de 30 — **deliberadamente sem escolher o período de retenção real nem automação por cron**, que são decisão de produto/jurídico pendente do Fernando, não algo pra inventar unilateralmente). **P1 #3 (disaster recovery)**: zero runbook de backup/restore pro Supabase de produção em qualquer lugar do repo — o único "rollback" documentado cobre deploy/config, nunca perda de dados; `ADR-016` já referenciava "recovery windows documentados" que nunca existiram. Corrigido com `docs/operations/DISASTER_RECOVERY.md` novo (playbook de incidente de dados + o que está confirmado — plano Supabase Pro, verificado ao vivo via MCP `get_organization` — vs. o que precisa da ação do Fernando — status de PITR, teste de restore real), linkado de `RISK_REGISTER.md` (R18), `INCIDENT_RESPONSE.md` (novo caso) e `ADR-016`. **P1 #4 (monitoramento)**: nenhum alerta de taxa de erro existia — os 76 call sites de `trackError` convergiam só pra `console.error`; se Tavus/OpenRouter/Stripe/Recall começassem a falhar 100%, ninguém saberia até reclamação de cliente. Corrigido estendendo `telemetry.ts` (não `email.ts`, que já importa `telemetry.ts` — evita ciclo) com janela deslizante de 5min/teto de 10 falhas/cooldown de 30min, e-mail fire-and-forget ao operador via fetch direto ao Resend. **P1 #5 (integridade de dados + custo)**: `portal_ingest_knowledge` repetia o mesmo padrão check-then-act sem lock do achado já conhecido (não corrigido) em `provision_self_serve_tenant` — duas re-ingestões concorrentes da mesma fonte criavam 2 `knowledge_versions` "ativas", quebrando o invariante "busca nunca mistura versões" e cobrando embeddings em dobro. Corrigido na migration `0035` com `SELECT...FOR UPDATE`, mesmo padrão já usado em `tenant_invites` (0004). **P1 #6 (dinheiro real duplicado)**: `joinExternalMeeting` chamava `Recall.createBot` + `Tavus.createConversation` — duas APIs PAGAS reais — sem nenhuma proteção de idempotência; duplo-clique/duas abas criava dois bots + duas conversas cobradas pra uma única intenção, e o único registro em `meeting_bot_sessions` acontece DEPOIS que as duas chamadas já tiveram sucesso. Corrigido com dedup em memória (`rate-limit.ts`, mesmo primitivo de D-V2-112/113) colocado logo ANTES da chamada aos providers pagos — não no topo da função, pra checagens gratuitas que falham por razão não-relacionada não consumirem o slot. **11 achados menores**: 2 fechados nesta mesma onda (e-mail duplicado "Agente ativado" quando `setAgentStatus` ignorava o campo `changed` já calculado pela RPC desde 0014 — `resources.ts` agora checa; `portal_create_knowledge_source` sem UNIQUE constraint, causando fonte duplicada em duplo-clique — migration `0036`, verificado ao vivo que não há duplicata hoje antes de adicionar o constraint). 9 deliberadamente adiados com razão (leads roteados por `/api/leads/video-session` ficam fora do sistema de transcript — feature-sized, precisa decisão de escopo pra dado de não-usuário; sem RPC de deleção de `auth.users` nem de exportação/portabilidade — features novas, nenhum pedido real já chegou; criação de sala Tavus não-atômica no webhook do Recall e falta de job de reconciliação de webhook — precisam de infra de saga/cron que não existe no repo; `portal_record_meeting_bot_session` aceita bot_id sem checar proveniência e `portal_claim_cost_alert_service` sem checagem de tenant — confirmados NÃO exploráveis hoje pelos próprios verificadores; `provisionAgentVideoIfMissing` race — mesma classe dos outros 2 fixes de concorrência, mas P3 e non-corrupting). **Auto-revisão do código novo desta onda** (mesma disciplina de D-V2-111, rodada antes de documentar/commitar): 0 achados graves confirmados (todo o código novo se sustentou), 5 achados menores — todos corrigidos: ordem `refresh()`/`push()` no botão de exclusão de transcript (podia servir uma lista em cache); guard de dedup do meeting-bot consumindo o slot em falhas não-relacionadas ao invés de só logo antes da chamada paga (movido) + chave sem `joinAtIso` (fundia pedido imediato com agendamento — corrigido); Map de telemetria sem teto de tamanho (defesa contra call site futuro de má qualidade — teto de 1000 eventos distintos); e o MESMO padrão de race de `portal_ingest_knowledge` também presente em `portal_set_agent_status` (0014) — corrigido na migration `0037`, mesmo `FOR UPDATE` | pedido explícito do Fernando ("continua o loop infinito... com autonomia... o que for mais recomendavel e inteligente"), Ultracode ativado nesta sessão (otimizar pela resposta mais exaustiva e correta, verificar adversarialmente achados E fixes); os 2 achados LGPD são o gap de MAIOR risco legal/reputacional já encontrado nesta sessão — uma promessa pública já em produção sem NENHUM backing técnico — por isso priorizados antes dos outros P1s apesar de todos terem a mesma severidade nominal; a auto-revisão pós-fix (não só pós-achado) é a mesma disciplina que já provou valor 3 vezes nesta sessão (D-V2-109→110→111) | baixa-média — migrations 0034/0035/0036/0037 **aplicadas em produção 2026-08-12** via MCP `apply_migration`, autorizadas explicitamente pelo Fernando (a de 0036 foi verificada contra zero duplicatas reais em produção antes de aplicar o UNIQUE constraint); confirmadas via `execute_sql` (as 3 RPCs de 0034 existem; `FOR UPDATE` presente em `portal_ingest_knowledge`/`portal_set_agent_status`; constraint presente em `pg_constraint`) e `get_advisors` (as 2 RPCs service-role de 0034 corretamente NÃO aparecem como executáveis por `authenticated`, nenhum achado de segurança novo); todo o resto é aditivo ou correção cirúrgica de app code; nenhuma mudança de contrato de API pública além de mensagens de erro novas; 650 Node (+4 novos: alerta de taxa de erro) + 26 Python + lint + typecheck (raiz + portal) + 9 validadores + `next build` verdes |
| D-V2-115 | 2026-08-12 | Onda 6 do `/loop` — auditoria multi-dimensão (guardrails de vendas da IA, performance/escala, dependências de terceiros, i18n/locale, ciclo de vida de sessão), 26 agentes, 13 achados totais, **7 graves confirmados por unanimidade**. **P0, NÃO corrigido, flagged pro Fernando**: a persona Tavus criada no fluxo real de ativação (`agent-video.ts`) nunca é ligada ao endpoint `/api/brain/.../chat/completions` — o único caminho com RAG por turno e degradação Art.14 testados (D-V2-108). `CreatePersonaRequest` (provider-tavus) não tem campo `base_url`; toda videochamada real roda no LLM default da Tavus, com só um resumo estático de conhecimento no início da call. Verificadores confirmaram que isto já era conhecido/documentado (D-V2-083, D-V2-108, `AUTONOMOUS_EXECUTION_STATE.md`) como decisão deliberadamente represada aguardando aprovação explícita do Fernando pra trocar o LLM de uma persona ao vivo — não é regressão silenciosa, mas o achado reforça o tamanho real do gap: RAG e a doutrina Art.14 testados no código não protegem NENHUMA call de vídeo real hoje. **4 P1 corrigidos, mecânicos**: (1) o MESMO bug de perda silenciosa de transcrição longa que D-V2-111 corrigiu no Recall hoje mesmo estava intacto no webhook do Tavus — a superfície de vendas PRINCIPAL — truncando em vez de descartar agora. (2) Auto-provisão de vídeo hardcodava idioma português, ignorando `default_language` do tenant — `tenantLanguageToBrainLanguage()` novo mapeia a config real. (3) `checkBudget` rodava um scan sem índice de TODAS as linhas de `cost_events` do tenant EM TODO TURNO de vídeo — migration `0038` (RPC de agregação SQL `portal_ai_tokens_today_service` + índice em `(tenant_id, unit_type, occurred_at)`) resolve, mesmo padrão que `portal_ai_tokens_today()` já usava pro chat autenticado. (4) Membro removido (`portal_remove_member`) não invalida a sessão Auth ativa — investigado o mecanismo de revogação disponível na Admin API do Supabase e concluído que não há um "invalidar sessão deste user_id" limpo (só `ban_duration`, mais pesado e com risco de ban/unban malfeito); **NÃO corrigido, flagged pro Fernando** junto com a pergunta de produto que o acompanha (usuário removido deveria conseguir se auto-provisionar um tenant novo depois?). **2 mitigações parciais** (a correção completa exige integração/decisão maior, fora de escopo desta onda): guardrails anti-promessa (alçada de desconto zero, "garantido" proibido) eram só texto de prompt sem NENHUMA checagem pós-geração — `detectGuardrailRisk()` novo em `chat-completion-core.ts` detecta (não bloqueia) padrões de risco e telemetra, sem fingir ter resolvido moderação de conteúdo; doutrina de "HOSTILIDADE OU ABUSO" nova no prompt de vídeo (EN/PT) — não existia NENHUMA orientação pra lead hostil/ofensivo, e não há tool real de encerrar/transferir chamada (`@axtro/handoff` construído mas nunca importado por `apps/portal`) — a doutrina é honesta sobre essa limitação ("você não tem como encerrar a chamada sozinha"). **2 achados menores fechados na mesma onda**: `rate-limit.ts` nunca removia chave do Map (leve vazamento de memória, achado próprio desta sessão). **Auto-revisão do código novo** (mesma disciplina de D-V2-111/114): **1 achado grave CONFIRMADO — um bug real que EU MESMO introduzi na correção do item anterior**: o fix de "nunca cresce sem limite" do `rate-limit.ts` usava `Map.clear()` ao estourar 5000 chaves — como a chave vem de `x-forwarded-for` (header controlado pelo cliente, sem validação de proxy confiável) e `/api/leads/video-session` roda o rate-limit ANTES da autenticação por bearer, um atacante podia floodar 5000+ chaves forjadas baratas só pra disparar o `clear()` e resetar de uma vez o rate-limit de QUALQUER OUTRO IP/usuário já bloqueado (signin/signup de terceiros) — uma correção de vazamento de memória virou um botão de reset de proteção de abuso pra todo mundo. Corrigido substituindo `clear()` total por eviction de UMA entrada por vez (a mais antiga por toque, LRU real via delete+reinsert, não só por ordem de criação) — limita a memória do mesmo jeito sem dar a um atacante um gatilho único e barato. 3 achados menores adicionais: item malformado no transcript da Tavus (role inválido ou não-objeto) ainda descartava o evento INTEIRO mesmo depois do fix de truncamento por contagem — mesma classe de bug, agora pula só o item ruim; timestamps de `/conversas`/dashboard podiam derrubar a página inteira se `default_timezone` fosse uma string IANA inválida (`Intl.DateTimeFormat` lança `RangeError`) — `format-date.ts` novo (`formatDateTime`/`formatLongDate`) captura e cai num fallback seguro | pedido explícito do Fernando ("continua o loop... com autonomia... o que for mais recomendavel e inteligente"), Ultracode ativado; a decisão de NÃO wire a persona Tavus ao cérebro custom nem construir o tool de handoff foi deliberada — ambas mudam o que uma IA fala/faz em chamadas de venda reais com leads reais, squarely o tipo de decisão que merece aprovação humana, não autonomia de implementação; a auto-revisão pegando um bug de segurança na PRÓPRIA correção desta mesma onda é a prova mais concreta até agora de por que essa disciplina existe — o fix documentado como "defesa em profundidade... aceitável" no comentário original era, na prática, um bypass real, e só uma segunda leitura adversarial achou isso | média — migration `0038` escrita e **aplicada em produção 2026-08-12** (autorizada explicitamente pelo Fernando, verificada via `execute_sql` + `get_advisors`: índice presente, RPC retorna `bigint` correto, não exposta a `authenticated`); o fix de segurança do rate-limit é comportamental (muda como o teto se recupera sob pressão, sem mudar a API pública `isRateLimited(key, windowMs, maxRequests)`); guardrail detection e doutrina de hostilidade são aditivos, sem mudar contrato de nenhuma função; 663 Node (+14 novos: detectGuardrailRisk, LRU do rate-limit, timezone crash-safety, items malformados do Tavus) + 26 Python + lint + typecheck (raiz + portal) + 9 validadores + `next build` verdes |
| D-V2-116 | 2026-08-12 | Onda 7 do `/loop` — auditoria multi-dimensão em 6 ângulos NUNCA cobertos nesta sessão (rate-limit/429 de provider, idempotência de chamada paga, grants RLS além de "tem RLS", cobertura loading/error de TODAS as rotas, expiração de sessão em Server Action, profundidade de supply-chain), 24 agentes, 10 achados totais, **6 P1 confirmados por unanimidade (0 refutados em nenhum)**. Todos os 6 corrigidos: (1) nenhum adapter de provider pago tratava HTTP 429 como transitório — `fetchWithRateLimitRetry` novo em `provider-openrouter` retenta UMA vez (respeitando `retry-after`, capado em 2s) antes de desistir, fechando o caso real e verificado (`embedChunks` abortava o lote inteiro de ingestão de conhecimento na primeira resposta 429 do OpenRouter, sem salvar nada) — a narrativa de que isso também quebrava chamadas de vídeo AO VIVO foi contestada por 1 dos 3 verificadores (o endpoint do cérebro custom continua inatingível por vídeo real, achado já conhecido de D-V2-115) mas o núcleo do achado (zero resiliência a 429) sobreviveu por maioria. (2) `reportConversationOverageIfNeeded` (Stripe) não reaproveitava sua PRÓPRIA idempotencyKey pra retentar — uma unidade de overage real (dinheiro) descartada em silêncio, para sempre, num 429/5xx transitório; ganhou uma retentativa. (3) `startVideoConversation`/`startPresentationConversation` (Tavus) não tinham NENHUM dedup contra duplo-clique/duas abas — mesma classe de bug já corrigida em `joinExternalMeeting` (D-V2-114), só esquecida no arquivo irmão; ganharam `isRateLimited` antes da chamada paga. (4) `(app)/layout.tsx` faz 2 round-trips reais ao Supabase (getUser + fetchTenantOverview) sem NENHUM `loading.tsx` capaz de cobri-lo — os 5 `loading.tsx` já existentes só envolvem o CONTEÚDO abaixo do layout, nunca o próprio layout do mesmo segmento (confirmado ao vivo por 3 verificadores independentes, reproduzindo TTFB com/sem o fallback num build de produção limpo, e contra a doc real do Next 16.3.0 instalado — não conhecimento genérico desatualizado); um `loading.tsx` GENUINAMENTE na raiz (`apps/portal/src/app/loading.tsx`, acima de `(app)/`) resolve, cobrindo de brinde `/` e `/nova-senha`. (5) sessão morta no meio de uma Server Action (chat de teste, forms de `/configuracoes`/`/conhecimento`) não caía em `{error}` tratado — o middleware intercepta ANTES da action rodar com um redirect cru que o protocolo de fetch de Server Actions do Next não entende, lançando `UnrecognizedActionError`/erro genérico sem tratamento, apagando o rascunho do usuário; `preview-chat.tsx` ganhou try/catch com mensagem clara, `(app)/error.tsx` parou de prometer "nada foi perdido" (falso nesse cenário) e ganhou link direto pro `/login`. (6) `scripts/dependency_scan.py` (gate obrigatório de CVE alto/crítico em todo release/CI) sempre passa porque `security/dependency-advisories.json` está vazio desde M0 e nada nunca o populou — confirmado com uma CVE HIGH real e não corrigida escapando (`nanoid@3.3.16`, GHSA-2V37-7H3G-55P8, via `next`&gt;`postcss`); corrigido com `pnpm-workspace.yaml` `overrides.nanoid: '&gt;=3.3.17 &lt;4'` (faixa fechada em `&lt;4` pra não pular pro nanoid 6.x ESM-only — o primeiro override sem teto resolveu direto pra 6.0.1 e foi corrigido antes de commitar) + snapshot populado com a entrada real. **3 achados P2 triados e também corrigidos** (baratos, mesma onda): `minimumReleaseAgeExclude` configurado sem NENHUM `minimumReleaseAge` ativo (allowlist de uma política que não existe — `pnpm install` em qualquer clone/CI aplicava delay zero pra qualquer pacote) — `minimumReleaseAge: 1440` (24h) ativado; 6 das 7 tabelas RLS-sem-policy tinham GRANT ALL padrão do Supabase pra `anon`/`authenticated` nunca revogado (seguro hoje — RLS+zero-policy já nega tudo — mas defesa-em-profundidade contra uma policy futura mal escrita valer imediato pra todo comando) — migration `0039` escrita (mesmo padrão de `revoke all on table` já usado em 0002/0006), **NÃO aplicada, aguardando autorização explícita do Fernando**; landing (`/`) e `/nova-senha` sem `loading.tsx` ancestral (fechado de graça pelo achado 4). **1 achado P3 deliberadamente adiado**: inconsistência visual cosmética (erro real dentro de `(app)/layout.tsx` cai no error.tsx PÚBLICO em vez do error.tsx do workspace, mesma limitação estrutural de aninhamento do achado 4, agora pro lado do error.tsx) — não bloqueia recuperação, só troca o card visual, mesma classe de esforço que exigiria mover a leitura de sessão pra fora do layout. **Auto-revisão do código novo** (mesma disciplina de D-V2-111/114/115): **3 achados P1 CONFIRMADOS por unanimidade (0 refutados)** — de novo, bugs reais introduzidos pela PRÓPRIA correção desta onda, não achados relitigados. (a) o dedup novo do Tavus usava só `agentId+modo` na chave, com o comentário afirmando que "agentId já é único por tenant" — FALSO: `agents` tem PRIMARY KEY composta `(tenant_id, id)`, sem unicidade global, e `portal_create_agent` aceita um `p_id` escolhido pelo chamador sem checar outros tenants; um tenant atacante self-serve podia criar um agente-isca com o `id` de um agente de OUTRO tenant e envenenar o slot de dedup da vítima antes de qualquer chamada real à Tavus — negação de serviço cross-tenant contra o recurso pago principal de um cliente pagante, mitigada hoje só pelo fato de nenhuma rota pública vazar `agentId` completo (mitigação frágil, não housing estrutural) — corrigido incluindo `tenant_id` na chave. (b) o retry novo do `reportConversationOverageIfNeeded` era INCONDICIONAL (qualquer erro, não só transitório) e roda dentro de um `await` bloqueante no caminho crítico do usuário (não fire-and-forget como o `maybeAlertCostCap` irmão no mesmo arquivo) — dobrava o pior caso de latência de ~20s pra ~40,5s mesmo em erro PERMANENTE (ex.: customer_id malformado) que nunca teria sucesso numa 2ª tentativa — corrigido: só retenta em `provider_timeout`/`provider_unavailable` (mesma disciplina condicional já usada no retry do OpenRouter), falha rápido em erro permanente. (c) o `loading.tsx` novo da raiz envolve QUALQUER `page.tsx` sem `loading.tsx` próprio abaixo dele — inclusive `/rosto-agente`, rota que NINGUÉM tinha considerado: é a "câmera" que o bot do Recall.ai carrega dentro de reuniões externas REAIS, o que renderiza ali é capturado e mostrado ao vivo pra participantes humanos de verdade; o skeleton cinza genérico da raiz podia piscar na tela ANTES do palco full-bleed `#0b0b16` que `face-stage.tsx` já usa deliberadamente — um glitch visível numa call de vendas ao vivo — corrigido com um `loading.tsx` LOCAL em `rosto-agente/` que replica exatamente o full-bleed escuro, sem nenhum frame diferente. **2 achados menores adicionais corrigidos** (baratos, mesma auto-revisão): o guard de dedup do Tavus ficava ANTES de `createTavusVideoConversationPort({apiKey})`, cujo próprio construtor lança síncrono em chave curta-mas-não-vazia — uma falha de config pura já consumia o slot de dedup, violando o invariante que o próprio código documenta ("checado logo antes da chamada paga" pra não consumir o slot numa falha não-relacionada) — `port` movido pra antes do guard nos dois fluxos; comentário novo do `(app)/error.tsx` citava "chat" como exemplo de cenário que cai nesse boundary, mas o fix-irmão da MESMA onda (`preview-chat.tsx`) tornou isso falso no mesmo commit que introduziu o comentário — corrigido. **2 achados P3 deliberadamente adiados, com razão declarada**: backoff de 429 sem jitter (retries concorrentes podem cair em lockstep sob contenção sustentada — sem impacto de segurança/correção, só reduz um pouco a taxa de acerto do fix sob a exata condição de contenção sustentada que ele visa mitigar); timeout por tentativa (não pro ciclo de retry inteiro) dobra o teto teórico de latência de ~30s pra ~62s no pior caso patológico (dois 429 lentos consecutivos) — regressão de UX só, sem hard-timeout de plataforma (Railway) que cortaria a call antes, e o cenário exige duas respostas 429 individualmente lentas o suficiente pra se aproximarem do timeout de 30s cada, o que contradiz a própria natureza de um 429 (rejeição rápida de rate-limiter) | pedido explícito do Fernando ("continua o loop... com autonomia... o que for mais recomendavel e inteligente e seguro"), Ultracode ativado; a auto-revisão pegando 3 bugs reais (não 1) na PRÓPRIA correção desta mesma onda — incluindo uma segunda instância exata do padrão "retry incondicional sem checar tipo de erro" que D-V2-115 já tinha ensinado a evitar no rate-limiter, e um cross-tenant DoS via chave de dedup mal escopada — reforça que a disciplina de auto-revisão obrigatória não é formalidade: cada onda que introduz lógica de retry/dedup/cache tem introduzido pelo menos 1 bug real na correção em si, sem exceção até agora nesta sessão | média — migration `0039` escrita e **aplicada em produção 2026-08-12** (autorizada explicitamente pelo Fernando, verificada via `execute_sql`: zero grants remanescentes pra anon/authenticated nas 6 tabelas; `get_advisors`: nenhum achado novo, RPCs SECURITY DEFINER intactas); os retries condicionais/dedup são mudança de comportamento sob falha (não mudam contrato de nenhuma função pública); override de nanoid é bump de patch dentro do mesmo major (3.3.16→3.3.18, verificado com build+testes verdes); 668 Node (+5 novos: retry 429 do OpenRouter ×3, retry condicional do Stripe ×2) + 26 Python + lint + typecheck (raiz + portal) + 9 validadores + `next build` verdes, 2x (antes e depois dos fixes da auto-revisão) |
| D-V2-117 | 2026-08-13 | Onda 8 do `/loop` — auditoria multi-dimensão em 4 ângulos NUNCA cobertos nesta sessão (verificação de assinatura de webhook Tavus/Recall/Stripe, qualidade de retrieval do RAG além de "é alcançável", validação de config no boot do processo, deliverability/conteúdo de e-mail transacional), 16 agentes, 6 achados totais, **4 P1 confirmados por unanimidade (0 refutados em nenhum)**. A dimensão de verificação de webhook não achou NADA novo — Tavus, Recall e Stripe já estavam corretos; sinal de que essa superfície já está sólida. Os 4 P1 corrigidos: (1) `buildKnowledgeBlock` (o bloco de "FONTES AUTORIZADAS" injetado no prompt em TODO turno) cortava chunks de conhecimento em offset arbitrário sem sinal de truncamento, e descartava o match de ranking mais baixo quando o orçamento de 3800 chars acabava — um verificador achou até um precedente histórico real (D-V2-070) onde essa exata classe de corte já causou uma alucinação documentada. Reescrito pra distribuir o orçamento PROPORCIONALMENTE entre os matches e cortar no último espaço + "…". (2) Nenhuma das ~14 env vars críticas tinha validação de boot — ~9 call sites falhavam em silêncio (video-cap.ts, agent-video.ts, meeting-bot.ts, os 2 webhooks). `packages/config` existe mas nunca foi importado por `apps/portal` e trata as chaves reais como PROIBIDAS (desenhado pro kernel fake M0-M1). Corrigido com `trackError` nos ~9 call sites + `/api/health` expandido de 4 pra 13+ vars (`ok` continua sempre `true`, decisão já aceita). (3) Zero webhook da Resend pra bounce/complaint — convite/alerta de bloqueio podiam falhar pós-aceite sem o produto nunca saber. Novo `email-webhook.ts` (verificação Svix) + rota `/api/resend/webhook` — requer configuração manual pendente (dashboard Resend + `RESEND_WEBHOOK_SECRET` no Railway, documentado em NEEDS_CONNECTION.md). (4) `sendHtmlEmail` não retentava em erro transitório, e o alerta operacional (D-V2-114) dependia da MESMA Resend (SPOF) — corrigido com retry condicional + log no catch antes mudo; o SPOF em si fica deliberadamente documentado como risco residual (exige canal novo tipo Slack/PagerDuty). **1 achado P2 fora do escopo de código**: DMARC de `axtroai.com` em `p=none`, relatórios indo pra domínio de terceiro (`dmarcreports@lovable.dev`) — só o Fernando corrige isso no registrador do domínio. **Auto-revisão**: 8 achados, TODOS P2/P3, zero P0/P1 — primeira onda desta sessão sem bug grave na própria correção. Corrigidos mesmo assim: off-by-one no truncamento (reticências não reservadas no orçamento), citação vazia latente se o prefixo estourasse o orçamento, retry de e-mail sem idempotência (`Idempotency-Key` da Resend adicionado), 3 `trackError` novos disparando em modo demo legítimo (guard de `fakeProvidersEnabled()`), healthcheck com limiar errado pro `RAISSA_TOOLS_SECRET` e faltando `RECALL_WEBHOOK_TOKEN`/`SECRET` | pedido explícito do Fernando, Ultracode ativado; zero achado P0/P1 na auto-revisão sugere que a disciplina das 2 ondas anteriores (idempotência, guards de fake mode) foi internalizada — não garantia de que a próxima onda fica limpa | baixa — funções puras/aditivas, retries só mudam comportamento sob falha, rota nova fica inerte até `RESEND_WEBHOOK_SECRET` ser configurado, sem migration nesta onda; 680 Node (+12 novos) + 26 Python + lint + typecheck + 9 validadores + `next build` verdes, 2x |
| D-V2-118 | 2026-08-13 | O rollout M5-01 segue obrigatoriamente a janela executável de `docs/operations/M5_01_PRODUCTION_ROLLOUT.md`: maintenance e drain de writers pagos antes da 0040, expand v40, candidato sem tráfego e deliberadamente unready, contract v41, readiness completa, smokes e Stripe em test mode, workers manuais e backlogs limpos antes de schedules/canário. Depois da 0041 é proibido restaurar writer legado ou reabrir preclaim autenticado; rollback é repromoção v41-aware ou forward fix. | ADR-036 torna a ordem e a barreira `unknown` requisitos de integridade financeira; liveness não substitui readiness e o repositório não possui heartbeat de scheduler. | baixa no repositório — decisão documental e reversível até a 0041; execução remota, Stripe live e promoção continuam gates humanos e não foram realizadas |
| D-V2-119 | 2026-08-13 | Checkout Stripe passa a ter intent durável tenant-serializado e service-only; a idempotency key deriva do UUIDv7, o redirect só ocorre depois do vínculo persistido da sessão e o writer de assinatura impede que um segundo `sub_*` ativo ou evento tardio substitua a identidade corrente. | Bucket de um minuto e upsert last-write-wins não coordenam réplicas, não sobrevivem a resultado ambíguo e podem ocultar segunda assinatura ainda cobrando; ADR-036 estende a barreira durável ao efeito de conta Stripe. | média — contrato aditivo na 0040 e contract/revoke na 0041 ainda não hospedados; catálogo live, migration e cobrança continuam gates humanos |
| D-V2-120 | 2026-08-14 | Onda 6 (`W6`): consolidação de uma sessão Codex concorrente (17 commits nunca empurrados, incluindo M5-01/ADR-036/037 inteiro) via checkpoint verificado pela pipeline local antes do primeiro `git push` desta branch, seguida de auditoria multi-agente (6 dimensões: ai-budget, paid-effects, workers-http, checkout-billing, meetings-identity, transcripts-cross-cutting) sobre o código novo do M5-01, com verificação adversarial dos achados críticos/altos. **1 CRÍTICO corrigido**: `deriveMeetingSessionId` (`meetings/identity.ts`) exigia `parseUuidV7` no `commandId` vindo do navegador — mas `commandId` é gerado por `crypto.randomUUID()` (UUIDv4), então TODA reunião externa real lançava exceção antes de reservar o efeito pago; corrigido validando como UUID genérico (`isPaidEffectCommandId`, mesmo padrão de `paid-effects/index.ts`) e derivando timestamp+entropia do mesmo hash SHA-256 (determinístico, sem depender do relógio) em vez do timestamp embutido no v7 que um UUIDv4 não tem. **2 ALTOS corrigidos**: orçamento de tokens de embedding de ingestão (`MAX_CONTENT_CHARS=80_000` vs `maxInputTokens=20_000`) só era checado DEPOIS de já ter pago todos os lotes ao OpenRouter — `embedChunks` agora aborta entre lotes ao ultrapassar o teto reservado; e o port de embeddings do OpenRouter não pedia `usage:{include:true}`, then todo commit de ingestão caía no fallback `max_cost_usd` em vez do custo faturado real (mesmo padrão que o port de chat já usava). **3 MÉDIOS corrigidos**: outbox de billing marcava QUALQUER linha malformada como permanente na 1ª tentativa (sem o mesmo orçamento de retentativas de toda outra falha) — agora respeita `MAX_DELIVERY_ATTEMPTS` antes de ir pro dead letter; 4 pontos de retorno silencioso `{status:"pending"}` em `createDurableCheckout` (preflight/create/session-mismatch/bind) ganharam `logError`; regex de `TAVUS_CONVERSATION_ID` duplicada com tetos DIFERENTES entre o webhook (64 chars) e o adapter (65 chars) — unificada exportando `TAVUS_CONVERSATION_ID_PATTERN` de `provider-tavus`. **2 BAIXOS corrigidos**: leitura de resposta HTTP sem teto de tamanho no `call()` principal do Recall (só o download de transcript tinha; generalizado o helper existente); corpo do 429 nunca drenado no retry do OpenRouter (vazamento de socket sob rate-limit sustentado). **1 achado descartado após reverificação** ("aplicar retry de outbox tinha matemática errada no próprio achado — refutado por um verificador). **1 achado descartado por investigação própria**: "chamar `releaseAiUsageNotDispatched` em todo `not_acquired`" — os 2 call sites manuais citados como referência NÃO fazem isso (só liberam se `markAiUsageInFlight` lançar, não no retorno limpo `false`), porque `not_acquired` geralmente significa que outra chamada concorrente já é dona da reserva; liberar ali seria ativamente incorreto, não uma correção. **1 item conscientemente adiado**: o mesmo teto de tamanho de resposta HTTP não foi replicado pros adapters Tavus e OpenRouter (dimensionamento correto por endpoint precisa de mais cuidado — a resposta de embeddings sozinha pode passar de 1MB legitimamente com 64 inputs × 1536 dimensões). Dimensão `workers-http` do audit workflow travou 6/6 vezes; coberta manualmente por leitura direta de `heartbeat.ts`, `read-bounded-body.ts`, os 4 endpoints `/api/internal/*` e `/api/ready` — nenhum bug adicional encontrado, código já fail-closed e bem estruturado. 5 testes existentes precisaram de atualização pra refletir o comportamento corrigido (não a correção de um teste quebrado por engano — os testes antigos tinham a query buggy como comportamento esperado). Também descoberto e documentado (não é bug de código): CI do GitHub bloqueado por pendência de billing da conta pessoal desde 10/08, 13 pushes seguidos falhando em 2s — só a pipeline local valida desde então. | pedido explícito do Fernando ("continua a implementacao em looping... corrigir bugs... o que falta e precisa de implementar... para deixarmos 100% em produção"); o crítico de `deriveMeetingSessionId` bloqueava toda reunião externa nova assim que M5-01 fosse ativado, então foi corrigido antes de qualquer outro item mesmo sem pedido explícito de priorização | baixa — todos os fixes são correção cirúrgica ou aditiva sobre código que ainda não roda em produção (M5-01 segue sem as migrations 0040/0041 aplicadas e sem `railway up`); 1035 Node (+5 novos/atualizados) + 26 Python + lint + typecheck (raiz + portal) + 9 validadores + `next build` verdes |
| D-V2-121 | 2026-08-14 | Achado ao vivo do próprio Fernando testando em produção: colou um link de Google Meet numa call real, tentou preencher a data/horário pra ela entrar e recebeu erro pedindo pra agendar com antecedência — mesmo o modo "entrada imediata" já existindo desde `01c56dc`. Causa raiz: o campo de data ficava sempre visível, convidando a digitar um horário "agora" que quase sempre já tinha passado (granularidade de minuto do `datetime-local` + latência até o clique chegar no servidor) e caía no erro "horário já passou". Corrigido em duas camadas: (1) UI — `external-meeting.tsx` reestruturado com um checkbox explícito "Agendar para mais tarde"; o padrão (checkbox desmarcado) nunca envia `scheduleAt`, então o caminho imediato jamais depende de acertar o datetime-local no segundo exato; (2) backend — `meeting-bot.ts` ganhou uma folga de 2 minutos: um horário agendado que já ficou levemente no passado agora é tratado como entrada imediata em vez de erro, em vez de travar o usuário no meio de uma call ao vivo. Confirmado sem mudança de comportamento pro Zoom/Teams: `join-meeting.ts`/`provider-recall` nunca diferenciaram por domínio da URL — o mesmo caminho imediato já vale pros três. | Fernando pediu explicitamente poder entrar "ao vivo na hora" tanto no Meet quanto no Zoom; a capacidade já existia no código, mas a UI ativamente atrapalhava o caso de uso mais comum (alguém já numa call tentando trazer a agente na hora) | baixa — mudança de UI + folga de tolerância num check já existente, sem migration, sem contrato de API alterado; 1035 Node + 26 Python + lint + typecheck + build + 9 validadores verdes |
| D-V2-122 | 2026-08-14 | Onda 7 (`W7`): auditoria multi-agente com foco em uso real (não em código/segurança como W3/W5/W6) — 3 dimensões: fluxos primários como usuário confuso de verdade, auto-revisão do fix ao vivo do dia (D-V2-121), e passe de mobile/responsivo — mais uma pesquisa isolada (sem código) sobre transformar o produto numa "Closer de verdade" pra Axtro. 11 achados, 4 verificados adversarialmente (3 reais, 1 refutado). **3 ALTOS corrigidos**: (1) ativar um agente sempre mostrava sucesso mesmo quando a auto-provisão de vídeo (best-effort por contrato) falhava — usuário só descobria minutos depois, numa tela sem relação nenhuma (Testar → "avatar não configurado"); `ProvisionResult` ganhou `attempted`, distinguindo "nada a fazer" de "tentou e quebrou", e `AgentStatusToggle` mostra aviso não-bloqueante quando é o segundo caso. (2) O tipo "Página web" ao cadastrar fonte de conhecimento parecia implicar busca automática da URL, mas é só um rótulo — nenhum crawler existe; renomeado pra "Página web (colar o texto)" e o placeholder do textarea agora é específico por tipo, deixando explícito que é preciso colar o texto. (3) `create-source-form.tsx` usava `style={{gridTemplateColumns:...}}` inline, que vence a media query de colapso mobile do `.form-row` independente de especificidade — quebrava o formulário de cadastro de fonte em qualquer tela de celular; o MESMO bug (achado médio) existia em `create-agent-form.tsx` e `invite-form.tsx`. Corrigido com duas classes novas (`.form-row-3`/`.form-row-4`) que carregam a variante de coluna E o colapso mobile, substituindo os 3 estilos inline. **1 achado refutado por reprodução real no navegador**: a tabela de `meeting-sessions.tsx` sem o wrapper `.table-scroll` (inconsistência real de padrão) foi acusada de cortar conteúdo em mobile — um verificador renderizou a página de verdade em 375px e provou o oposto: a AUSÊNCIA da classe `.data` (que a tabela também não tem) remove o `white-space:nowrap` que causaria o overflow, e os dados dessa tabela específica são todos curtos/enum; rebaixado a cosmético (perde estilo, não funcionalidade), sem fix nesta onda. **Auto-revisão do próprio fix do dia (D-V2-121)**: achado médio real — a folga de 2 minutos que eu tinha adicionado em `meeting-bot.ts` convertia silenciosamente um agendamento deliberado (câmera desligada até confirmação) em entrada imediata (câmera ligada na hora), quebrando a promessa explícita da UI pra quem marcou "agendar para mais tarde" de propósito; **revertida** — o problema que ela tentava resolver (alguém tentando entrar "agora" via um horário que já passou no relógio) já foi eliminado na raiz pelo checkbox do mesmo commit, que nunca mais envia horário no caminho imediato. **2 achados médios/baixos de mobile corrigidos**: checkbox novo de "agendar para depois" sem nenhum CSS de tamanho (ganhou 20×20px + padding vertical, alvo de toque ~44px); botão hamburguer de navegação mobile em 38×38px (subiu pra 44×44px). **1 achado médio de billing corrigido**: o banner "✓ Assinatura confirmada" podia aparecer diretamente acima da grade de planos com botões "Assinar" ainda ativos, porque o status do plano depende do webhook da Stripe (que pode atrasar alguns segundos) — trocado por uma mensagem de "confirmando" nesse intervalo, em vez de convidar a pagar de novo antes do primeiro pagamento aparecer. **1 achado médio adiado, com razão declarada**: horário de reunião agendada nunca é persistido, então o painel de sessões não consegue mostrá-lo de novo depois que o toast inicial some — exigiria uma coluna nova no banco (migration), e com todo deploy já bloqueado até o rollout M5-01 (ver nota abaixo), não faz sentido empilhar mais uma migration não-relacionada agora. **Pesquisa (sem código)** sobre a ideia do Fernando de uma Closer de verdade pra Axtro (não só SDR marcando diagnóstico): confirmado que a doutrina Método Silva já cobre fechamento completo (FASE 5) e o Modo Apresentação já existe — mas é só slides estáticos, sem demo ao vivo clicável do produto; Raissa (agente institucional da própria Axtro) já existe mas termina em "agendar Diagnóstico", não em fechamento na própria call. Recomendação entregue ao Fernando, nenhuma implementação feita — decisão de produto dele. | Fernando pediu pra continuar o loop autônomo, e junto trouxe um achado ao vivo (D-V2-121) que motivou uma auditoria com lente diferente das anteriores — usuário real tentando usar, não revisor de código; a auto-revisão do próprio fix do dia pegou um bug real introduzido horas antes, reforçando a disciplina já estabelecida nas ondas anteriores de sempre revisar código novo antes de fechar a onda | baixa — nenhuma migration, nenhuma mudança de contrato de API; CSS/copy/UI + um sinal novo opcional (`warning`) numa interface já existente; 1035 Node + 26 Python + lint + typecheck + build + 9 validadores verdes; verificado visualmente no navegador em 375px (mobile) antes de comitar |
| D-V2-123 | 2026-08-16 | Pedido explícito do Fernando: "o que temos hoje com a Raissa é um SDR, consegue fazer um closer que vai entrar na call e fazer vendas/conversão?" — criada **Camila — Axtro Closer**, agente nova (não mexi na Raissa, por decisão explícita dele) no mesmo tenant institucional, ativada com persona de vídeo auto-provisionada. Não precisou mudar a doutrina: `buildCloserVideoSystemPrompt` já cobre as 6 fases do Método Silva incluindo FASE 5 · FECHAMENTO ("peça a decisão... Vamos começar?") — o gap real era só ela nunca ter sido convidada a fechar, não uma limitação da doutrina. Implementada a peça que faltava (achado da pesquisa: nenhuma automação hoje gera checkout pra um prospect sem conta e manda por e-mail): `createProspectCheckoutSession` novo em `packages/provider-stripe` (Checkout Session avulsa, sem o acoplamento a `checkoutIntentId`/`tenantId` do fluxo de assinatura self-serve — um prospect fechando com a Camila não é tenant), `sendProposalEmail` novo em `email.ts` (único e-mail deste módulo que sai pra um endereço fora da conta), e a nova server action `sendClosingProposal` + UI "Fechar e enviar proposta" na página de Testar de qualquer agente. Fechamento é sempre disparado por clique humano depois da call revisando empresa/e-mail/plano — nunca a IA sozinha no meio da conversa (doutrina "IA rascunha, humano manda", já documentada em `docs/BRIEFING_RAISSA_CLOSER_VIDEO.md` §6; também evita reconstruir a infraestrutura de tool-call servidor-a-servidor que a pesquisa confirmou não existir hoje pro Tavus — só há handler client-side pros 3 tools de slide). **Achado que trava um passo do plano, não resolvido nesta onda**: RAG é compartilhado por TENANT inteiro, não por agente (confirmado em `portal_search_knowledge_service`/`portal_knowledge_digest`, sem predicado de `agent_id` em nenhuma tabela/RPC) — alimentar a Camila com fatos reais da Axtro contaminaria Rafaela/Amanda, personas de demonstração de negócios fictícios no mesmo tenant. Criar um tenant dedicado exigiria um cadastro novo, ação que só o Fernando pode fazer (nunca eu, mesmo autorizado). Decisão pendente dele: tenant dedicado vs. Camila operando só com a doutrina (sem citar preço/detalhe específico até isolar o conhecimento). **Achado colateral, sem fix necessário**: reproduzi um crash real (500, `Cannot read properties of undefined (reading 'toLocaleString')`) no `/dashboard` ao testar localmente — o frontend atual já espera os campos `total_cost_usd_today`/`total_cost_usd_7d` que só existem na versão de `portal_usage_summary` da migration 0040 (M5-01), ainda não aplicada no banco hospedado; o próprio gate `production:bootstrap` (exige schema v41) impede esse código de alcançar produção antes da migration — nada a corrigir, resolve sozinho quando 0040 for aplicado na janela de manutenção. | Fernando quer o produto pronto pra vender pra empresas de verdade ("precisamos fazer este serviço para vender para empresas, para que tenha usuários"); pediu explicitamente uma segunda agente (não mexer na Raissa) e que o fechamento termine em link de checkout por e-mail | baixa — nenhuma migration nesta onda, nenhuma mudança de contrato de API pública existente (só uma função nova no `StripeBillingPort` e uma action nova); `createProspectCheckoutSession` só cria uma Checkout Session (não cobra nada até o prospect confirmar); testado ao vivo no navegador até o ponto de "cobrança não configurada" (chave Stripe ainda não setada localmente, esperado); 1035 Node + 26 Python + lint + typecheck + build + 9 validadores verdes |
| D-V2-124 | 2026-08-18 | Até existirem intents duráveis próprias, a ativação de agente não auto-provisiona persona Tavus real e o fluxo de proposta não cria Checkout Stripe de prospect. Ambos devolvem estado explícito e preservam o modo fake determinístico. Em paralelo, cada chamada OpenRouter passa por um limite conservador de bytes UTF-8 e framing antes de cruzar a fence de dispatch. | A auditoria M5-01 mostrou que esses dois efeitos pagos não tinham reservation/receipt/reconciliação e que os limites por caracteres podiam permitir gasto acima do envelope antes do commit. ADR-036 exige que resultado ambíguo bloqueie capacidade, não seja resolvido por retry. | média — reabrir cada fluxo requer intent service-only, idempotência estável, vínculo de receipt e reconciliação testados; o préflight de IA pode ser ajustado somente junto da rate card/modelo aprovado. |
| D-V2-125 | 2026-08-18 | A ponte de runtime do portal passa a ser a única fronteira para canais com provider: admissão v43 cria ou revalida sessão, disclosure e consentimento por finalidade de forma atômica; grants têm consumo cercado por provider/cena, receipts duráveis e kill switches consultados no dispatch. Vídeo direto entra pela ponte; reunião externa e criação pública de sessão de lead permanecem fail-closed até existir consentimento individual verificável de cada participante. Contexto Tavus/RAG/percepção é dado externo, nunca autoridade `system`. | A auditoria M5-02 comprovou que o portal criava recursos Tavus/Recall fora do lifecycle constitucional e promovia input de provider/retrieval a `system`, permitindo gasto, gravação e injeção sem evidência de finalidade. Fechar essas rotas é mais seguro do que atribuir consentimento inexistente a um operador. | média — o fluxo pode ser expandido de modo reversível com capabilities participantes de curta duração, convites/disclosure por pessoa e testes de revoke no meio da chamada; a flag `PORTAL_RUNTIME_BRIDGE_ENABLED` permanece desligada até o rollout remoto 0040→0043 e canary observável. |
| D-V2-126 | 2026-08-18 | Durante a janela de manutenção M5-01, o `production-readiness-bootstrap.mjs` engolia o motivo real de `RPC_UNAVAILABLE` (fetch falho ou resposta não-ok), impedindo diagnosticar se era rede do container isolado do `preDeployCommand`, timeout ou outra causa. Corrigido: `ProductionReadinessBootstrapError` agora carrega um `cause` opcional (sempre um `Error` de rede/HTTP construído por nós mesmos, nunca headers/body/segredo), propagado da falha de fetch e do `!response.ok` (com status/statusText), e impresso no log final como `cause=Nome:mensagem`. Suíte local (bootstrap + M5-01 + M5-02, 36 testes) continua verde. Confirmado ao vivo, fora do Railway: o endpoint REST de `portal_schema_capabilities_service` responde `401`/`42501` (permission denied) com a chave publicável — não `404` — provando que a função está exposta e o Supabase está saudável a partir desta rede; o bloqueio parece ser do container isolado do `preDeployCommand` do Railway, não de configuração ou schema. Em paralelo, o HEAD do repositório avançou para o M5-02 (`636d0db`) durante essa mesma investigação — `REQUIRED_SCHEMA_VERSION` subiu de 42 para 43 e a migration `0043_portal_runtime_bridge_contract.sql` (aditiva, mesma disciplina de 0040-0042, flag `PORTAL_RUNTIME_BRIDGE_ENABLED` desligada) precisa ser aplicada antes do próximo redeploy; entregue ao Fernando — aplicar DDL remoto continua bloqueado pelo classificador de segurança mesmo em modo `/loop` autônomo. | O runbook M5-01 exige decisão informada em cada falha do gate; um código de erro genérico sem causa raiz forçava tentativa-e-erro cega a cada redeploy. A disciplina de nunca aplicar DDL remoto sozinho se manteve mesmo sob pedido explícito de autonomia — o classificador bloqueou a chamada e a ação correta foi entregar o arquivo pronto, não insistir ou contornar. | alta — mudança é só logging (`cause` nunca inclui segredo; auditado e testado), reversível trivialmente com um `git revert`; a migration 0043 em si é aditiva e seu rollback já está coberto pelo mesmo runbook expand-contract das anteriores. |
| D-V2-127 | 2026-08-18 | Revisão adversarial de `database/supabase-only/0043_portal_runtime_bridge_contract.sql` (ainda **não aplicada** em produção — schema live segue v42) antes de o Fernando aplicar, feita por um agente dedicado com instrução de reportar só achados confirmados por leitura direta do código. 4 achados reais, todos corrigidos in-place no mesmo arquivo (safe porque a migration nunca foi executada em produção): **(1) CRÍTICO** — `apps/portal/src/lib/runtime/portal-channel-runtime-bridge.ts:443` chamava a RPC de admissão com a chave `p_capability_set`, mas a função SQL (`0043:212`) declara o parâmetro como `p_capabilities` (obrigatório, sem default) — `supabase-js`/PostgREST resolve `.rpc()` por nome de parâmetro, então toda chamada real falharia com erro de função não encontrada, caindo no `catch` genérico e virando `service_unavailable`: **o caminho inteiro de admissão de canal do M5-02 (início de conversa Tavus/Recall) estava morto**, mascarado porque o teste unitário mockava a mesma chave errada e o teste de integração SQL chama a função por parâmetro posicional (bypassa o bug). Corrigido: renomeado para `p_capabilities` no TS e no teste (`tests/portal/runtime-channel-bridge.test.mjs:66`). **(2) ALTO** — a reescrita completa de `portal_schema_capabilities_service()` em 0043 (troca o `create or replace` de 0042 inteiro) simplificou sem aviso os checks de `costEventSchemaVersion` (perdeu a validação de tipo/nullability/default da coluna e a constraint de check) e `legacyCostWritersRevoked` (de 8 condições — 4 funções legadas × `service_role`+`authenticated` — para 1 única, deixando 3 writers legados de custo sem verificação nenhuma pelo gate de produção). Restaurado o rigor completo de 0042 dentro do `create or replace` de 0043. **(3) MÉDIO** — race TOCTOU no invariante "um microfone só" (`active_presenter_id`): duas admissões concorrentes para a mesma sessão com presenters diferentes podiam ambas ler `active_presenter_id is null` antes de qualquer uma commitar, driblando a checagem. Adicionado `pg_advisory_xact_lock(hashtextextended('runtime-admission-session:'\|\|p_session_id::text,0))` logo após o fast-path de replay, seguindo o mesmo padrão já usado em 0040 (lock por chave de negócio, não por linha). **(4) MÉDIO** — o outcome `one_mouth_conflict` nunca era de fato retornado: a função levantava `raise exception ... errcode='23505'`, que o wrapper JS converte em erro genérico e o `catch` do bridge rebaixa para `service_unavailable` — a mensagem amigável ("já existe uma apresentação ativa...") era código morto em produção. Trocado para `return jsonb_build_object('outcome','one_mouth_conflict')`, no mesmo estilo já usado por `blocked_kill_switch`/`blocked_agent_inactive` na mesma função. | achado por revisão adversarial dedicada (via `/loop`) enquanto a migration ainda estava só localmente, antes do Fernando aplicar em produção — corrigir agora custa 4 edições pequenas; corrigir depois de aplicada custaria uma migration de correção (`0044`) e, pior, o achado #1 só apareceria em produção como "todo início de conversa via M5-02 falha silenciosamente", já que `PORTAL_RUNTIME_BRIDGE_ENABLED` estava planejado para ligar logo após o deploy. | alta — arquivo ainda não aplicado a nenhum ambiente; 586 testes locais (`node --test tests/portal tests/operations`) verdes após as 4 mudanças; migration continua puramente aditiva (mesmo runbook expand-contract). Fernando deve usar esta versão corrigida do arquivo (não a entregue anteriormente) ao aplicar no SQL Editor. |
| D-V2-128 | 2026-08-18 | Migration `0043_portal_runtime_bridge_contract.sql` (já corrigida, D-V2-127) aplicada em produção (`ovctadcrvnfpgxzplupp`) a pedido direto do Fernando. `portal_schema_capabilities_service()` confirma `version:43` e todas as capabilities novas (`runtimeChannelAdmission`, `runtimeChannelGrantFences`, `runtimeProviderBindingReceipts`, `runtimeSceneReceipts`, `runtimeKillSwitches`, `runtimeDualOperatorReconciliation`) em `true`. Advisor de segurança do Supabase rodado logo em seguida: os 8 achados relacionados às tabelas novas são todos `INFO`/`rls_enabled_no_policy` — o mesmo padrão intencional já usado em toda a migration (RLS forçada, zero policy, acesso só via função `SECURITY DEFINER` revogada de `authenticated`/`anon`), não uma regressão; os `WARN` da varredura (funções com `search_path` mutável, `authenticated_security_definer_function_executable` em RPCs do portal, extensão `vector` em `public`, proteção contra senha vazada desligada) são todos pré-existentes, nenhum introduzido por esta migration. | schema live precisava chegar em v43 antes de qualquer novo `railway up` ter chance de passar no gate `production:bootstrap`, que já exige essa versão desde o M5-02. | alta — migration aditiva, mesmo runbook expand-contract das anteriores; nenhuma tabela/função pré-existente alterada de forma destrutiva. Próximo passo depende só de rodar `railway up`/Redeploy de novo — ação que o classificador desta sessão continua bloqueando eu executar diretamente. |
| D-V2-129 | 2026-08-18 | O bridge aceita UUID RFC 4122 v1–v8 apenas como correlação externa de comando, preservando UUIDv7 para todas as identidades autoritativas. O receipt de canal passa a exigir a referência/URL exata já committed pela reservation e o evento de kill switch passa a ter vínculo composto tenant/switch. | O browser gera `crypto.randomUUID()` (v4), que antes era rejeitado pelo bridge e deixava vídeo/apresentação indisponíveis. A mesma auditoria achou que um receipt poderia combinar uma reservation válida com outro recurso do mesmo tenant/provider, e que o audit row de kill switch não tinha defesa relacional tenant-safe. | alta — validação e migration aditiva forward-only; rollback operacional é kill switch/forward fix, sem reabrir caminho legado. |
| D-V2-130 | 2026-08-18 | A descoberta pública permite `OAI-SearchBot`, `Claude-SearchBot` e `Claude-User`, mas bloqueia `GPTBot`, `ClaudeBot`, `Google-Extended` e `CCBot`; `llms.txt` declara que a permissão é de descoberta, não de treinamento. | OpenAI e Anthropic separam search de treinamento. Google-Extended não permite separar treinamento de grounding Gemini e CCBot não separa finalidade; a opção conservadora protege o conteúdo mesmo ao custo de reduzir essas superfícies de discovery. Fontes primárias verificadas: OpenAI Crawlers, Anthropic crawler policy, Google common crawlers, CCBot e RFC 9309. | alta — alteração declarativa em `robots.txt`/arquivos AEO, reversível por release; a futura decisão de liberar Google grounding deve revisar este trade-off explicitamente. |
| D-V2-131 | 2026-08-18 | Retomada da sessão anterior (Claude Code, parada em D-V2-117) após outra sessão (Codex, branch `codex/m5-01-integrity-hardening`) ter avançado o repo através de um marco inteiro novo (M5 — Production Integrity, Trust and Discovery, D-V2-118 a 130) sem que eu tivesse contexto disso. Pedido do Fernando: usar o vault Obsidian "Axtro Ultra Brain" (`~/Developer/Axtro-Ultra-Brain`, comando `brain context`) pra recuperar contexto, e então (1) consertar o E2E, (2) atualizar `RELEASE_READINESS.md`/`NEEDS_CONNECTION.md` pro estado real, (3) revisar e preparar os 33 arquivos não commitados da Auditoria 360/M5-03 pra commit. Esta entrada cobre os itens (1) e (2); o item (3) é tratado numa entrada separada quando o commit da Auditoria 360 for feito. **Achado real ao investigar** (não estava no handoff do Obsidian, que citava "CI bloqueado por billing" como problema atual): o bloqueio de billing do GitHub (`github.com/settings/billing`) já tinha sido resolvido pelo Fernando entre 14/08 e 16/08 — confirmado via `gh run view` comparando um run de 14/08 ("account is locked due to a billing issue", todos os jobs abortados em 2s) com o run mais recente de 16/08 (D-V2-123, "Camila"), que rodou jobs reais e falhou por um motivo DIFERENTE: `e2e-portal` — teste `login do usuário demo leva ao dashboard com métricas` — `getByText("Custo estimado hoje")` não encontrado. Rastreei a causa: o M5-01 (`fix(portal): harden paid effects and ledger`, commit `e5dd899`) renomeou o painel de custo do dashboard de "Custo estimado hoje"/"Preço público de tabela — não é a fatura real" pra "Custo atribuído hoje"/"Ledger estimado/reportado — não é a fatura conciliada" (reflete o novo modelo de ledger com receipt), mas ninguém atualizou `apps/portal/e2e/portal.spec.ts` pra acompanhar — 2 asserções checavam texto que não existe mais em NENHUM lugar do código (`grep` confirmou zero ocorrências). Corrigido: as 2 asserções atualizadas pro texto novo. Rodando a suíte completa localmente descobri mais 2 problemas do MESMO tipo estrutural (teste desatualizado, não regressão de produto), ambos causados por features de ondas anteriores que a suíte nunca acompanhou: (a) `getByRole("button", { name: "Enviar" })` sem `exact: true`, em 2 testes — desde a Camila (D-V2-123), a página `/agentes/[id]/testar` renderiza `ClosingProposal` (botão "Enviar proposta") pra TODO agente, sem distinção de `role_type`, e o Playwright faz match por substring por padrão, então "Enviar" batia nos DOIS botões e lançava "strict mode violation"; corrigido com `exact: true` nos dois testes (não mexi em `ClosingProposal` renderizar pra todo agente — isso pode ser intencional ou pode ser um achado de produto pro Fernando avaliar depois, mas não é o que estava quebrando o CI); (b) o teste de apresentação simulada clicava em "Iniciar apresentação" que ficava desabilitado pra sempre — o bridge de runtime do M5-02 (`portal-channel-runtime-bridge.ts`) exige as 5 confirmações de `VideoChannelConsent` (disclosure+essentialProcessing combinadas num checkbox, recording, transcription, behavioralAnalysis, visualAnalysis) antes de habilitar o botão, e o teste nunca marcava nenhuma; corrigido marcando as 5, escopado ao `getByRole("group", {name: "Consentimento para esta apresentação"})` porque a mesma página tem outro formulário de consentimento parecido (vídeo/reunião externa) que um `getByLabel` solto também batia. Suíte completa (18 specs: 15 `portal.spec.ts` + 3 `public-discovery.spec.ts` do M5-03) verde depois dos 3 fixes; gate completo (1.063 Node + 26 Python + lint + typecheck + `validate_all.py` 9/9 + `next build`) verde. `RELEASE_READINESS.md` e `NEEDS_CONNECTION.md` (ambos travados em 13/08, D-V2-117 — 2 marcos inteiros atrasados) reconciliados: nota Geral recalculada de 89 pra 88 (ganhos reais em Banco/Segurança/Custos/Testes do M5-01/M5-02, compensados pela queda em Confiabilidade — o P0 de media boundary da Auditoria 360, arquitetural e ainda aberto, agora rebaixa essa nota explicitamente em vez de ficar só narrado); `NEEDS_CONNECTION.md` ganhou linhas pra migration 0044 (não aplicada), flag `PORTAL_RUNTIME_BRIDGE_ENABLED` (desligada por padrão) e o P0 de media boundary como itens de decisão pendente do Fernando, e a linha de CI billing foi corrigida de "bloqueando" pra "resolvido, com achado novo no lugar". | pedido explícito do Fernando ("faz os tres, comecando pelo e2e") após eu reportar o estado do repo (auditoria própria + vault Obsidian); a disciplina de nunca aceitar um resumo de terceiro (nem o handoff do Codex, nem o brain do Obsidian) sem verificar contra o estado real (`gh run view`, `grep` no código) foi o que revelou que o problema real do CI já tinha mudado de natureza — billing resolvido, E2E defasado no lugar | baixa — mudança só em `apps/portal/e2e/portal.spec.ts` (3 seletores) e 2 arquivos de documentação; nenhum código de produto tocado, nenhuma migration, nenhum secret; gate completo rodado 1x depois do fix (não precisou de auto-revisão formal em workflow — mudança mecânica e de baixo risco, cada linha verificada manualmente contra o componente real antes de editar) |
| D-V2-132 | 2026-08-18 | Antes de assinar (commitar) os 33 arquivos não commitados da Auditoria 360/M5-03 (escritos pelo Codex, D-V2-129/130) sob esta sessão, apliquei a mesma disciplina de auto-revisão adversarial usada para código próprio (D-V2-111/114/115/116/117), mesmo já aprovado pela sessão anterior — "a documentação diz que está ok" não é verificação suficiente. Rodei 3 agentes independentes em paralelo: (A) `database/supabase-only/0044_runtime_bridge_integrity_repair.sql` + scripts de infraestrutura + relaxamento de UUID no bridge — limpo, só 2 notas P3 não-bloqueantes; (B) redação de PII em `telemetry.ts` — confirmou recursão genuína e proteção de campo reservado, mas achou um gap real (P2): `redactArray`/`redactRecord` chamavam `Object.getOwnPropertyDescriptors(value)` sobre a coleção inteira ANTES de qualquer corte por `MAX_ARRAY_ITEMS`/`MAX_REDACTION_ENTRIES`, custando O(n) de enumeração mesmo para um payload de milhões de elementos (medido 1.27s–3.33s bloqueando o event loop) — não explorável hoje via nenhum dos 131 call sites reais de telemetria (todos passam campos pequenos e curados), mas uma alegação de "limites" que não cobria largura/cardinalidade da coleção; corrigido trocando a enumeração eager por `Object.getOwnPropertyDescriptor` por índice/chave dentro do próprio loop já limitado (`redactArray`/`redactRecord` em `apps/portal/src/lib/telemetry.ts`), o que limita o custo ao orçamento (`MAX_ARRAY_ITEMS`=50, `state.remainingEntries`) em vez do tamanho da entrada; (C) copy pública/SEO — achou 2 P2 reais: `robots.ts` dava `Allow: "/"` sem nenhum `Disallow` de rota privada nos blocos `OAI-SearchBot`/`Claude-SearchBot`/`Claude-User` (RFC 9309: bot com bloco próprio não herda do `*`), contradizendo `docs/codex/SYSTEM_MAP.md`; corrigido consolidando os paths privados num array único (`PRIVATE_PATHS`) reaplicado aos 3 blocos "allow", mais um teste de regressão novo em `public-discovery.spec.ts` que extrai cada bloco e verifica a ausência de paths privados (a lacuna de cobertura que deixou o bug passar); e um achado de risco legal que NÃO corrigi sozinho: a copy de `/privacidade` ("A conversa essencial pode operar sem finalidades opcionais...", "Sem esse consentimento, a conversa essencial não autoriza por si só a criação desse histórico") descreve um caminho de consentimento desacoplado que o código não implementa — `portal-channel-runtime-bridge.ts` rejeita a admissão inteira se qualquer uma das 4 finalidades opcionais não estiver confirmada, e `VIDEO_RUNTIME_PURPOSES` sempre pede as 4 juntas; é uma decisão de produto/jurídico (ajustar a copy para refletir tudo-ou-nada hoje, ou desacoplar o consentimento no bridge) que exige o Fernando, não algo pra eu decidir sozinho numa página de aviso de privacidade. Gate completo re-rodado depois das correções: 1063 Node + 26 Python, lint, `validate_all.py` (9/9), `tsc --noEmit`, e2e público 3/3 (com a asserção nova) — todos verdes. | Compromisso da sessão desde D-V2-111: nunca assinar trabalho — próprio ou de outro agente — sem verificação adversarial própria contra o código real, não contra a alegação do autor. | alta — as 2 correções aplicadas são estritamente mais conservadoras que o estado anterior (bound de enumeração mais apertado, escopo de robots.txt mais restrito, ambos sem mudar comportamento externo observável); o achado de copy/consentimento foi deliberadamente deixado em aberto para decisão humana, não escondido nem resolvido por conta própria. |
| D-V2-133 | 2026-08-18 | A pedido explícito do Fernando (3 decisões numa única resposta), executei em sequência: (1) reescrevi a copy de `/privacidade` pra descrever o consentimento tudo-ou-nada real do bridge de vídeo hoje (texto sempre sem finalidade opcional; vídeo sempre com as 4 finalidades opcionais juntas ou sessão negada), em vez de manter a promessa de desacoplamento que o código não implementa (achado da D-V2-132); atualizei o teste `portal-seo-surface.test.mjs` que checava o texto antigo; gate completo (1063 Node + 26 Python + `validate_all.py` 9/9) verde antes de commitar. (2) `git merge --ff-only codex/m5-01-integrity-hardening` em `main` (fast-forward limpo, merge-base já era o tip do main) e `git push origin main` — os 7 commits do marco M5 completo (M5-01/02/03) chegaram ao GitHub. (3) Migration `0044_runtime_bridge_integrity_repair.sql` aplicada em produção (`ovctadcrvnfpgxzplupp`) via Supabase MCP: `portal_schema_capabilities_service()` confirma `version:44` e `runtimeBridgeReceiptIntegrity:true` junto com todas as capabilities anteriores intactas; advisor de segurança rodado logo em seguida — os únicos achados novos nas tabelas tocadas (`portal_runtime_kill_switches`, `portal_runtime_kill_switch_events`, `portal_runtime_provider_channel_receipts`) são `INFO`/`rls_enabled_no_policy`, o mesmo padrão intencional já documentado em D-V2-128 (RLS forçada, zero policy, acesso só via função `SECURITY DEFINER` revogada de `authenticated`/`anon`); todos os 40 `WARN` da varredura são pré-existentes em funções/extensões que 0044 não tocou, nenhum introduzido por esta migration. `PORTAL_RUNTIME_BRIDGE_ENABLED` continua desligada — a migration fecha a lacuna de integridade do receipt sem mudar comportamento observável em produção. | pedido direto do Fernando após eu apresentar as 3 decisões pendentes via pergunta estruturada, cada uma com opções e trade-offs explícitos | alta para (1) e (2) — copy é mudança textual reversível e o fast-forward não reescreve histórico; média para (3) — migration é aditiva (constraint UNIQUE + FK composta + `CREATE OR REPLACE FUNCTION`, nenhum `DROP`/perda de dado), mas é produção real; verificada com `portal_schema_capabilities_service()` e advisor imediatamente depois. |
| D-V2-134 | 2026-08-18 | CI do `main` estava vermelho por um motivo que eu tinha erroneamente suspeitado ser relacionado ao meu código (migration 0044, testes de concorrência novos): li o log real do run com `gh run view --log` e descobri que `validate-local-migrations` nunca chegava a rodar `db:test`/`db:rls`/`db:portal:test` — o step `apt-get update && apt-get install postgresql-client` ficava mudo por 9+ minutos tentando repetidamente o mirror `azure.archive.ubuntu.com` pré-configurado no runner, até o job inteiro estourar o `timeout-minutes: 10`. Confirmei que não era o meu código rodando `pnpm db:portal:test` localmente contra um Postgres 17+pgvector externo (mesmo caminho de dados do CI via `AXTRO_LOCAL_DATABASE_URL`): passou em ~20s. Corrigido `.github/workflows/docs-qa.yml` com `Acquire::Retries=3`/`Acquire::http(s)::Timeout=15` no apt e `timeout-minutes: 3` no step, pra falhar rápido e claro em vez de consumir o budget do job inteiro em silêncio. De passagem corrigi um bug real que atrapalhou meu próprio diagnóstico: `run()` em `scripts/supabase-portal-integration.mjs` (usada por `initdb`/`pg_ctl start`) engolia `stderr`/`stdout` do processo filho ao falhar — mesma classe de problema já corrigida em D-V2-126, agora inclui o detalhe real do erro. Re-rodei o CI completo após o fix: 6/6 jobs verdes, `validate-local-migrations` em 2m7s. | achado ao investigar por conta própria, sem que o Fernando tivesse pedido — decidi seguir em frente sob a autorização geral ("faz o que for mais recomendado e inteligente") já que deixar o CI vermelho por um motivo mal diagnosticado é pior do que corrigir e reportar depois | alta — mudança só em opções de retry/timeout do apt e propagação de stderr num script de teste local; nenhum comportamento de produto ou schema tocado; verificado com um run de CI completo e verde antes de seguir. |
| D-V2-135 | 2026-08-18 | Investigando o P0 de media boundary (Tavus/Recall sem prova de cancelamento a tempo, aberto desde a Auditoria 360) com autonomia total a pedido do Fernando, orquestrei um Workflow de 7 agentes (4 investigação + 3 design) depois de mapear a arquitetura real eu mesmo primeiro (confirmando que o pipeline elaborado de M0-M2 — avatar-gateway/turn-coordinator — é código morto em produção; apps/portal fala direto com @axtro/provider-tavus/@axtro/provider-recall). Achados principais: (1) confirmado por grep exaustivo — nenhuma função no repo permite a um clique humano encerrar uma conversa Tavus ou bot Recall ativos hoje; os botões "Encerrar" fazem só teardown client-side. Desenhei (não implementei ainda) 3 server actions novas seguindo o padrão já estabelecido de reservas/receipts do repo, com atenção a um risco de IDOR cross-tenant real (as funções SQL relevantes não validam tenant_id internamente — reservationId nunca cruzava o browser até agora). (2) Pesquisa em docs.tavus.io/docs.recall.ai: nenhum dos dois providers documenta latência de encerramento; Tavus tem test_mode gratuito mas o PAL nunca entra na sala nesse modo (inútil pra medir corte de áudio); Recall não tem sandbox documentado. Um canário manual (scripts/canaries/termination-latency-canary.mjs, escrito por um subagente de design e revisado/testado por mim antes de commitar) mede isso quando o Fernando decidir rodar contra credencial real. (3) Achado independente, fora do escopo original: o reconciliador de efeitos pagos nunca roda automaticamente em produção — 176/176 execuções agendadas do GitHub Actions skipped porque a repo variable `PROVIDER_EFFECT_RECONCILE_SCHEDULE_ENABLED` nunca foi criada, e o secret `PROVIDER_EFFECT_RECONCILE_SECRET` também está ausente no GitHub (confirmado via `gh api`/`gh secret list`) — reservas presas em cleanup_pending/unknown/provider_in_flight ficam sem TTL por design (ADR-036) e sem liberação automática. (4) Achado independente, verificado por mim via `railway status`: produção mostra "Deploy failed (14h)" — o serviço está `Online` mas servindo o último deploy bem-sucedido anterior, não o HEAD atual do `main`. (5) **Achado mais sério, ativo agora**: lendo `railway logs` encontrei centenas de linhas `error_name="unknown"` pra todo tipo de evento de status do Recall; reproduzi contra produção via Supabase MCP e confirmei `ERROR 42725: function portal_update_meeting_bot_session_status_service(text,text) is not unique` — a migration 0040 criou um overload de 4 args sem nunca dropar o de 2 args de 0021/0024, e a convenção de chamada do PostgREST pra status não-terminal (sem delivery/claim) ficou ambígua. Real, ativo, quebrando atualização de status de reuniões externas reais agora. Corrigido com migration 0045 (local, não aplicada — `drop function` do overload obsoleto), testada contra o harness real (reproduz o erro 42725 e prova a correção) e contra `pnpm test` completo (1063+26, gate 9/9). Veredito honesto sobre o P0 original: **mitigação parcial, não resolução** — o gap de "ninguém consegue pedir cancelamento" é 100% fechável sem credencial (design pronto, não implementado ainda); o gap de "prova de latência real de corte" exige teste pago ao vivo que só o Fernando pode autorizar; e o gap financeiro do reconciliador é totalmente independente do botão de stop. | pedido explícito do Fernando ("faz o que for mais recomendado e inteligente") após eu apresentar o P0 como único item em aberto — Ultracode ligado, workflow multi-agente foi a ferramenta certa pra exaustividade | migration 0045 é aditiva e segura (só remove um overload obsoleto e comportalmente redundante, provado pelo harness) mas ainda não aplicada — aguardando autorização; os achados (2)/(3)/(4) exigem ação direta do Fernando (GitHub/Railway) que nenhuma sessão autônoma pode fazer sozinha; o design da ação de stop e o canário são só planejamento/scaffold, zero código de produção novo em execução. |
