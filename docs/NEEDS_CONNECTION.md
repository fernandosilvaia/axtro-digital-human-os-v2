# NEEDS_CONNECTION — o que depende só de você

Tudo abaixo já tem código, adapter, mock e teste prontos. Falta apenas a conexão indicada.

| Serviço | Variável ou ação necessária | Onde conectar | Status |
|---|---|---|---|
| Resend (e-mail de convite) | ~~`RESEND_API_KEY`~~ **RESOLVIDO 2026-07-31**: configurada no Railway | Railway → Variables | — |
| Domínio próprio | ~~URL de produção era a raw `*.up.railway.app`~~ **RESOLVIDO 2026-08-01**: `closer.axtroai.com` conectado no Railway (Settings → Domains, feito por você fora desta sessão) | Railway → Settings → Domains | — |
| Portal URL nos e-mails e SEO | ~~`PORTAL_PUBLIC_URL` e `NEXT_PUBLIC_SITE_URL`~~ **RESOLVIDO 2026-08-01**: ambas atualizadas de `portal-production-b43e.up.railway.app` para `https://closer.axtroai.com` no Railway + redeploy (`NEXT_PUBLIC_SITE_URL` é build-time). Achado real: o domínio próprio já estava conectado, mas essas duas variáveis continuavam apontando pra URL crua — e-mails, sitemap, `og:url` e `<link rel="canonical">` ficariam com o domínio errado até o fix. Verificado ao vivo: `sitemap.xml`, `robots.txt`, `og:url` e canonical todos em `closer.axtroai.com` | Railway → Variables | — |
| Telefonia (Telnyx) | Conta + `TELNYX_API_KEY` + número SIP | PENDENCIAS_EXTERNAS · sem adapter ainda (T10) | aguardando conta |
| Meet/Zoom/Teams (Recall.ai) | ~~Conta + `RECALL_API_KEY`~~ **RESOLVIDO 2026-07-31**: chave do Doppler validada contra a API real (região `us-west-2` confirmada; as outras 3 dão 401), `RECALL_API_KEY`/`RECALL_API_REGION`/`RECALL_WEBHOOK_TOKEN` no Railway, UI no portal | `/agentes/<id>/testar` → "Levar para uma reunião externa" | funcional — falta só o webhook (linha abaixo) |
| **Webhook do Recall.ai** | ~~Só a raw URL do Railway estava cadastrada~~ **RESOLVIDO 2026-08-01**: segundo endpoint cadastrado no dashboard apontando pro domínio próprio (`https://closer.axtroai.com/api/recall/webhook?token=<RECALL_WEBHOOK_TOKEN>`), mesmos 8 eventos de status de bot. Os dois endpoints (raw Railway + `closer.axtroai.com`) ficam ativos em paralelo — nenhum foi removido. Sanity check pós-cadastro: `curl` no endpoint novo com token errado responde 401 (rota de pé, autenticação funcionando) | https://us-west-2.recall.ai/dashboard/webhooks/ | **opcional para testar**: sem ele o agente entra na reunião normalmente; só o status da sessão no nosso banco fica sem atualizar |
| Assinatura HMAC do webhook do Recall.ai | ~~Segredo do workspace não gerado~~ **RESOLVIDO 2026-08-01**: `RECALL_WEBHOOK_SECRET` (`whsec_...`, dashboard Recall.ai → Developers → API Keys & Secrets) gerado pelo Fernando, colocado no Doppler e sincronizado no Railway. Camada adicional ao token na URL (as duas são exigidas quando o segredo está configurado) — verifica os cabeçalhos `webhook-id`/`webhook-timestamp`/`webhook-signature`, janela anti-replay de 5min, 9 testes novos (D-V2-099) | `apps/portal/src/lib/meetings/webhook.ts` | funcionando — puramente aditivo, sem regressão no token que já funcionava |
| Billing (Stripe) | **Código pronto desde D-V2-101**, migrations `0025`-`0033` TODAS aplicadas em produção (verificado via `execute_sql`, ver README de supabase-only), e `node scripts/stripe_setup.mjs` já RODOU DE VERDADE em 2026-08-10 (catálogo real na Stripe: 1 Meter + 3 Products + 6 Prices, `livemode: false` confirmado). Falta só: (1) configurar `STRIPE_SECRET_KEY`/`STRIPE_WEBHOOK_SECRET`/6 `STRIPE_PRICE_*`/`STRIPE_CONVERSATION_OVERAGE_EVENT_NAME` no Railway (bloqueado — ação em infraestrutura compartilhada, aguardando você colar no dashboard); (2) cadastrar o webhook `https://closer.axtroai.com/api/stripe/webhook` no dashboard Stripe; (3) validar uma assinatura de teste ponta a ponta (checkout → webhook → painel mostrando o plano); (4) só então trocar pra chave live — **gate humano explícito** | dashboard.stripe.com + Railway → Variables | aguardando as env vars no Railway (gate humano de pagamento) — sem elas o card "Plano e cobrança" degrada com aviso e o resto do portal segue normal |
| Telemetria (Sentry ou log drain dedicado) | Decisão de vendor + DSN/credencial | `apps/portal/src/lib/telemetry.ts` já centraliza e redige todo log — só falta o wiring do vendor escolhido | aguardando decisão (logs estruturados do Railway já funcionam como observabilidade mínima) |
| Rate card de custos (taxa negociada) | ~~Preços reais por unidade~~ **RESOLVIDO 2026-07-24 com preço público de tabela** (D-V2-078) — se você tiver taxa negociada diferente da lista pública, atualize as constantes em `database/supabase-only/0017_rate_card.sql` | supabase-only 0017 | opcional — funciona com preço público, atualize se tiver taxa negociada |
| DPIA / parecer jurídico | Contratar avaliação por jurisdição (percepção emocional, ADR-035) | PENDENCIAS_EXTERNAS "Jurídico" | bloqueante p/ mercados regulados |
| Upgrade plano Tavus | Se os créditos conversacionais esgotarem de novo (D-V2-067) | dashboard Tavus | funcionando hoje |
| Auto-deploy Railway↔GitHub | Conectar o repo no dashboard do Railway (Settings → Source) | dashboard Railway | recomendado — hoje cada release exige `railway up --service portal` manual (foi o caso desta sessão: 5 PRs mergeados só chegaram à produção depois de deploy manual explícito) |
| CI roda o e2e (15 specs Playwright) | ~~Cadastrar 4 secrets no GitHub~~ **RESOLVIDO 2026-08-01**: `SUPABASE_URL`, `SUPABASE_PUBLISHABLE_KEY`, `DEMO_EMAIL`, `DEMO_PASSWORD` cadastrados via `gh secret set`. Achado real ao verificar o run de verdade (não só o docs_qa.py local): o job estava quebrado desde D-V2-096 por dois bugs de workflow (`if` de job com `secrets` — contexto não permitido; pacotes do monorepo não buildados antes do e2e) — corrigidos e validados numa branch de teste antes de ir pra `main` (D-V2-098) | `.github/workflows/docs-qa.yml`, job `e2e-portal` | funcionando — 5 jobs verdes em todo push/PR pra `main`, incluindo os 15 specs Playwright reais |

Já conectados e funcionando: Supabase (auth+dados+RAG), OpenRouter (chat+embeddings), Tavus (vídeo por persona), ElevenLabs (voz via persona), Resend SMTP (e-mails de auth), Railway (deploy manual da `main`, confirmado 2026-07-22), GitHub (CI), domínio próprio `closer.axtroai.com` (confirmado 2026-08-01).
