# NEEDS_CONNECTION: o que depende só de você

Tudo abaixo já tem código, adapter, mock e teste prontos. Falta apenas a conexão indicada.

| Serviço | Variável ou ação necessária | Onde conectar | Status |
|---|---|---|---|
| ~~🔴 CI do GitHub bloqueado por billing~~ (achado 2026-08-14) | **RESOLVIDO entre 14/08 e 16/08**. Ficou vermelho de novo duas vezes desde então por regressões reais de produto (não billing): D-V2-131 (3 asserções de E2E desatualizadas pelo M5-01/M5-02) e D-V2-140 (2026-08-20, achado ao vivo: o rebrand da landing quebrou o focus trap do menu mobile: `visibility:hidden→visible` via CSS não fica focável no mesmo frame no Chromium; corrigido com duplo `requestAnimationFrame`). Ambas corrigidas e verificadas (19 specs de e2e local + gate completo) | github.com/settings/billing (só se voltar a bloquear) | resolvido: CI verde em `main` |
| Resend (e-mail de convite) | ~~`RESEND_API_KEY`~~ **RESOLVIDO 2026-07-31**: configurada no Railway | Railway → Variables | - |
| **Webhook de eventos da Resend** (achado D-V2-117, onda 8) | Código atual: `apps/portal/src/lib/email-webhook.ts` com verificação Svix e rota `/api/resend/webhook`, cobrindo `email.bounced`, `email.complained` e `email.delivery_delayed` sem endereço em log. Falta: cadastrar `https://closer.axtroai.com/api/resend/webhook`, configurar `RESEND_WEBHOOK_SECRET` e, em M6-05, persistir reconciliação idempotente também para `sent`, `delivered`, `failed`, `bounced` e `suppressed`. | dashboard.resend.com/webhooks + Railway Variables | aguardando cadastro manual e M6-05; aceite da API ainda não prova entrega final |
| **Outbox de notificação terminal de reunião, M6-01** | Implementação local pronta: migration 0057, worker, readiness, fake, batch isolado por destinatário e workflow manual protegido. Falta autorização para aplicar 0057, publicar o bridge v56/v57, configurar segredo, criar o GitHub Environment `production`, fixar a URL canônica e executar somente canary fake em ambiente isolado. Não há backfill histórico. A lease atual é global, então provider real, flag ligada em ambiente compartilhado, agenda e tráfego de clientes ficam bloqueados até M6-05 adicionar escopo de canary por tenant, quota, fairness, custo, reconciliação final, circuit breaker, backlog SLO e retenção. | Railway, Supabase e GitHub Actions; seguir `docs/operations/MEETING_TERMINAL_NOTIFICATION_DISPATCHER.md` | aguardando rollout staged com flag desligada e canary fake isolado; nenhum envio real autorizado; nada remoto foi alterado por M6-01 |
| Domínio próprio | ~~URL de produção era a raw `*.up.railway.app`~~ **RESOLVIDO 2026-08-01**: `closer.axtroai.com` conectado no Railway (Settings → Domains, feito por você fora desta sessão) | Railway → Settings → Domains | - |
| Portal URL nos e-mails e SEO | ~~`PORTAL_PUBLIC_URL` e `NEXT_PUBLIC_SITE_URL`~~ **RESOLVIDO 2026-08-01**: ambas atualizadas de `portal-production-b43e.up.railway.app` para `https://closer.axtroai.com` no Railway + redeploy (`NEXT_PUBLIC_SITE_URL` é build-time). Achado real: o domínio próprio já estava conectado, mas essas duas variáveis continuavam apontando pra URL crua: e-mails, sitemap, `og:url` e `<link rel="canonical">` ficariam com o domínio errado até o fix. Verificado ao vivo: `sitemap.xml`, `robots.txt`, `og:url` e canonical todos em `closer.axtroai.com` | Railway → Variables | - |
| Telefonia (Telnyx) | Conta Telnyx real + `TELNYX_API_KEY` + um Call Control App/`connection_id` provisionado + número. Adapter pronto em `packages/provider-telnyx/` (`createTelnyxPort`: discar chamada, SMS, status por polling e por webhook assinado Ed25519) desde 2026-08-24: fake determinístico testado (24 testes, zero rede), modo real é **hipótese de integração lida da OpenAPI pública da Telnyx, nunca validada contra sandbox** | `packages/provider-telnyx/` · isolado, ainda não conectado a nenhuma Server Action do portal (T10 em TASKS.md) | aguardando conta Telnyx real pra validar o modo real; fake mode utilizável hoje sem chave |
| **Google Calendar OAuth (ADR-039, onda 1b-ii)** | Código pronto e testado em modo fake: troca `code`→`refresh_token`/`access_token` (`exchangeGoogleAuthorizationCode` em `packages/provider-google-calendar/`), rota de callback `GET /api/google-calendar/oauth/callback`, Server Actions de conectar/desconectar (`apps/portal/src/lib/actions/calendar-connection.ts`), UI em Configurações (`calendar-section.tsx`): tudo isso já custodia o refresh token no Supabase Vault via `portal_connect_google_calendar_service` (0052). Falta: (1) `GOOGLE_OAUTH_CLIENT_ID`/`GOOGLE_OAUTH_CLIENT_SECRET` reais (Google Cloud Console → OAuth client ID tipo "Web application", redirect URI `https://closer.axtroai.com/api/google-calendar/oauth/callback` cadastrado exatamente); (2) provavelmente a revisão de app do Google pra escopos sensíveis de Calendar antes de sair do modo de teste interno. Sem isso, o botão "Conectar Google Calendar" falha fechado (`calendar_error=nao_configurado`); com `PORTAL_FAKE_PROVIDERS=1` o fluxo inteiro (state anti-CSRF, callback, RPC de conexão) já é demonstrável sem credencial real. ADR-039 já marca esta conexão como gate humano ALTO (custódia de credencial de terceiro). Nenhum tenant real deve conectar antes da revisão de segurança do fluxo OAuth que a própria ADR cita como trabalho à parte | Google Cloud Console → APIs & Services → Credentials + Railway → Variables | aguardando credencial real do Fernando; código e fake mode prontos hoje |
| Meet/Zoom/Teams (Recall.ai) | ~~Conta + `RECALL_API_KEY`~~ **RESOLVIDO 2026-07-31**: chave do Doppler e região `us-west-2` validadas; UI pronta. O antigo `RECALL_WEBHOOK_TOKEN` pode ser removido depois da troca de endpoints abaixo: M5-01 não confia em bearer na URL. | `/agentes/<id>/testar` → "Levar para uma reunião externa" | fluxo de criação funcional; release M5-01 depende dos dois itens Recall abaixo |
| ~~**Webhook assinado do Recall.ai**~~ | M5-01 tornou HMAC obrigatório. `RECALL_WEBHOOK_SECRET=whsec_...` **confirmada configurada no Railway** (verificado via `/api/ready`, check `recall_webhook_hmac:true`, 2026-08-20/D-V2-140). A rota ignora o query token, limita o body, deduplica `webhook-id` e aplica janela anti-replay. | - | resolvido |
| ~~Allowlist de transcript Recall~~ | **RESOLVIDO 2026-08-20 (D-V2-140)**: `RECALL_TRANSCRIPT_DOWNLOAD_HOSTS=recallai-production-bot-data.s3.amazonaws.com` configurada no Railway: host documentado oficialmente pela Recall (docs.recall.ai/v1.10/docs/storage-and-playback) pra URLs assinadas de transcript. Os 4 bots reais de teste (31/07) já tinham o artifact expirado na Recall quando isso foi resolvido, então o valor ainda não foi confirmado contra um evento `transcript.done` real. Validar no próximo. | Railway → Variables | resolvido, validação empírica pendente no próximo evento real |
| ~~Billing (Stripe)~~ | **Código pronto desde D-V2-101, migrations aplicadas, dispatcher M5-01 confirmado live em produção** (2026-08-20, D-V2-140): `/api/ready` confirma `billing_usage_outbox_flag:true`, `stripe_api_key/webhook_secret/meter_name/price_catalog:true` e um heartbeat real de `billing_usage` gravado pelo predeploy. `STRIPE_WEBHOOK_SECRET` foi copiada do Doppler (`axtro-human-digital-os`/`prd`) pro Railway. **O que ainda falta**: o dispatcher durável roda hoje só via o heartbeat sintético do predeploy: o worker AGENDADO de verdade (GitHub Actions `Dispatch Stripe billing usage`) nunca disparou porque `BILLING_DISPATCH_SCHEDULE_ENABLED` não existe como repo variable (confirmado `gh variable list` vazio, 2026-08-20). Ver linha de reconciliação abaixo. | GitHub → Actions repo variables | outbox/checkout/webhook live; dispatch agendado real ainda não roda |
| Telemetria (Sentry ou log drain dedicado) | Decisão de vendor + DSN/credencial | `apps/portal/src/lib/telemetry.ts` já centraliza e redige todo log: só falta o wiring do vendor escolhido | aguardando decisão (logs estruturados do Railway já funcionam como observabilidade mínima) |
| **Checkout via Stripe Connect (ADR-040, `request_checkout`, a 4ª tool de negócio)** | Código pronto e testado ponta a ponta em modo fake (conectar/desconectar a conta Stripe do tenant via OAuth, catálogo com preflight de preço vivo, aprovação humana obrigatória do `tenant_admin`, a orquestração real de dispatch→Checkout Session→commit→e-mail do link). Falta: (1) aplicar a migration `0069_business_action_checkout_stripe_connect.sql` em produção (ainda não aplicada em lugar nenhum); (2) `STRIPE_CONNECT_CLIENT_ID` real (dashboard.stripe.com/settings/connect/onboarding-options, habilitar "Onboard accounts with OAuth" primeiro — nota: a própria Stripe recomenda Connect Onboarding/Account Links em vez de OAuth para contas Standard novas, o ADR já decidiu OAuth conscientemente, mas vale revisar antes do próximo item); (3) `STRIPE_CONNECT_OAUTH_STATE_SECRET` (`openssl rand -hex 32`); (4) `STRIPE_CONNECT_WEBHOOK_SECRET` (endpoint de webhook separado do de assinatura, com "escutar eventos em contas conectadas" habilitado); (5) revisão de segurança e compliance dedicada do fluxo (custódia de credencial de terceiro + pagamento, duas das quatro categorias de risco ALTO do Fernando ao mesmo tempo); (6) só depois disso ligar `PORTAL_BUSINESS_ACTION_CHECKOUT_ENABLED` — ela não existe em nenhum ambiente hoje, e nenhum tenant deve conectar uma conta Stripe real antes da revisão. Telas de aprovação/catálogo/conexão são trabalho de produto fora do escopo de código do ADR-040. | `database/supabase-only/`, Stripe Dashboard → Connect, Railway → Variables, `docs/adr/ADR-040-closer-checkout-stripe-connect.md` | código completo aguardando gate humano de segurança/compliance antes de qualquer aplicação/ativação |
| **Workflow de governança de dados, M6-04 (ADR-046)** | Migration `0059_data_governance_disposition_workflow.sql` (13 RPCs: pedido, aprovação, política, cancelamento, status, vínculo de titular, inventário, legal hold, expiração) implementa a máquina de estados inteira do ADR-046 (exclusão/redação de titular, fechamento de tenant com dupla aprovação, legal hold, tombstone, exceção de backup), mas **nenhuma das 13 RPCs é chamada de nenhum lugar em `apps/` ou `packages/`** hoje. `apps/workflow-worker/src/data-governance.ts` (lado executor) já existe mas é código morto, fora do `railway.json`. Falta a camada de aplicação inteira: Server Actions/rotas pra abrir e acompanhar um pedido, tela de aprovação com quorum de dois operadores, e religar `resources.ts`/`transcripts.ts` (que hoje excluem agente/conhecimento/transcrição direto, sem passar por este workflow) pra chamar o novo fluxo em vez da RPC antiga. Migration 0059 **não pode ser aplicada** antes disso: ela desabilita as RPCs de exclusão direta que essas 3 ações ainda usam, quebrando-as. Achado e decisão explícita de sessão anterior (2026-09-02): "categoria banco de risco alto", deixado sem tocar até o Fernando confirmar escopo. Não é um fix pontual: é uma segunda camada de aplicação sobre uma migration de ~4000 linhas, no domínio de exclusão de dado de titular (LGPD/GDPR) com fechamento irreversível de tenant. | `docs/adr/ADR-046-data-governance-disposition-workflow.md`, `database/supabase-only/0059_data_governance_disposition_workflow.sql`, `apps/workflow-worker/src/data-governance.ts` | aguardando o Fernando decidir escopo (quem abre o pedido: self-service do tenant ou só operador Axtro; qual fatia construir primeiro) antes de qualquer código de aplicação novo |
| ~~**Reconciliação de efeitos Tavus/Recall e dispatch de billing**~~ (achado 2026-08-20, D-V2-140) | **Agendamento RESOLVIDO em 2026-09-04**: `PROVIDER_EFFECT_RECONCILE_SCHEDULE_ENABLED` e `BILLING_DISPATCH_SCHEDULE_ENABLED` confirmadas `true` via `gh variable list`; os dois workflows rodaram com sucesso de 04/09 até a manhã de 14/09. **Bloqueio de billing da conta GitHub de 14/09 a 18/09 RESOLVIDO hoje (2026-09-18)**: os dois workflows voltaram a rodar com sucesso a partir de ~19h35 UTC (confirmado via `gh run list`). Ressalva: o cron declarado é a cada 5min, mas a cadência real observada ainda está bem mais espaçada (1-3h) enquanto o agendador do GitHub Actions recupera prioridade normal do repo; `/api/ready` (`workers` check) pode oscilar pra `false` até isso normalizar, sem exigir nenhuma ação. | github.com/settings/billing | resolvido; confirmar cadência do cron volta ao normal nos próximos dias |
| ~~Crédito OpenRouter esgotado~~ (achado 2026-09-14) | **RESOLVIDO, confirmado pelo Fernando em 2026-09-18** (print do dashboard: US$47,26 disponível, pay-as-you-go). `total_usage` tinha ultrapassado `total_credits` (270/270, negativo) na conta usada por `OPENROUTER_API_KEY`, degradando toda geração de IA pro fallback estático; recarregado. | openrouter.ai/settings/credits | resolvido |
| **Observação/reconciliação de uso OpenRouter** | `AI_USAGE_RECONCILE_SECRET` **já configurada no Railway** (confirmado `/api/ready`, 2026-08-20): o backlog agregado read-only em `/api/internal/ai-usage` já funciona. A mutação por fatura não está remotamente exposta: `/api/internal/ai-usage/manual` retorna 404 sem ler body ou tocar o banco. M5-02 deve adicionar identidade de operador autenticada, dupla aprovação e receipt auditável antes de abrir qualquer mutação. | runbook `docs/operations/AI_USAGE_RECONCILIATION.md` | observação read-only funcional; reconciliação remota (mutação) segue adiada para M5-02 por decisão de design, não por config pendente |
| Rate card de custos (taxa negociada) | ~~Preços reais por unidade~~ **RESOLVIDO 2026-07-24 com preço público de tabela** (D-V2-078). Se você tiver taxa negociada diferente da lista pública, atualize as constantes em `database/supabase-only/0017_rate_card.sql` | supabase-only 0017 | opcional: funciona com preço público, atualize se tiver taxa negociada |
| DPIA / parecer jurídico | Contratar avaliação por jurisdição (percepção emocional, ADR-035) | PENDENCIAS_EXTERNAS "Jurídico" | bloqueante p/ mercados regulados |
| Upgrade plano Tavus | Se os créditos conversacionais esgotarem de novo (D-V2-067) | dashboard Tavus | funcionando hoje |
| ~~Auto-deploy Railway↔GitHub~~ | **RESOLVIDO** (confirmado 2026-09-15): `git push origin main` dispara build/deploy no Railway sozinho, sem `railway up` manual. Verificado ao vivo pelo ID de deployment mudando logo após o push. | - | resolvido |
| **Política edge da demo pública, M6-03** | Aplicar globalmente a política v3 exata para `POST /demo/start`, `POST /demo/command`, `POST /demo/end` e métodos GET/HEAD na árvore `/demo`, com teto ativo 32, fila zero e 429. Somente depois configurar a attestation canônica e o segredo HMAC. Readiness, bootstrap e runtime falham fechados quando o par está ausente, parcial ou divergente. | Edge/WAF da plataforma, Railway Variables e `docs/operations/PUBLIC_DEMO_ROLLOUT.md` | aguardando rollout remoto autorizado; nenhum limite, segredo ou attestation remota foi alterado por M6-03 |
| CI roda os E2E público e autenticado | A demo pública isolada não usa conta nem provider. A fixture autenticada usa `SUPABASE_URL`, `SUPABASE_PUBLISHABLE_KEY`, `E2E_TENANT_ADMIN_EMAIL` e `E2E_TENANT_ADMIN_PASSWORD`; o workflow mantém `DEMO_*` somente como fallback temporário de migração e agora falha quando a configuração está parcial. Os nomes remotos ainda precisam ser migrados no rollout autorizado de M6-03. Traces, screenshots e uploads estão desligados para impedir persistência de cookies bearer, estado assinado e conteúdo do tenant. | `.github/workflows/docs-qa.yml`, `apps/portal/e2e/` e `docs/operations/PUBLIC_DEMO_ROLLOUT.md` | specs novas foram escritas e typechecked; execução local em navegador aguarda a escolha de navegador do usuário e o gate remoto aguarda o próximo push autorizado |
| ~~**Migration `0044_runtime_bridge_integrity_repair.sql`**~~ (M5-03/Auditoria 360, D-V2-133) | **Aplicada em produção** (`ovctadcrvnfpgxzplupp`) a pedido do Fernando. `portal_schema_capabilities_service()` confirma `version:44` e `runtimeBridgeReceiptIntegrity:true`; advisor de segurança sem regressão (só o padrão INFO já esperado nas 3 tabelas novas, WARNs todos pré-existentes). | - | resolvido |
| ~~Migration `0046_provider_effect_termination_fence.sql`~~ (D-V2-136) | **Aplicada em produção** (`ovctadcrvnfpgxzplupp`): `portal_schema_capabilities_service()` confirmou `version:46` e `providerEffectTerminationFence:true`; RLS forçada e execução do RPC de término limitada a `service_role` foram verificadas. O resultado continua sendo aceite/pending do provider, não prova de silêncio de mídia. | - | resolvido |
| ~~Migration `0047_service_role_app_schema_usage.sql`~~ (D-V2-137) | **Aplicada em produção** (`ovctadcrvnfpgxzplupp`): capability confirmou `version:47`/`serviceRoleAppSchemaUsage:true`; a probe PostgREST tipada, inerte e sem dados reais retornou 200. O grant permanece limitado a `USAGE` de schema/domínio para `service_role`. | - | resolvido |
| ~~Migration `0048_tavus_stage_settlement_timestamp.sql`~~ (D-V2-138) | **Aplicada em produção**: a capability confirmou `version:48`/`tavusStageExpiryConcurrencyFence:true`; settle, resolve e revoke Tavus usam relógio de parede após lock e mantêm `updated_at` monotônico sem estender `expires_at`. | - | resolvido |
| ~~Flag `PORTAL_RUNTIME_BRIDGE_ENABLED`/`PORTAL_BUSINESS_ACTION_BRIDGE_ENABLED`~~ (M5-02/ADR-041) | **RESOLVIDO 2026-09-13 (D-V2-170)**: Fernando autorizou explicitamente ligar as duas em produção. Bridge de runtime (consentimento/disclosure/kill switches) e o funil de ação de negócio (`register_lead`/`propose_meeting_slots`/`confirm_meeting_slot`) estão `true` desde então, com as 3 tools registradas na conta Tavus real e anexadas às 4 personas ativas. Ressalva explícita do próprio Fernando: risco aceito pra ele testar com a própria empresa, **não** é liberação pra tráfego de cliente pagante. O P0 de media boundary abaixo segue igualmente aberto e não foi resolvido por isso. | Railway → Variables | resolvido (uso próprio); reavaliar antes de qualquer cliente real |
| **Flag `PORTAL_PROVIDER_TERMINATION_ENABLED`** (D-V2-136) | Ação "encerrar agora" (stop) de conversas Tavus/reuniões Recall está no código, com lease/recibo durável e autorização `tenant_admin`, mas a flag continua explicitamente `false`. O botão fica correto-e-inerte. **Deploy saudável confirmado em produção desde 2026-08-20 (D-V2-140)**: a última pré-condição técnica está resolvida; falta só o canário de terminação. Aceite do provider não prova silêncio físico de mídia tardia. | Railway → Variables | não ligar antes do canário de terminação |
| **P0 arquitetural: media boundary não provada** (Auditoria 360, AXTRO-360-09) | Tavus/Recall nunca provaram fim-a-fim que um cancelamento/barge-in real corta a geração de áudio/vídeo a tempo: sem essa prova, pode haver "fala tardia" vazando depois que o sistema já considerou o turno cancelado. **Bloqueia qualquer promoção de canal realtime pago pra produção.** Recomendação registrada: abrir tarefa arquitetural dedicada (adapter de mídia + canário com traces reais contra o sandbox do provider), não é um fix pontual de código | decisão de escopo/arquitetura do Fernando | aberto, não é algo que uma sessão decida sozinha |

Já conectados e funcionando: Supabase (auth+dados+RAG), OpenRouter (chat+embeddings, crédito esgotado agora, ver linha acima), Tavus (vídeo por persona), ElevenLabs (voz via persona), Recall.ai (bot de reunião + webhook HMAC + allowlist de transcript), Stripe (checkout+outbox+webhook+dispatch agendado, ver linha de reconciliação acima), Resend SMTP (e-mails de auth), Railway (auto-deploy no push pra `main`, confirmado 2026-09-15), GitHub (CI e workflows agendados voltaram a rodar com sucesso em 2026-09-18 depois do billing lock de 14/09 a 18/09, ver linha acima), domínio próprio `closer.axtroai.com` (confirmado 2026-08-01).
