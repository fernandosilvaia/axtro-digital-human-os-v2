# NEEDS_CONNECTION — o que depende só de você

Tudo abaixo já tem código, adapter, mock e teste prontos. Falta apenas a conexão indicada.

| Serviço | Variável ou ação necessária | Onde conectar | Status |
|---|---|---|---|
| ~~🔴 CI do GitHub bloqueado por billing~~ (achado 2026-08-14) | **RESOLVIDO entre 14/08 e 16/08** (verificado via `gh run view` em 18/08 — o push do D-V2-123 em 16/08 já rodou jobs reais, não mais "account locked"). Achado NOVO no lugar (D-V2-131, verificado ao vivo): o `main` remoto ficou vermelho de novo desde 16/08, mas por 3 asserções de E2E desatualizadas (labels de custo renomeadas pelo M5-01, botão "Enviar proposta" da Camila tornando um seletor ambíguo, consentimento obrigatório do M5-02 antes de "Iniciar apresentação") — não por regressão de produto nem billing. Corrigido nesta rodada; falta só fazer o push/merge pra `main` refletir o fix | github.com/settings/billing (só se voltar a bloquear) | fix local pronto, aguardando decisão de push/merge da branch `codex/m5-01-integrity-hardening` |
| Resend (e-mail de convite) | ~~`RESEND_API_KEY`~~ **RESOLVIDO 2026-07-31**: configurada no Railway | Railway → Variables | — |
| **Webhook de eventos da Resend** (achado D-V2-117, onda 8) | Código pronto: `apps/portal/src/lib/email-webhook.ts` (verificação de assinatura Svix) + rota `/api/resend/webhook` — trata `email.bounced`/`email.complained`/`email.delivery_delayed`, loga via telemetria (só `email_id`, nunca o endereço). Falta: (1) cadastrar o endpoint `https://closer.axtroai.com/api/resend/webhook` no dashboard da Resend (Webhooks → Add Endpoint), selecionando os 3 eventos acima; (2) colar o "Signing Secret" gerado como `RESEND_WEBHOOK_SECRET` no Railway | dashboard.resend.com/webhooks + Railway → Variables | aguardando cadastro manual — sem isso, um convite/alerta que falha DEPOIS de aceito pela Resend continua invisível pro produto (mitigado só parcialmente pelo retry condicional em erro transitório que já existe) |
| Domínio próprio | ~~URL de produção era a raw `*.up.railway.app`~~ **RESOLVIDO 2026-08-01**: `closer.axtroai.com` conectado no Railway (Settings → Domains, feito por você fora desta sessão) | Railway → Settings → Domains | — |
| Portal URL nos e-mails e SEO | ~~`PORTAL_PUBLIC_URL` e `NEXT_PUBLIC_SITE_URL`~~ **RESOLVIDO 2026-08-01**: ambas atualizadas de `portal-production-b43e.up.railway.app` para `https://closer.axtroai.com` no Railway + redeploy (`NEXT_PUBLIC_SITE_URL` é build-time). Achado real: o domínio próprio já estava conectado, mas essas duas variáveis continuavam apontando pra URL crua — e-mails, sitemap, `og:url` e `<link rel="canonical">` ficariam com o domínio errado até o fix. Verificado ao vivo: `sitemap.xml`, `robots.txt`, `og:url` e canonical todos em `closer.axtroai.com` | Railway → Variables | — |
| Telefonia (Telnyx) | Conta + `TELNYX_API_KEY` + número SIP | PENDENCIAS_EXTERNAS · sem adapter ainda (T10) | aguardando conta |
| Meet/Zoom/Teams (Recall.ai) | ~~Conta + `RECALL_API_KEY`~~ **RESOLVIDO 2026-07-31**: chave do Doppler e região `us-west-2` validadas; UI pronta. O antigo `RECALL_WEBHOOK_TOKEN` pode ser removido depois da troca de endpoints abaixo: M5-01 não confia em bearer na URL. | `/agentes/<id>/testar` → "Levar para uma reunião externa" | fluxo de criação funcional; release M5-01 depende dos dois itens Recall abaixo |
| **Webhook assinado do Recall.ai** | M5-01 tornou HMAC obrigatório. Substituir os dois endpoints antigos com `?token=...` por `https://closer.axtroai.com/api/recall/webhook`, manter somente os eventos suportados e confirmar `RECALL_WEBHOOK_SECRET=whsec_...` canônico no Railway. A rota ignora o query token, limita o body, deduplica `webhook-id` e aplica janela anti-replay. | https://us-west-2.recall.ai/dashboard/webhooks/ + Railway → Variables | **pendente antes do deploy M5-01**; `/api/ready` real permanece 503 com segredo inválido/ausente |
| Allowlist de transcript Recall | Configurar `RECALL_TRANSCRIPT_DOWNLOAD_HOSTS` com os hostnames exatos observados nas URLs assinadas da conta/região (sem wildcard, IP, porta ou URL). Começar pelo host S3 documentado apenas depois de confirmá-lo em um evento real. | Railway → Variables + evento `transcript.done` de teste | **pendente antes do deploy M5-01**; download falha fechado sem allowlist válida |
| Billing (Stripe) | **Código base pronto desde D-V2-101** e migrations `0025`-`0033` já aplicadas; o dispatcher durável M5-01 ainda não tocou produção. O rollout exige maintenance/drain, sem tráfego legado: (1) colocar o serviço público em manutenção na Railway e confirmar zero requests pagos em voo; (2) aplicar 0040 e 0041; (3) subir o candidato sem promover e exigir `/api/ready` v41; (4) recriar/verificar em test mode os Prices de **US$30/conversa** e atualizar os seis `STRIPE_PRICE_*`; (5) configurar outbox, webhook, reconciler e workflows; (6) executar manualmente ambos os workers e guardar evidência de backlog/DLQ/held/unknown/cleanup; (7) validar checkout → webhook → outbox → Meter; (8) só então promover e retirar manutenção. Uma falha mantém manutenção e só permite rollback para código compatível com v41 — nunca o artefato legado contra schema v41. **Gate humano explícito; nenhuma cobrança ou mudança live foi autorizada.** | dashboard.stripe.com + Railway → Variables + GitHub → Actions secrets/variables | aguardando maintenance/drain, migrations, catálogo de teste, env vars e preflight manual |
| Telemetria (Sentry ou log drain dedicado) | Decisão de vendor + DSN/credencial | `apps/portal/src/lib/telemetry.ts` já centraliza e redige todo log — só falta o wiring do vendor escolhido | aguardando decisão (logs estruturados do Railway já funcionam como observabilidade mínima) |
| Reconciliação de efeitos Tavus/Recall | Código, endpoint automático protegido, fila durável e workflow prontos em M5-01. Após o rollout em manutenção: configurar no Railway `PROVIDER_EFFECT_RECONCILER_ENABLED=true` e segredo aleatório; no GitHub, configurar URL/secret; executar primeiro o workflow manual e observar backlog/DLQ. A rota HTTP manual foi deliberadamente fechada (404): o segredo do scheduler não pode liberar efeito ambíguo. Uma futura exceção exige identidade de operador, aprovação independente e recibo de evidência auditável. | Railway → Variables + GitHub → Actions; runbook `docs/operations/PROVIDER_EFFECT_RECONCILIATION.md` | aguardando migrations e preflight; schedule falha fechado e nenhum TTL libera gasto desconhecido |
| **Observação/reconciliação de uso OpenRouter** | Após o rollout M5-01, gerar um segredo aleatório exclusivo (mínimo 24 caracteres) em `AI_USAGE_RECONCILE_SECRET` no Railway somente para o backlog agregado read-only em `/api/internal/ai-usage`. A mutação por fatura não está remotamente exposta: `/api/internal/ai-usage/manual` retorna 404 sem ler body ou tocar o banco. M5-02 deve adicionar identidade de operador autenticada, dupla aprovação e receipt auditável antes de abrir qualquer mutação. | Railway → Variables; runbook `docs/operations/AI_USAGE_RECONCILIATION.md` | observação aguarda migration/rollout e segredo; reconciliação remota adiada para M5-02, e `unknown` permanece bloqueante sem TTL/liberação automática |
| Rate card de custos (taxa negociada) | ~~Preços reais por unidade~~ **RESOLVIDO 2026-07-24 com preço público de tabela** (D-V2-078) — se você tiver taxa negociada diferente da lista pública, atualize as constantes em `database/supabase-only/0017_rate_card.sql` | supabase-only 0017 | opcional — funciona com preço público, atualize se tiver taxa negociada |
| DPIA / parecer jurídico | Contratar avaliação por jurisdição (percepção emocional, ADR-035) | PENDENCIAS_EXTERNAS "Jurídico" | bloqueante p/ mercados regulados |
| Upgrade plano Tavus | Se os créditos conversacionais esgotarem de novo (D-V2-067) | dashboard Tavus | funcionando hoje |
| Auto-deploy Railway↔GitHub | Conectar o repo no dashboard do Railway (Settings → Source) | dashboard Railway | recomendado — hoje cada release exige `railway up --service portal` manual (foi o caso desta sessão: 5 PRs mergeados só chegaram à produção depois de deploy manual explícito) |
| CI roda o e2e (18 specs Playwright) | ~~Cadastrar 4 secrets no GitHub~~ **RESOLVIDO 2026-08-01**: `SUPABASE_URL`, `SUPABASE_PUBLISHABLE_KEY`, `DEMO_EMAIL`, `DEMO_PASSWORD` cadastrados via `gh secret set`. Achado real ao verificar o run de verdade (não só o docs_qa.py local): o job estava quebrado desde D-V2-096 por dois bugs de workflow (`if` de job com `secrets` — contexto não permitido; pacotes do monorepo não buildados antes do e2e) — corrigidos e validados numa branch de teste antes de ir pra `main` (D-V2-098). Contagem subiu de 15 pra 18 com `public-discovery.spec.ts` (M5-03, D-V2-130) | `.github/workflows/docs-qa.yml`, job `e2e-portal` | os 18 specs passam localmente (verificado 18/08, D-V2-131) — falta o push/merge pra `main` rodar isso no CI de novo |
| ~~**Migration `0044_runtime_bridge_integrity_repair.sql`**~~ (M5-03/Auditoria 360, D-V2-133) | **Aplicada em produção** (`ovctadcrvnfpgxzplupp`) a pedido do Fernando. `portal_schema_capabilities_service()` confirma `version:44` e `runtimeBridgeReceiptIntegrity:true`; advisor de segurança sem regressão (só o padrão INFO já esperado nas 3 tabelas novas, WARNs todos pré-existentes). | — | resolvido |
| **Flag `PORTAL_RUNTIME_BRIDGE_ENABLED`** (M5-02) | Bridge de runtime (consentimento/disclosure/kill switches pra canais com provider) está no código e no schema (v44 desde D-V2-133), mas a flag default continua `false` — ninguém usa ainda. Decisão de quando ligar depende só de resolução (ou aceite explícito do risco) do P0 de media boundary abaixo — a migration 0044 já não é mais bloqueio | Railway → Variables | não ligar antes de decisão sobre o P0 |
| **P0 arquitetural: media boundary não provada** (Auditoria 360, AXTRO-360-09) | Tavus/Recall nunca provaram fim-a-fim que um cancelamento/barge-in real corta a geração de áudio/vídeo a tempo — sem essa prova, pode haver "fala tardia" vazando depois que o sistema já considerou o turno cancelado. **Bloqueia qualquer promoção de canal realtime pago pra produção.** Recomendação registrada: abrir tarefa arquitetural dedicada (adapter de mídia + canário com traces reais contra o sandbox do provider) — não é um fix pontual de código | decisão de escopo/arquitetura do Fernando | aberto, não é algo que uma sessão decida sozinha |

Já conectados e funcionando: Supabase (auth+dados+RAG), OpenRouter (chat+embeddings), Tavus (vídeo por persona), ElevenLabs (voz via persona), Resend SMTP (e-mails de auth), Railway (deploy manual da `main`, confirmado 2026-07-22), GitHub (CI), domínio próprio `closer.axtroai.com` (confirmado 2026-08-01).
