# Backlog

`MVP_TASK_GRAPH.yaml` is the executable source of tasks and dependencies. The Codex must update task status in `PROGRESS.md`, not edit task dependencies to make progress appear easier.

A task can be split into smaller PRs, but its acceptance criteria remain mandatory. A dependency may only be marked green with test or artifact evidence.
