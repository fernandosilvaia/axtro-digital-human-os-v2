# Contracts instructions
- JSON Schema 2020-12.
- Every object uses `additionalProperties: false`.
- Required fields are explicit.
- Every schema has one valid and one invalid example.
- Breaking changes require new schema version and compatibility note.
- Run `python3 scripts/validate_contracts.py` after any change.
